
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let const_sinceKey:String = "user/shall tee delivery connect procedure"
fileprivate let main_gapText:String = "teInfomay value"

/*: "gallery/getGalleryByUid" :*/
fileprivate let data_urgeMsg:[Character] = ["g","a","l","l","e","r","y","/","g","e"]
fileprivate let user_marriedFormat:[Character] = ["t","G","a","l","l","e","r"]
fileprivate let dataDuringName:[Character] = ["y","B","y","U","i","d"]

/*: "gallery/upload" :*/
fileprivate let mainFormalText:[Character] = ["g","a","l","l","e"]
fileprivate let userBoldUrl:String = "R"
fileprivate let dataResponseName:String = "y/uploadplay conversation concern yesterday"

/*: "gallery/deletePic" :*/
fileprivate let show_pastStr:[Character] = ["g","a","l","l","e"]
fileprivate let data_objectTitle:[Character] = ["r","y","/","d","e","l","e","t","e","P","i","c"]

/*: "user/tagDelete" :*/
fileprivate let kPopularityCheekKey:String = "check gross height shape noseuser/"
fileprivate let kHelloStr:[Character] = ["t","a"]
fileprivate let userTipUrl:String = "unit prospect worker closegDelete"

/*: "mf/user/tagEdit" :*/
fileprivate let mainTinPath:[Character] = ["m","f","/","u","s","e","r","/","t","a","g"]
fileprivate let mainCommercialId:String = "then presentEdit"

/*: "app/feedback" :*/
fileprivate let k_sharedStr:String = "range harvest unknownapp/f"
fileprivate let app_opTitle:[Character] = ["e","e","d","b","a","c","k"]

/*: "IM/getMyBlackList" :*/
fileprivate let appThemPath:String = "particular grant domainIM/g"
fileprivate let app_circlePossibleId:String = "stat discover exclusiveBlac"

/*: "mf/user/getSetting" :*/
fileprivate let kBackStillMessage:String = "mf/usion capture professional"
fileprivate let mainMeasureId:[Character] = ["e","t","t","i","n","g"]

/*: "mf/user/setConfig" :*/
fileprivate let kSoldierMsg:String = "damage animamf/us"
fileprivate let userSampleMessage:String = "task against say meet meetCon"
fileprivate let const_giftTitle:[Character] = ["f","i","g"]

/*: "mf/user/settingStatus" :*/
fileprivate let showCastEvaluateData:[Character] = ["m","f","/","u","s"]
fileprivate let app_actAirName:[Character] = ["e","r","/","s","e","t","t","i","n","g","S","t","a","t","u","s"]

/*: "type" :*/
fileprivate let appMysteryName:[UInt8] = [0xba,0xb7,0xbe,0xab]

private func sumFormation(clearly num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "user/lockAccount" :*/
fileprivate let dataAgentUrl:String = "reach hood regulate pan examineuser/l"
fileprivate let constYaFollowingPath:[Character] = ["o","u","n","t"]

/*: "mf/user/editInfo" :*/
fileprivate let noti_cancerData:String = "baseball effect tension second classmf/use"
fileprivate let app_vidMsg:String = "tInfoeasy burn decide editor resistance"

/*: "mf/userSign/list" :*/
fileprivate let noti_dockValue:String = "lab po choice safe hellomf/userSi"
fileprivate let show_upName:[Character] = ["g","n","/","l","i","s","t"]

/*: "mf/userSign/signIn" :*/
fileprivate let dataEffFormat:[Character] = ["m","f","/","u","s","e","r","S","i","g","n","/","s","i","g","n","I","n"]

/*: "mf/dating/saveVideo" :*/
fileprivate let constNoticePath:[Character] = ["m","f","/","d","a","t","i","n","g","/","s","a","v","e","V","i"]
fileprivate let appShouldTitle:[Character] = ["d","e","o"]

/*: "mf/dating/getConfig" :*/
fileprivate let mainModifyValue:String = "mf/dcollection far"
fileprivate let const_bigMessage:String = "concern explain/getC"

/*: "mf/dating/deleteVideo" :*/
fileprivate let user_taFunDefinitionValue:String = "mf/dpass hand over clock"
fileprivate let show_faultName:String = "dinformlinformtinform"
fileprivate let appStatusKey:[Character] = ["V","i","d","e","o"]

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let appDevoteAcquireName:String = "mf/grevar in gift"
fileprivate let data_broadId:String = "year install cos a strokeateA"
fileprivate let noti_aroundFullDominantData:String = "Remarcarrier resistance title transaction retain"
fileprivate let k_liverValue:String = "global"

/*: "mf/greet/list" :*/
fileprivate let mainOurMsg:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let constMutualName:String = "bag transport/list"

/*: "mf/greet/add" :*/
fileprivate let constCharmTitle:String = "easef"
fileprivate let show_becomeData:String = "/greetnet calendar"

/*: "mf/greet/del" :*/
fileprivate let data_appropriateMsg:String = "foundation implement resource succeedmf/gre"

/*: "mf/greet/getSettings" :*/
fileprivate let k_deadlineValue:String = "execute commit paper skin futuremf/greet/"
fileprivate let kVariationKey:String = "outside"
fileprivate let main_composeAffectPath:[Character] = ["t","S","e","t","t","i","n","g","s"]

/*: "mf/greet/extraSetting" :*/
fileprivate let notiCameraStr:[Character] = ["m","f","/","g","r","e","e","t","/","e","x","t","r"]
fileprivate let show_convertKey:String = "aSettingpersonal option minimum snow distribution"

/*: "game/list" :*/
fileprivate let user_recommendationUrl:[Character] = ["g","a"]
fileprivate let const_voiceFormat:String = "me/listjoin create security answer deny"

/*: "category" :*/
fileprivate let data_flexibleMessage:[UInt8] = [0x3,0x1,0x14,0x5,0x7,0xf,0x12,0x19]

private func changeResource(after num: UInt8) -> UInt8 {
    return num ^ 96
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GuidanceRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class GuidanceRequestTool: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bright(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (String(const_sinceKey.prefix(5)) + "upda" + String(main_gapText.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func birthday(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (String(data_urgeMsg) + String(user_marriedFormat) + String(dataDuringName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func uploadStatistical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (String(mainFormalText) + userBoldUrl.lowercased() + String(dataResponseName.prefix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func recording(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(show_pastStr) + String(data_objectTitle))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func buildBecome(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(kPopularityCheekKey.suffix(5)) + String(kHelloStr) + String(userTipUrl.suffix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func fit(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(mainTinPath) + String(mainCommercialId.suffix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func limitedTalk(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(k_sharedStr.suffix(5)) + String(app_opTitle))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func valueColumn(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(appThemPath.suffix(4)) + "etMy" + String(app_circlePossibleId.suffix(4)) + "kList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sumBroadcast(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(kBackStillMessage.prefix(5)) + "er/getS" + String(mainMeasureId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func feed(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(kSoldierMsg.suffix(5)) + "er/s" + String(userSampleMessage.suffix(5)) + String(const_giftTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func constraint(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(showCastEvaluateData) + String(app_actAirName))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: appMysteryName.map{sumFormation(clearly: $0)}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func customer(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(dataAgentUrl.suffix(6)) + "ockAcc" + String(constYaFollowingPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func light(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(noti_cancerData.suffix(6)) + "r/edi" + String(app_vidMsg.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func alongside(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(noti_dockValue.suffix(9)) + String(show_upName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func inward(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(dataEffFormat))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func isTv(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(constNoticePath) + String(appShouldTitle))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func same(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(mainModifyValue.prefix(4)) + "ating" + String(const_bigMessage.suffix(5)) + "onfig")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func singleDoingCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(user_taFunDefinitionValue.prefix(4)) + "ating/" + show_faultName.replacingOccurrences(of: "inform", with: "e") + String(appStatusKey))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func reward(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(appDevoteAcquireName.prefix(6)) + "et/upd" + String(data_broadId.suffix(4)) + "udio" + String(noti_aroundFullDominantData.prefix(5)) + k_liverValue.replacingOccurrences(of: "global", with: "k"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func theme(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(mainOurMsg) + String(constMutualName.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func innerRevenue(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (constCharmTitle.replacingOccurrences(of: "ease", with: "m") + String(show_becomeData.prefix(6)) + "/add")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func panel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(appDevoteAcquireName.prefix(6)) + "et/upd" + String(data_broadId.suffix(4)) + "udio" + String(noti_aroundFullDominantData.prefix(5)) + k_liverValue.replacingOccurrences(of: "global", with: "k"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bringForth(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(data_appropriateMsg.suffix(6)) + "et/del")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func collection(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(k_deadlineValue.suffix(9)) + kVariationKey.replacingOccurrences(of: "outside", with: "ge") + String(main_composeAffectPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func transactionToo(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(notiCameraStr) + String(show_convertKey.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func earnWithinDifferentialCost(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(user_recommendationUrl) + String(const_voiceFormat.prefix(7)))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: data_flexibleMessage.map{changeResource(after: $0)}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
