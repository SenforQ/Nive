
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_dockCheckName:[UInt8] = [0x42,0x45,0x42,0x5f,0x3,0x48,0x44,0x4f,0x4e,0x59,0x11,0x2,0xb,0x43,0x4a,0x58,0xb,0x45,0x44,0x5f,0xb,0x49,0x4e,0x4e,0x45,0xb,0x42,0x46,0x5b,0x47,0x4e,0x46,0x4e,0x45,0x5f,0x4e,0x4f]

private func mobileFault(dry num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "Cancel" :*/
fileprivate let k_intoInvestMsg:String = "makeup ticket ban twoCancel"

/*: "Submit" :*/
fileprivate let appYaUrl:[Character] = ["S","u","b","m","i"]
fileprivate let app_sizeData:[Character] = ["t"]

/*: "Please tell us your problems                   " :*/
fileprivate let k_itsId:[UInt8] = [0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x73,0x6d,0x65,0x6c,0x62,0x6f,0x72,0x70,0x20,0x72,0x75,0x6f,0x79,0x20,0x73,0x75,0x20,0x6c,0x6c,0x65,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "You need to choose at least one." :*/
fileprivate let const_processingText:[UInt8] = [0x50,0x66,0x7c,0x29,0x67,0x6c,0x6c,0x6d,0x29,0x7d,0x66,0x29,0x6a,0x61,0x66,0x66,0x7a,0x6c,0x29,0x68,0x7d,0x29,0x65,0x6c,0x68,0x7a,0x7d,0x29,0x66,0x67,0x6c,0x27]

/*: "Please tell us your problems." :*/
fileprivate let show_weightRevenueData:String = "frame board mic editor relationPleas"
fileprivate let userAnyoneDataId:[Character] = ["e"," ","t","e","l","l"," ","u","s"," ","y","o","u","r"," ","p","r","o","b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let const_attributeStr:String = "app/ulisten interrupt must exception"
fileprivate let mainHarassmentMsg:String = "esaleort"

/*: "reason" :*/
fileprivate let app_refuseId:[Character] = ["r","e","a","s","o"]
fileprivate let app_translationValue:[Character] = ["n"]

/*: "desc" :*/
fileprivate let userMicMsg:[UInt8] = [0x3e,0x3f,0x4d,0x3d]

fileprivate func prisonWed(fragment num: UInt8) -> UInt8 {
    let value = Int(num) + 38
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let app_concludeMsg:String = "mf/momenstrike pose sheer"
fileprivate let show_peerFormat:String = "injury sibling object satisfyt/report"

/*: "momentId" :*/
fileprivate let kAnnouncementStr:[UInt8] = [0x41,0x43,0x41,0x49,0x42,0x58,0x65,0x48]

private func titleurePush(box num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "mf/momentReply/report" :*/
fileprivate let show_amText:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let noti_bounceStr:[Character] = ["a","p","p","/","v","i","d","e","o"]
fileprivate let mainLiverText:[Character] = ["R","e","p","o","r","t"]

/*: "logId" :*/
fileprivate let dataCarvePath:[UInt8] = [0x85,0x86,0x8e,0xa0,0x8d]

/*: "Submitted successfully" :*/
fileprivate let show_fileMessage:[Character] = ["S","u","b","m","i","t","t","e","d"," ","s","u","c"]
fileprivate let kSickMessage:[Character] = ["c"]
fileprivate let mainRatingMsg:[Character] = ["e","s","s","f","u","l","l","y"]

/*: "Nudity" :*/
fileprivate let main_colorHistoryPath:String = "favorite aspect popular keyNudity"

/*: "Violence" :*/
fileprivate let userHoneyTitle:[Character] = ["V","i","o","l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let noti_positValue:String = "me expectedHaras"
fileprivate let k_beamMinuteName:String = "ex"

/*: "Suicide or Self-injury" :*/
fileprivate let k_harvestUrl:[Character] = ["S","u","i","c","i","d","e"," ","o"]
fileprivate let kMenuUrl:[Character] = ["r"," ","S","e"]
fileprivate let mainEachOthersData:String = "lf-inenterprise square kit others input"

/*: "False Information" :*/
fileprivate let appSphereMessage:[Character] = ["F","a","l","s","e"," ","I","n","f","o","r"]
fileprivate let notiHeFileValue:String = "matisubject"

/*: "Spam" :*/
fileprivate let notiItsLightName:String = "Spamwhere past"

/*: "Hate Speech" :*/
fileprivate let kLinkTitle:[Character] = ["H","a","t","e"," ","S","p","e","e","c","h"]

/*: "Terrorism" :*/
fileprivate let userMoreMessage:[Character] = ["T","e","r","r","o","r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let notiOppositeUrl:[UInt8] = [0x7c,0x4e,0x43,0x4a,0xf,0x40,0x49,0xf,0x66,0x43,0x43,0x4a,0x48,0x4e,0x43,0xf,0x40,0x5d,0xf,0x7d,0x4a,0x48,0x5a,0x43,0x4e,0x5b,0x4a,0x4b,0xf,0x68,0x40,0x40,0x4b,0x5c]

/*: "Sending disgusting messages" :*/
fileprivate let mainOccurNapKey:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s","g","u","s","t","i","n","g"," ","m","e","s"]
fileprivate let const_cancelPath:[Character] = ["s","a","g","e","s"]

/*: "Using photos of nudity or drugs" :*/
fileprivate let userResolutionUrl:[UInt8] = [0x60,0x46,0x5c,0x5b,0x52,0x15,0x45,0x5d,0x5a,0x41,0x5a,0x46,0x15,0x5a,0x53,0x15,0x5b,0x40,0x51,0x5c,0x41,0x4c,0x15,0x5a,0x47,0x15,0x51,0x47,0x40,0x52,0x46]

private func mSize(raw num: UInt8) -> UInt8 {
    return num ^ 53
}

/*: "Harassing or sending ads" :*/
fileprivate let user_heartTitle:String = "Harasmedal step weak"
fileprivate let noti_minutePath:[Character] = ["s","i"]
fileprivate let noti_pureUrgeUrl:String = "ng or database opposition during reference"
fileprivate let k_interestValue:String = "once first exception build sceneing ads"

/*: "Defraud or using fake info" :*/
fileprivate let const_clingProblemCommunicationData:String = "coordinator toward inside have originalDefrau"
fileprivate let const_mTitle:String = "tender click milk enterprise nightsing f"
fileprivate let notiSucceedId:String = "bottom portion comment arrowake info"

/*: "Others" :*/
fileprivate let mainExposureId:String = "Othersdata street content statistical manual"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExtraView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum AdditionCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ExtraView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: AdditionCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [PositReactiveCompatible] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = PositReactiveCompatible()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: AdditionCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        fee()
        //: setupSubViewsConstraint()
        before()
        //: setupReasons()
        rangeSetupCorner()
        //: bindInteraction()
        stick()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_dockCheckName.map{mobileFault(dry: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(k_intoInvestMsg.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.retirementProgram(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.shareOff(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.retirementProgram().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(measure), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(appYaUrl) + String(app_sizeData)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.shareOff(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.textStud(colors: UIColor.photoByModify(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(choose), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: show_explainId - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.itInstall(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = YesterdayViewDelegate(placeholder: String(bytes: k_itsId.reversed(), encoding: .utf8)!.localized, placeholderColor: UIColor.eigenvalueOfAMatrix(), frame: CGRect(x: 10, y: 10, width: show_explainId - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .shareOff(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.aircraft()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .shareOff(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.itInstall(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(CellThen.self, forCellReuseIdentifier: CellThen.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ExtraView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func funcSpeakerStrive() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func showThroughView(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = DelineateThen.getWindow()
            let window = DelineateThen.premiumOperation()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func translate(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func measure() {
        //: self.dismissReportView(succeed: false)
        self.translate(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func choose() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.diskTerms(showMsg: String(bytes: const_processingText.map{$0^9}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.diskTerms(showMsg: (String(show_weightRevenueData.suffix(5)) + String(userAnyoneDataId)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(const_attributeStr.prefix(5)) + "serR" + mainHarassmentMsg.replacingOccurrences(of: "sale", with: "p"))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (String(app_refuseId) + String(app_translationValue)): selectedModel.type, String(bytes: userMicMsg.map{prisonWed(fragment: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(app_concludeMsg.prefix(8)) + String(show_peerFormat.suffix(8)))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: kAnnouncementStr.map{titleurePush(box: $0)}, encoding: .utf8)!: tempId, (String(app_refuseId) + String(app_translationValue)): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(show_amText))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (String(app_refuseId) + String(app_translationValue)): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(noti_bounceStr) + String(mainLiverText))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: dataCarvePath.map{$0^233}, encoding: .utf8)!: tempId, (String(app_refuseId) + String(app_translationValue)): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        EnsureReactiveCompatible.habit(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.sirenSong(showMsg: (String(show_fileMessage) + String(kSickMessage) + String(mainRatingMsg)).localized)
                //: self.dismissReportView(succeed: true)
                self.translate(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ExtraView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: CellThen.className()) as! CellThen
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.remain(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ExtraView {
    /// 添加视图
    //: private func setupSubviews() {
    private func fee() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func before() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func rangeSetupCorner() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(main_colorHistoryPath.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(userHoneyTitle)).localized,
                           //: "Harassment".localized,
                           (String(noti_positValue.suffix(5)) + "smen" + k_beamMinuteName.replacingOccurrences(of: "ex", with: "t")).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(k_harvestUrl) + String(kMenuUrl) + String(mainEachOthersData.prefix(5)) + "jury").localized,
                           //: "False Information".localized,
                           (String(appSphereMessage) + notiHeFileValue.replacingOccurrences(of: "subject", with: "on")).localized,
                           //: "Spam".localized,
                           (String(notiItsLightName.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(kLinkTitle)).localized,
                           //: "Terrorism".localized,
                           (String(userMoreMessage)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: notiOppositeUrl.map{$0^47}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(mainOccurNapKey) + String(const_cancelPath)).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: userResolutionUrl.map{mSize(raw: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(user_heartTitle.prefix(5)) + String(noti_minutePath) + String(noti_pureUrgeUrl.prefix(6)) + "send" + String(k_interestValue.suffix(7))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(const_clingProblemCommunicationData.suffix(6)) + "d or u" + String(const_mTitle.suffix(6)) + String(notiSucceedId.suffix(8))).localized,
                           //: "Others".localized]
                           (String(mainExposureId.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = AppManager.share.appConfigMode.videoReport["reason"] {
            if let reason = AppManager.share.appConfigMode.videoReport[(String(app_refuseId) + String(app_translationValue))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = PositReactiveCompatible()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func stick() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
