
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_merelyMsg:[UInt8] = [0x40,0x47,0x40,0x5d,0x1,0x4a,0x46,0x4d,0x4c,0x5b,0x13,0x0,0x9,0x41,0x48,0x5a,0x9,0x47,0x46,0x5d,0x9,0x4b,0x4c,0x4c,0x47,0x9,0x40,0x44,0x59,0x45,0x4c,0x44,0x4c,0x47,0x5d,0x4c,0x4d]

private func blindLicense(tender num: UInt8) -> UInt8 {
    return num ^ 41
}

/*: "Text greeting" :*/
fileprivate let dataAppropriateFormat:String = "Text early may"
fileprivate let const_neverPlanKey:String = "handle"

/*: "Add" :*/
fileprivate let data_fromCapId:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let notiHugeKey:String = "priorityype"

/*: "addStatus" :*/
fileprivate let mainFrameworkStr:String = "addStbench task eff access"

/*: "list" :*/
fileprivate let user_agoTitle:String = "libehavior"

/*: "Are you sure to delete this message？" :*/
fileprivate let constCompareSocialFormat:[UInt8] = [0x9b,0xa8,0xbf,0xfa,0xa3,0xb5,0xaf,0xfa,0xa9,0xaf,0xa8,0xbf,0xfa,0xae,0xb5,0xfa,0xbe,0xbf,0xb6,0xbf,0xae,0xbf,0xfa,0xae,0xb2,0xb3,0xa9,0xfa,0xb7,0xbf,0xa9,0xa9,0xbb,0xbd,0xbf,0x35,0x66,0x45]

private func mainAge(margin num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "Cancel" :*/
fileprivate let const_acceptStr:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let show_bucketTitle:String = "sure instance yesterday rem newDelete"

/*: "The upper limit has been reached" :*/
fileprivate let dataComponentStr:[UInt8] = [0x95,0xa9,0xa4,0xe1,0xb4,0xb1,0xb1,0xa4,0xb3,0xe1,0xad,0xa8,0xac,0xa8,0xb5,0xe1,0xa9,0xa0,0xb2,0xe1,0xa3,0xa4,0xa4,0xaf,0xe1,0xb3,0xa4,0xa0,0xa2,0xa9,0xa4,0xa5]

private func betterLooking(here num: UInt8) -> UInt8 {
    return num ^ 193
}

/*: "text" :*/
fileprivate let dataOccurPortBehaviorMessage:[Character] = ["t","e","x","t"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let constTemporaryPath:[UInt8] = [0xc9,0xef,0xf9,0xf9,0xff,0xe9,0xe9,0xfc,0xef,0xf6,0xf6,0xe3,0xba,0xfb,0xfe,0xfe,0xff,0xfe,0xb6,0xba,0xea,0xf6,0xff,0xfb,0xe9,0xff,0xba,0xed,0xfb,0xf3,0xee,0xba,0xfc,0xf5,0xe8,0xba,0xfb,0xea,0xea,0xe8,0xf5,0xec,0xfb,0xf6]

private func honeyStack(medication num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "Any-Hex/Java" :*/
fileprivate let kDiscTitle:String = "apply lens softenAny-Hex"
fileprivate let notiDraftBoxText:[Character] = ["/","J","a","v","a"]

/*: "#FF506D" :*/
fileprivate let userDiscoverName:String = "assert moment girl#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let main_recognizeMessage:String = "attention certain trapbtn_me"
fileprivate let user_conditionBindDotPath:String = "e_deletecondition profile remark"
fileprivate let const_dueTrapStr:[Character] = ["_","n","o","r"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let userGuideStr:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63,0xa,0x64,0x64,0x61,0x20,0x6f,0x74,0x20,0x77,0x6f,0x6c,0x65,0x62,0x20,0x6e,0x6f,0x74,0x74,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let data_journeyMsg:[UInt8] = [0x23,0x1c,0x11,0x1a,0x54,0x15,0x10,0x10,0x1d,0x1a,0x13,0x54,0x19,0x1,0x18,0x0,0x1d,0x4,0x18,0x11,0x54,0x0,0x11,0xc,0x0,0x54,0x19,0x11,0x7,0x7,0x15,0x13,0x11,0x58,0x54,0x0,0x1c,0x11,0x54,0x7,0xd,0x7,0x0,0x54,0x11,0x19,0x54,0x3,0x1d,0x18,0x18,0x54,0x7,0x11,0x1a,0x10,0x54,0x0,0x1c,0x11,0x19,0x54,0x16,0xd,0x54,0x0,0x1,0x6,0x1a,0x7,0x4f,0x7,0x18,0x1d,0x10,0x11,0x54,0x0,0x1b,0x54,0x0,0x1c,0x11,0x54,0x19,0x11,0x7,0x7,0x15,0x13,0x11,0x54,0x0,0x1b,0x54,0x10,0x11,0x18,0x11,0x0,0x11]

private func bothCur(why num: UInt8) -> UInt8 {
    return num ^ 116
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiddleContentVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class MiddleContentVc: MirrorRealmReactiveCompatible {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: DraftTextView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_merelyMsg.map{blindLicense(tender: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(dataAppropriateFormat.prefix(5)) + "greetin" + const_neverPlanKey.replacingOccurrences(of: "handle", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.referenceAndNonsolidColour()
        //: designView()
        weightAir()
        //: getAudioListData()
        comment()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [BotEchoGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [BotEchoGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(data_fromCapId)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.shareOff(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.textStud(colors: UIColor.photoByModify(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(have), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension MiddleContentVc {
    //: func getAudioListData() {
    func comment() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(notiHugeKey.replacingOccurrences(of: "priority", with: "t"))] = 2
        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        GuidanceRequestTool.theme(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.upgrade()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(mainFrameworkStr.prefix(5)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(user_agoTitle.replacingOccurrences(of: "behavior", with: "st"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [BotEchoGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<BotEchoGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func output(index: Int) {
        //: let config = ShowAlertConfig()
        let config = EditAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.shareOff(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        PulseReactiveCompatible.boxMinimize(message: String(bytes: constCompareSocialFormat.map{mainAge(margin: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(const_acceptStr)).localized, rightBtnTitle: (String(show_bucketTitle.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            PulseReactiveCompatible.carveOutOnicial()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            PulseReactiveCompatible.carveOutOnicial()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: BotEchoGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            UniversalProgressHUD.addUp()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            GuidanceRequestTool.bringForth(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                UniversalProgressHUD.coverBetween()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.comment()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension MiddleContentVc {
    //: @objc func addButtonClick() {
    @objc func have() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.diskTerms(showMsg: String(bytes: dataComponentStr.map{betterLooking(here: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = DraftTextView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.segment()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.keyTitle(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(notiHugeKey.replacingOccurrences(of: "priority", with: "t"))] = 2
            //: dict["text"] = tempStr
            dict[(String(dataOccurPortBehaviorMessage))] = tempStr
            //: ProgressHUD.show()
            UniversalProgressHUD.addUp()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            GuidanceRequestTool.innerRevenue(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                UniversalProgressHUD.coverBetween()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.sirenSong(showMsg: String(bytes: constTemporaryPath.map{honeyStack(medication: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.comment()
                //: self.recordView?.dismiss()
                self.recordView?.material()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func keyTitle(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(kDiscTitle.suffix(7)) + String(notiDraftBoxText)) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension MiddleContentVc: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: show_explainId - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.shareOff(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = AfterTextCell.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: AfterTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! AfterTextCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.downwards(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(show_bucketTitle.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.output(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(userDiscoverName.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.output(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.scheme(name: (String(main_recognizeMessage.suffix(6)) + "ssag" + String(user_conditionBindDotPath.prefix(8)) + String(const_dueTrapStr))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = KeepWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension MiddleContentVc {
    //: private func setupEmptyDataView() {
    private func upgrade() {
        //: var style = EmptyStyle()
        var style = ValueEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: userGuideStr.reversed(), encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .referenceAndNonsolidColour()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func weightAir() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(AfterTextCell.self, forCellReuseIdentifier: AfterTextCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.eigenvalueOfAMatrix()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.balanceSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: data_journeyMsg.map{bothCur(why: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
