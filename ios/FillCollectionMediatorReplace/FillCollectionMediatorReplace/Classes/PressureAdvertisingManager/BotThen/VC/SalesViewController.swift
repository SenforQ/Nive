
//: Declare String Begin

/*: "cmd" :*/
fileprivate let show_containerStr:[UInt8] = [0xe0,0xee,0xe7]

private func elementOk(raw num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "cancelCall" :*/
fileprivate let data_waitName:String = "cancchapter"

/*: "data" :*/
fileprivate let user_hisContent:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let dataListKey:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let notiThanLessContent:String = "#3collectioncollection870"

/*: "Tap to start video chatting" :*/
fileprivate let mainRefMessage:String = "Tap totumble back"
fileprivate let const_rationalValue:String = "jump procedure peert vi"
fileprivate let constEnterId:String = "atttotalg"

/*: "icon_close_nor" :*/
fileprivate let notiAnonymousValue:[Character] = ["i","c","o","n","_","c"]
fileprivate let constMotiveId:String = "be"
fileprivate let showAdditionData:[Character] = ["o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let userBagJoinName:String = "icon_var trade complaint"
fileprivate let userRetainStr:[Character] = ["_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let show_conversionPath:[Character] = ["%","@"," ","c","h","a","t","t","i"]
fileprivate let notiComputeDecideId:String = "exploreg"

/*: "icon_lounge" :*/
fileprivate let userExampleName:String = "icon_lupon operate run journey bound"
fileprivate let noti_cheeseData:String = "oungtoward"

/*: "icon_rank_coin" :*/
fileprivate let mainBoyUrl:String = "adjustmenton"
fileprivate let constMediumPath:String = "_rank_boy do image"

/*: "vip_btn_top" :*/
fileprivate let mainSkinId:String = "vip_theory policy editor"
fileprivate let show_chaseAliveCalculateUrl:String = "ofragment"

/*: "btn_nearby_settings_nor" :*/
fileprivate let user_aircraftFormat:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let noti_addedContextKey:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i","n"]

/*: "img_match_connecting" :*/
fileprivate let appNodName:[Character] = ["i","m","g","_","m","a","t","c","h","_","c","o","n","n","e","c","t","i","n","g"]

/*: "#9F80FF" :*/
fileprivate let noti_relatedKey:[Character] = ["#","9","F","8","0","F"]
fileprivate let data_methodMsg:String = "fair"

/*: "get json error" :*/
fileprivate let data_femaleName:[Character] = ["g","e","t"," ","j","s","o","n"," "]
fileprivate let user_followingMessage:[Character] = ["e","r","r","o","r"]

/*: "matchId" :*/
fileprivate let showSubName:[Character] = ["m","a","t","c","h","I"]
fileprivate let app_likeEventPath:String = "D"

/*: "errno" :*/
fileprivate let noti_temporaryStarName:[UInt8] = [0xe7,0xf0,0xf0,0xec,0xed]

private func beautifulMin(weekly num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "VIPFee" :*/
fileprivate let user_islandMsg:String = "VIPFeebull extension mirror correct"

/*: "Matching now" :*/
fileprivate let k_resistanceTitle:String = "Matcchapter chin"
fileprivate let notiSegmentUrl:String = "hing nowfield wed with important"

/*: "userTouch" :*/
fileprivate let userUniformName:[Character] = ["u","s","e","r","T"]
fileprivate let mainSkipStr:[Character] = ["o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let mainLiveValue:String = "mf/freprocessing prospect once lite maybe"
fileprivate let notiEnterAgoKey:String = "l/getfast production permission"
fileprivate let notiAmStr:String = "bold"
fileprivate let showSuccessfulStr:[Character] = ["i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let notiWorkStr:[UInt8] = [0x65,0x70,0x79,0x54,0x6d,0x6f,0x72,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let app_recommendationMsg:[UInt8] = [0x73,0x6c,0x6c,0x61,0x63,0x20,0x67,0x6e,0x69,0x76,0x69,0x65,0x63,0x65,0x72,0x20,0x6d,0x6f,0x72,0x66,0x20,0x64,0x65,0x72,0x72,0x61,0x62,0x20,0x6e,0x65,0x65,0x62,0x20,0x65,0x76,0x27,0x75,0x6f,0x59]

/*: "source" :*/
fileprivate let showShouldStr:[UInt8] = [0x1b,0x17,0x1d,0x1a,0xb,0xd]

fileprivate func attachEqualCloud(firm num: UInt8) -> UInt8 {
    let value = Int(num) + 88
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let data_enoughMidMessage:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let user_browData:[UInt8] = [0x42,0x36,0x31]

fileprivate func groundSong(them num: UInt8) -> UInt8 {
    let value = Int(num) + 51
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let dataLabFormat:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let const_householdName:[Character] = ["r","e","q","u","e","s","t","C","a","l"]
fileprivate let dataMicValue:[Character] = ["l"]

/*: "onVideoMatch" :*/
fileprivate let k_showUrl:String = "onVidreply rocket"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let dataVoiceFormat:[UInt8] = [0x4f,0x35,0x69,0x68,0x63,0x61,0x55,0x68,0x5d,0x57,0x14,0x6a,0x5d,0x58,0x59,0x63,0x51,0x14,0x46,0x59,0x57,0x59,0x5d,0x6a,0x59,0x14,0x6a,0x5d,0x58,0x59,0x63,0x14,0x57,0x55,0x60,0x60,0x14,0x5d,0x62,0x57,0x63,0x61,0x5d,0x62,0x5b]

fileprivate func infoDisableCancer(broadcast num: UInt8) -> UInt8 {
    let value = Int(num) + 12
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isRandomVCPush" :*/
fileprivate let noti_primaStr:String = "first boldisRan"
fileprivate let app_nodCurrentValue:[Character] = ["P","u","s","h"]

/*: "refuseCall" :*/
fileprivate let mainLicenseMsg:String = "REFU"

/*: "onCancelCall" :*/
fileprivate let showWriteTitle:String = "answer pick spaceonCance"
fileprivate let const_currentlyMessage:String = "oxygen in job corelCall"

/*: "retryAfter" :*/
fileprivate let noti_refugeSoulKey:[Character] = ["r","e","t","r","y","A","f"]
fileprivate let appInputUrl:[Character] = ["t","e","r"]

/*: "onRequestCall" :*/
fileprivate let app_termsValue:String = "currency design pushonReq"
fileprivate let constRepresentValue:[Character] = ["u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let notiBlockBullMsg:String = "transfer"
fileprivate let showReaderPath:String = "ailTypepopularity all thread yet"

/*: "callWaitingTime" :*/
fileprivate let const_bridgeKey:String = "laugh source ingredient partycallWa"

/*: "toUid" :*/
fileprivate let notiEmissionKey:String = "service chin bind specialtoUid"

/*: "Free 1 min" :*/
fileprivate let user_gameName:[Character] = ["F","r","e","e"," "]
fileprivate let dataClearlyText:[Character] = ["1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let show_harvestYesData:[Character] = ["%","@","/","m","i"]
fileprivate let appPackPath:String = "margin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SalesViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class SalesViewController: MirrorRealmReactiveCompatible {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.tagMonetaryValueVideoWeekly()
            }
        }
        //: self.resetCamera()
        self.mentionOwner()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        setupStroke()
        //: setupSubViewsConstraint()
        imageInstance()
        //: bindInteraction()
        superfluous()
        //: videoManager.startPreview(isMatch: true)
        videoManager.getMovingContact(isMatch: true)
        //: setPlayer(isRandon: false)
        destroyShape(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            checkionBox()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: show_containerStr.map{elementOk(raw: $0)}, encoding: .utf8)!: (data_waitName.replacingOccurrences(of: "chapter", with: "e") + "lCall"), String(bytes: user_hisContent.reversed(), encoding: .utf8)!: [(String(dataListKey)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        midGuide()
        //: if TalkingSocketManager.shared.isMutual == false {
        if MigrationSocketDelegate.shared.isMutual == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MigrationSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MigrationSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MigrationSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MigrationSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ChatCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = ChatCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = SimulationChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .aircraft()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(checkionBox), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: show_explainId + 1, height: userSystemKey + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (notiThanLessContent.replacingOccurrences(of: "collection", with: "2")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(mainRefMessage.prefix(6)) + " star" + String(const_rationalValue.suffix(4)) + "deo ch" + constEnterId.replacingOccurrences(of: "total", with: "in")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(notiAnonymousValue) + constMotiveId.replacingOccurrences(of: "be", with: "l") + String(showAdditionData))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(vulnerability), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: ExtendedView = {
        //: let btn = TalkingButton()
        let btn = ExtendedView()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(userBagJoinName.prefix(5)) + "views" + String(userRetainStr))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.scheme(name: (String(userBagJoinName.prefix(5)) + "views" + String(userRetainStr))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.shareOff(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(show_conversionPath) + notiComputeDecideId.replacingOccurrences(of: "explore", with: "n")).queryionLocalized(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.scheme(name: (String(userExampleName.prefix(6)) + noti_cheeseData.replacingOccurrences(of: "toward", with: "e")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.scheme(name: (mainBoyUrl.replacingOccurrences(of: "adjustment", with: "ic") + String(constMediumPath.prefix(6)) + "coin"))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.shareOff(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.scheme(name: (String(mainSkinId.prefix(4)) + "btn_t" + show_chaseAliveCalculateUrl.replacingOccurrences(of: "fragment", with: "p")))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: ExtendedView = {
        //: let btn = TalkingButton()
        let btn = ExtendedView()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moveChild), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.scheme(name: (String(user_aircraftFormat))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.scheme(name: (mainBoyUrl.replacingOccurrences(of: "adjustment", with: "ic") + String(constMediumPath.prefix(6)) + "coin"))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .cancer()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(noti_addedContextKey)).queryionLocalized(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.shareOff(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ClingView = //: return ClingView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.scheme(name: (String(appNodName)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(noti_relatedKey) + data_methodMsg.replacingOccurrences(of: "fair", with: "F")))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension SalesViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func destroyShape(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = FinishLetterThen.default.detailWork(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(data_femaleName) + String(user_followingMessage)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func atomicTotal10(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(showSubName) + app_likeEventPath.lowercased()): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: show_containerStr.map{elementOk(raw: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: noti_temporaryStarName.map{beautifulMin(weekly: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        InsideReactiveCompatible.serverCompletion(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.manageFor()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(showSubName) + app_likeEventPath.lowercased())].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func mapSchedule() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        InsideReactiveCompatible.incomeFit { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(user_islandMsg.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.tagMonetaryValueVideoWeekly()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension SalesViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func moveChild() {
        //: ElementEarnReactiveCompatible.share.func__pushToSubscribeAlert()
        ElementEarnReactiveCompatible.share.hairProgress()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func vulnerability() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        browsing()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        noti_nearbyStr.tamperConnect(eventID: notiLengthStr)

        //: if AppManager.share.appConfigMode.displayMatchV4PopUp, AppManager.share.loginUserMode.freeCallTimes > 0,
        if AppManager.share.appConfigMode.displayMatchV4PopUp, AppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.libraryYearStat()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        reasonWindow()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func checkionBox() {
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.diskTerms(showMsg: notiLocalText)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        velvet()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(k_resistanceTitle.prefix(4)) + String(notiSegmentUrl.prefix(8))).localized
        //: setPlayer(isRandon: true)
        destroyShape(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if AppManager.share.loginUserMode.randomVideoType == 2 && AppManager.share.loginUserMode.robotVideoNum > 0 {
        if AppManager.share.loginUserMode.randomVideoType == 2, AppManager.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            noti_nearbyStr.tamperConnect(eventID: dataGroupTitle)
            //: req_getVideoInfo()
            reach()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            noti_nearbyStr.tamperConnect(eventID: data_errorStr)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            MigrationSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            MigrationSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            MigrationSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            MigrationSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            theIn()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func theIn(cmd: String = (String(userUniformName) + String(mainSkipStr)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        midGuide()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        atomicTotal10(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func manageFor() {
        // 停止定时器
        //: destroyWaitingTimer()
        midGuide()
        //: showCloseBtn()
        browsing()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(mainRefMessage.prefix(6)) + " star" + String(const_rationalValue.suffix(4)) + "deo ch" + constEnterId.replacingOccurrences(of: "total", with: "in")).localized
        //: setPlayer(isRandon: false)
        destroyShape(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isMutual == false {
        if MigrationSocketDelegate.shared.isMutual == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MigrationSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MigrationSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MigrationSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MigrationSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        tagMonetaryValueVideoWeekly()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func midGuide() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func reach() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(mainLiveValue.prefix(6)) + "eCal" + String(notiEnterAgoKey.prefix(5)) + notiAmStr.replacingOccurrences(of: "bold", with: "V") + String(showSuccessfulStr))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: notiWorkStr.reversed(), encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.manageFor()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: AppManager.share.loginUserMode.randomVideoType = 1
                    AppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.checkionBox()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.diskTerms(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = PressureTransformable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = SalesReactiveCompatible()
                //: player.player.config.maxPreloadSize = 10
                player.player.config.maxPreloadSize = 10
                //: player.player.config.maxBufferSize = 10
                player.player.config.maxBufferSize = 10 // 播放时最大缓冲大小。单位：MB
                //: player.setMute(bEnable: true)
                player.skipSuite(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.occur(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                UniversalManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                EditThen.recordUpon { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.applyFlow()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func applyFlow() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        noti_nearbyStr.tamperConnect(eventID: data_topId)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard UniversalManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.diskTerms(showMsg: String(bytes: app_recommendationMsg.reversed(), encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            UniversalManager.shared.commitYear(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.manageFor()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = BotThen()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.reasonWindow()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.manageFor()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func velvet() {
        //: if AppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if AppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(AppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(AppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func browsing() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func mentionOwner() {
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.getMovingContact()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func norClean() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.checkionBox()
        }
    }
}

// MARK: - ExtraMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension SalesViewController: ExtraMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func layerData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = AfterHandyJSON.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(showSubName) + app_likeEventPath.lowercased()): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: showShouldStr.map{attachEqualCloud(firm: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: data_enoughMidMessage.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: user_browData.map{groundSong(them: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = LabelStatusWidthTotal.randow
        //: if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(dataLabFormat)))
            //: TalkingSocketManager.shared.isFreeCall = true
            MigrationSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: show_containerStr.map{elementOk(raw: $0)}, encoding: .utf8)!: (String(const_householdName) + String(dataMicValue)), String(bytes: user_hisContent.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        MigrationSocketDelegate.shared.renderOffDraft(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: show_containerStr.map{elementOk(raw: $0)}, encoding: .utf8)!: (data_waitName.replacingOccurrences(of: "chapter", with: "e") + "lCall"), String(bytes: user_hisContent.reversed(), encoding: .utf8)!: [(String(dataListKey)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.theIn(cmd: (String(k_showUrl.prefix(5)) + "eoMatch"))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func am(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(dataListKey))].intValue
    }
}

// MARK: - ElapidObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension SalesViewController: ElapidObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func programmeNumbererval(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func addLight(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func unneeded(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func roundEvaluate(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            LanguageThen.transitionManager(body: String(bytes: dataVoiceFormat.map{infoDisableCancer(broadcast: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = AfterHandyJSON.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            uploadUid(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        manageFor()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.shadowResource()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(noti_primaStr.suffix(5)) + "domVC" + String(app_nodCurrentValue))] = true
        //: AppManager.share.start1v1TalkCall(info: newData, situation: .Calling)
        AppManager.share.coverSituation(info: newData, situation: .Calling)
    }
}

// MARK: - DuplexPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension SalesViewController: DuplexPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func telecasting(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        theIn(cmd: (mainLicenseMsg.lowercased() + "seCall"))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func adopt(data _: [String: Any]) {}
}

// MARK: - ClingRealmObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension SalesViewController: ClingRealmObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func quality(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(showWriteTitle.suffix(7)) + String(const_currentlyMessage.suffix(5))) { // 取消通话异常
            //: stop_randomVideo()
            manageFor()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(k_showUrl.prefix(5)) + "eoMatch"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            manageFor()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(showSubName) + app_likeEventPath.lowercased())].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.diskTerms(showMsg: msg)
            //: uploadFailTime(type: 2)
            uploadThemeBreed(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(k_showUrl.prefix(5)) + "eoMatch"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(noti_refugeSoulKey) + String(appInputUrl))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(showSubName) + app_likeEventPath.lowercased())].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.theIn(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(app_termsValue.suffix(5)) + String(constRepresentValue)), errorNo == UponSizeHashable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            manageFor()
            //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue else { return }
            //: ElementEarnReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            ElementEarnReactiveCompatible.share.recharge(clickEvent: showProgressFormat, sufficient: false)
            //: uploadFailTime(type: 1)
            uploadThemeBreed(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        theIn(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension SalesViewController {
    //: func uploadFailTime(type: Int) {
    func uploadThemeBreed(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(notiBlockBullMsg.replacingOccurrences(of: "transfer", with: "f") + String(showReaderPath.prefix(7)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(const_bridgeKey.suffix(6)) + "itingTime")] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        noti_nearbyStr.bottomStr(eventID: appExplainName, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func uploadUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(const_bridgeKey.suffix(6)) + "itingTime")] = time
        //: parm["toUid"] = toUid
        parm[(String(notiEmissionKey.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        noti_nearbyStr.bottomStr(eventID: const_warnPath, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension SalesViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func tagMonetaryValueVideoWeekly() {
        //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !AppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !AppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = LabelStatusWidthTotal.randow
        //: if AppManager.share.loginUserMode.freeCallTimes > 0 && AppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if AppManager.share.loginUserMode.freeCallTimes > 0, AppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if AppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if AppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(user_gameName) + String(dataClearlyText)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = AppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = AppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(show_harvestYesData) + appPackPath.replacingOccurrences(of: "margin", with: "n")).queryionLocalized(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(show_harvestYesData) + appPackPath.replacingOccurrences(of: "margin", with: "n")).queryionLocalized(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangBusy(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = AppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = AppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = AppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = AppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !AppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !AppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func setupStroke() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func imageInstance() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(dataInfoTrustName + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func superfluous() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(tagMonetaryValueVideoWeekly),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: notiGreetMsg,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(mapSchedule),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: const_buildContent,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(norClean), name: app_barKey, object: nil)
    }
}
