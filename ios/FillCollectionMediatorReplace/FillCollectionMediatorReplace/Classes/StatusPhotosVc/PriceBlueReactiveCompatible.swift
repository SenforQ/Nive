
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let k_pushEntityBetweenMessage:String = "head appear environmentmf/mome"
fileprivate let noti_signFormat:String = "nt/listregain bottom current"

/*: "mf/moment/moments" :*/
fileprivate let userLibraryHarassmentFormat:String = "people blank plunge remf/mom"
fileprivate let showSplitMessage:String = "ntsomeone"

/*: "mf/moment/like" :*/
fileprivate let userSearchStr:String = "keep bill remote soapmf/mome"
fileprivate let userUrgeHorseId:String = "nt/likeion upon refuge mini"

/*: "mf/crush/send" :*/
fileprivate let constAlongData:String = "mf/crucount addition shirt"
fileprivate let k_belowValue:[Character] = ["s","h","/","s","e","n","d"]

/*: "mf/moment/view" :*/
fileprivate let userOldMarriedValue:String = "mode rate combinedmf/mome"
fileprivate let showUniformGrantPath:String = "pub lower sincent/view"

/*: "mf/momentReply/list" :*/
fileprivate let appRecordText:String = "mf/mwarning exactly its shot"
fileprivate let app_portraitTitle:String = "Reply/lrow cost"
fileprivate let mainSpotMessage:String = "IST"

/*: "mf/momentReply/save" :*/
fileprivate let noti_logMessage:String = "others plus current promf/m"
fileprivate let kQuantityData:String = "Replybehind topic"
fileprivate let appIonKey:String = "/saveprivate ping act"

/*: "mf/moment/videoSign" :*/
fileprivate let user_teeId:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d","e","o","S","i","g"]
fileprivate let app_criminalText:String = "top"

/*: "dynamicPic.zip" :*/
fileprivate let notiSoonText:String = "dynprogressm"
fileprivate let appTermsName:String = ".zipenable cycle defense"

/*: "pic" :*/
fileprivate let userSpotMessage:[UInt8] = [0xdf,0xc6,0xcc]

private func disableEngineering(production num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "content" :*/
fileprivate let constForeStr:String = "conassistante"
fileprivate let show_partyValue:String = "nfocus"

/*: "mf/moment/save" :*/
fileprivate let noti_fullData:[Character] = ["m","f","/","m","o"]
fileprivate let dataComposeFormat:String = "sh"
fileprivate let userLessTitle:String = "ent/savedecision location"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let main_faultMsg:[UInt8] = [0xfc,0xd9,0xce,0xc0,0xc5,0xdf,0xc4,0x8c,0xff,0xd9,0xcf,0xcf,0xc9,0xdf,0xdf,0xca,0xd9,0xc0,0x80,0x8c,0xeb,0xc3,0x8c,0xd8,0xc3,0x8c,0xe1,0xd5,0x8c,0xe8,0xd5,0xc2,0xcd,0xc1,0xc5,0xcf,0xdf,0x8c,0xd8,0xc3,0x8c,0xda,0xc5,0xc9,0xdb]

private func snapBig(section num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "mf/moment/saveVideo" :*/
fileprivate let userDimensionValue:String = "mf/mohello lam select"
fileprivate let k_sharedData:String = "in output script total/save"
fileprivate let main_blindName:String = "Videomargin explore later strength"

/*: .png" :*/
fileprivate let showSoftenMeasurePath:[Character] = [".","p","n","g"]

/*: "dynamicPic" :*/
fileprivate let noti_sinceStretchUrl:String = "dynjudge"

/*: "freeImage" :*/
fileprivate let dataExtraUrl:[Character] = ["f","r","e","e","I","m"]
fileprivate let dataBetweenMsg:[Character] = ["a","g","e"]

/*: "mf/momentReply/replylist" :*/
fileprivate let noti_timeMastUrl:String = "mf/mconnect shake awake top"
fileprivate let appLessName:String = "little add migration ionReply/"
fileprivate let showTieMsg:String = "jobst"

/*: "mf/moment/delete" :*/
fileprivate let mainAgeTitle:[Character] = ["m","f","/","m","o","m"]
fileprivate let mainButFormat:String = "ent/dbudget keep"
fileprivate let data_administrativeTitle:String = "eboundete"

/*: "mf/momentReply/delete" :*/
fileprivate let user_relationFormat:String = "mf/moequally automatic region below"
fileprivate let appPoseMsg:String = "motive pub him meet agreeply/"

/*: "mf/moment/pin" :*/
fileprivate let data_teamStr:[Character] = ["m","f"]
fileprivate let constThreeName:[Character] = ["/","m","o","m","e","n","t","/","p","i","n"]

/*: "mf/moment/remindNum" :*/
fileprivate let kForeheadId:String = "mf/moinmate pad run shot situation"
fileprivate let k_closetName:String = "mindNumcurrency bullet dit technology"

/*: "mf/moment/remindList" :*/
fileprivate let dataBusyMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m","i","n","d","L","i","s","t"]

/*: "page" :*/
fileprivate let constWelcomeMsg:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let const_staffHeritageStr:[Character] = ["c","o","s","/","g","e","t","C","r","e"]
fileprivate let appPullStr:String = "dentiforthl"

/*: "type" :*/
fileprivate let mainRequireConfirmName:String = "TYPE"

/*: "user/reportUserInfo" :*/
fileprivate let noti_mindMessage:String = "user/freshman invest social past"
fileprivate let userShelfYourselfValue:String = "until errorUserInfo"

/*: "uid" :*/
fileprivate let show_laterPhaseUrl:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let userAccountFromData:String = "pointvice"
fileprivate let notiMayOverKey:[Character] = ["I","d"]

/*: "deviceName" :*/
fileprivate let const_behindStr:String = "devchildc"
fileprivate let constDocumentId:String = "eNameconversion identity today word discover"

/*: "iphone" :*/
fileprivate let main_tabGlobalMirrorData:String = "iphinmatee"

/*: "fileUrl" :*/
fileprivate let userMidMessage:String = "reject song report objectfileU"
fileprivate let kVidPopBondId:[Character] = ["r","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PriceBlueReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class PriceBlueReactiveCompatible: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func target(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(k_pushEntityBetweenMessage.suffix(7)) + String(noti_signFormat.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func scarlet(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(userLibraryHarassmentFormat.suffix(6)) + "ent/mome" + showSplitMessage.replacingOccurrences(of: "someone", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func visualThan(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(userSearchStr.suffix(7)) + String(userUrgeHorseId.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func lay(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(constAlongData.prefix(6)) + String(k_belowValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func transactionMoment(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(userOldMarriedValue.suffix(7)) + String(showUniformGrantPath.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func catalogue(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(appRecordText.prefix(4)) + "oment" + String(app_portraitTitle.prefix(7)) + mainSpotMessage.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func sinceOwner(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(noti_logMessage.suffix(4)) + "oment" + String(kQuantityData.prefix(5)) + String(appIonKey.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func lockCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(user_teeId) + app_criminalText.replacingOccurrences(of: "top", with: "n"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func americanism(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        renounce()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            theDecide(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((notiSoonText.replacingOccurrences(of: "progress", with: "a") + "icPic" + String(appTermsName.prefix(4))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: userSpotMessage.map{disableEngineering(production: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(constForeStr.replacingOccurrences(of: "assistant", with: "t") + show_partyValue.replacingOccurrences(of: "focus", with: "t"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ExplosiveTraceDetectionRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(noti_fullData) + dataComposeFormat.replacingOccurrences(of: "sh", with: "m") + String(userLessTitle.prefix(8)))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.sirenSong(showMsg: String(bytes: main_faultMsg.map{snapBig(section: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.renounce()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func moment(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(userDimensionValue.prefix(5)) + "ment" + String(k_sharedData.suffix(5)) + String(main_blindName.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func theDecide(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.beauty()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(showSoftenMeasurePath))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((noti_sinceStretchUrl.replacingOccurrences(of: "judge", with: "a") + "micPic"))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func renounce() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(dataExtraUrl) + String(dataBetweenMsg)))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func root(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(noti_timeMastUrl.prefix(4)) + "oment" + String(appLessName.suffix(6)) + "reply" + showTieMsg.replacingOccurrences(of: "job", with: "li"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func ratingWith(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(mainAgeTitle) + String(mainButFormat.prefix(5)) + data_administrativeTitle.replacingOccurrences(of: "bound", with: "l"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func important(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(user_relationFormat.prefix(5)) + "mentRe" + String(appPoseMsg.suffix(4)) + "delete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(data_teamStr) + String(constThreeName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func bringHome(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(kForeheadId.prefix(5)) + "ment/re" + String(k_closetName.prefix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func quantityerrupt(page: Int, completion: @escaping (_ data: [SnakeDomainTransformable]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(dataBusyMessage))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: constWelcomeMsg.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [SnakeDomainTransformable] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<SnakeDomainTransformable>.deserialize(from: arr as? Array) as? [SnakeDomainTransformable] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func resType(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(const_staffHeritageStr) + appPullStr.replacingOccurrences(of: "forth", with: "a"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(mainRequireConfirmName.lowercased()): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func need(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestServer = AppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = AppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(noti_mindMessage.prefix(5)) + "report" + String(userShelfYourselfValue.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = AppManager.share.loginUserMode.userID
        params[(String(show_laterPhaseUrl))] = AppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(userAccountFromData.replacingOccurrences(of: "point", with: "de") + String(notiMayOverKey))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(const_behindStr.replacingOccurrences(of: "child", with: "i") + String(constDocumentId.prefix(5)))] = (main_tabGlobalMirrorData.replacingOccurrences(of: "inmate", with: "on"))
        //: params["type"] = 1
        params[(mainRequireConfirmName.lowercased())] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(userMidMessage.suffix(5)) + String(kVidPopBondId))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
