
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiSearchionStr:[UInt8] = [0x71,0x76,0x71,0x7c,0x30,0x6b,0x77,0x6c,0x6d,0x7a,0x42,0x31,0x28,0x70,0x69,0x7b,0x28,0x76,0x77,0x7c,0x28,0x6a,0x6d,0x6d,0x76,0x28,0x71,0x75,0x78,0x74,0x6d,0x75,0x6d,0x76,0x7c,0x6d,0x6c]

fileprivate func enoughStock(deny num: UInt8) -> UInt8 {
    let value = Int(num) + 248
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let const_hundredKey:[UInt8] = [0x71,0x92,0x87,0x90,0x42,0x96,0x8a,0x87,0x90,0x42,0x47,0x62,0x42,0x99,0x8b,0x8e,0x8e,0x42,0x94,0x87,0x85,0x91,0x8f,0x8f,0x87,0x90,0x86,0x42,0x97,0x95,0x87,0x94,0x95,0x42,0x96,0x91,0x42,0x9b,0x91,0x97,0x50,0x42,0x63,0x42,0x90,0x87,0x99,0x42,0x85,0x8a,0x83,0x96,0x42,0x99,0x8b,0x8e,0x8e,0x42,0x84,0x87,0x42,0x8f,0x83,0x86,0x87,0x42,0x83,0x88,0x96,0x87,0x94,0x42,0x83,0x42,0x95,0x97,0x85,0x85,0x87,0x95,0x95,0x88,0x97,0x8e,0x42,0x94,0x87,0x85,0x91,0x8f,0x8f,0x87,0x90,0x86,0x83,0x96,0x8b,0x91,0x90,0x50]

fileprivate func serveIsland(fit num: UInt8) -> UInt8 {
    let value = Int(num) - 34
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let dataAngleUrl:[UInt8] = [0xad,0xc3,0xc9,0xc6,0x74,0xb5,0xca,0xb5,0xc8,0xb5,0xc6,0x74,0xb5,0xc2,0xb8,0x74,0xc2,0xbd,0xb7,0xbf,0xc2,0xb5,0xc1,0xb9,0x74,0xcb,0xbd,0xc0,0xc0,0x74,0xc2,0xc3,0xc8,0x74,0xb6,0xb9,0x74,0xb8,0xbd,0xc7,0xc4,0xc0,0xb5,0xcd,0xb9,0xb8,0x74,0xbd,0xc2,0x74,0xc8,0xbc,0xb9,0x74,0xc6,0xb5,0xc2,0xbf,0xbd,0xc2,0xbb,0x82]

fileprivate func everyLastIsolate(on num: UInt8) -> UInt8 {
    let value = Int(num) + 172
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let k_signatureText:[UInt8] = [0x68,0x57,0x42,0x49,0x7,0x53,0x4f,0x42,0x49,0x7,0x66,0x57,0x57,0x4b,0x5e,0x49,0x46,0x4a,0x42,0x7,0x50,0x4e,0x4b,0x4b,0x7,0x55,0x42,0x44,0x48,0x4a,0x4a,0x42,0x49,0x43,0x7,0x52,0x54,0x42,0x55,0x54,0x7,0x53,0x48,0x7,0x5e,0x48,0x52,0x9,0x7,0x72,0x54,0x42,0x7,0x5e,0x48,0x52,0x55,0x7,0x4e,0x43,0x42,0x49,0x53,0x4e,0x53,0x5e,0x7,0x53,0x48,0x7,0x40,0x55,0x42,0x42,0x53,0x7,0x42,0x46,0x44,0x4f,0x7,0x48,0x53,0x4f,0x42,0x55,0x9]

private func victoryMergeLower(near num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let main_iconUrl:[UInt8] = [0x3f,0x60,0x55,0x5e,0x10,0x64,0x58,0x55,0x5e,0x10,0x15,0x30,0x10,0x67,0x59,0x5c,0x5c,0x10,0x62,0x55,0x53,0x5f,0x5d,0x5d,0x55,0x5e,0x54,0x10,0x65,0x63,0x55,0x62,0x63,0x10,0x64,0x5f,0x10,0x69,0x5f,0x65,0x1e,0x10,0x45,0x63,0x55,0x10,0x69,0x5f,0x65,0x62,0x10,0x59,0x54,0x55,0x5e,0x64,0x59,0x64,0x69,0x10,0x64,0x5f,0x10,0x57,0x62,0x55,0x55,0x64,0x10,0x55,0x51,0x53,0x58,0x10,0x5f,0x64,0x58,0x55,0x62,0x1e]

fileprivate func pinionUniform(basic num: UInt8) -> UInt8 {
    let value = Int(num) + 16
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "#FF2348" :*/
fileprivate let userMeasurementData:[Character] = ["#","F","F","2","3"]
fileprivate let userFilterGradeStr:String = "48"

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let constStateTitle:[UInt8] = [0x64,0x6f,0x68,0x74,0x65,0x6d,0x20,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x72,0x65,0x76,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x73,0x6f,0x6f,0x68,0x63,0x20,0x2c,0x74,0x6e,0x75,0x6f,0x63,0x63,0x61,0x20,0x65,0x68,0x74,0x20,0x79,0x66,0x69,0x72,0x65,0x76,0x20,0x6f,0x74,0x20,0x64,0x65,0x65,0x6e,0x20,0x75,0x6f,0x79,0x20,0x2c,0x64,0x72,0x6f,0x77,0x73,0x73,0x61,0x70,0x20,0x61,0x20,0x70,0x75,0x20,0x74,0x65,0x73,0x20,0x6f,0x54]

/*: "Verified by Phone" :*/
fileprivate let kPackKey:String = "Verifdistribution whether elect access clip"
fileprivate let userExtendStr:String = "should gradeby Ph"
fileprivate let constTaskText:[Character] = ["o","n","e"]

/*: "Verified by Email" :*/
fileprivate let noti_gradeKey:[Character] = ["V","e","r","i","f","i"]
fileprivate let userCommunicationMsg:String = "paper softened "
fileprivate let userDrawingKey:String = "by Emailrecent tick now game"

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let mainVisibleName:[UInt8] = []

fileprivate func innerCity(north num: UInt8) -> UInt8 {
    let value = Int(num) - 27
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UNRECOVERABLE" :*/
fileprivate let const_unlessMessage:String = "unreco"

/*: "Delete Account" :*/
fileprivate let constRecallData:[Character] = ["D","e","l","e","t","e"]
fileprivate let dataStopName:[Character] = [" ","A"]
fileprivate let main_filmMessage:[Character] = ["c","c","o","u","n","t"]

/*: "Cancel" :*/
fileprivate let appSearchTempMsg:String = "big coordinatorCancel"

/*: "Delete" :*/
fileprivate let noti_mistMsg:[Character] = ["D","e","l","e","t","e"]

/*: "Logout succeeded!" :*/
fileprivate let main_cooperativeKey:String = "Logoutbass minimize"
fileprivate let notiVariationText:[Character] = ["c","e","e","d","e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WhiteGoodDataSource.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum SuborderOphidiaQuickLookable: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum InsideExpressionConvertible: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class WhiteGoodDataSource: MirrorRealmReactiveCompatible {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = ZzLabelSettingModel()

    //: var  type = SettingsUniversal.Contact_information
    var type = SuborderOphidiaQuickLookable.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiSearchionStr.map{enoughStock(deny: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.referenceAndNonsolidColour()
        //: designView()
        owner()
        //: getSettingData()
        doinglyData()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [InsideExpressionConvertible] = {
        //: var array = [SettingsUniversalType]()
        var array = [InsideExpressionConvertible]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey - data_keyPath), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Your avatar and nickname will not be displayed in the ranking.".localized, "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized]
        var array = ["", String(bytes: const_hundredKey.map{serveIsland(fit: $0)}, encoding: .utf8)!.queryionLocalized(dataTagName), String(bytes: dataAngleUrl.map{everyLastIsolate(on: $0)}, encoding: .utf8)!.localized, String(bytes: k_signatureText.map{victoryMergeLower(near: $0)}, encoding: .utf8)!.localized]
        //: if AppManager.share.loginUserMode.sex == "2" {
        if AppManager.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: main_iconUrl.map{pinionUniform(basic: $0)}, encoding: .utf8)!.queryionLocalized(dataTagName)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension WhiteGoodDataSource {
    //: func getSettingData() {
    func doinglyData() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        GuidanceRequestTool.sumBroadcast(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<ZzLabelSettingModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func headFile(type: SuborderOphidiaQuickLookable) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: if AppManager.share.appConfigMode.enableSmsLogin {
            if AppManager.share.appConfigMode.enableSmsLogin {
                //: DataSource.append(.Phone_Number)
                DataSource.append(.Phone_Number)
            }
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if (AppManager.share.appConfigMode.enableSmsLogin &&
            if (AppManager.share.appConfigMode.enableSmsLogin &&
                //: AppManager.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                AppManager.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                //: AppManager.share.loginUserMode.email.count > 0 {
                AppManager.share.loginUserMode.email.count > 0
            {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func resumeLay() {
        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        PhotoPreserveReactiveCompatible.will { succeed, _, _ in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: guard succeed else { return }
            guard succeed else { return }
            //: ElementEarnReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: AppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
            ElementEarnReactiveCompatible.share.switcherPwd(type: .PhoneLogin, content: AppManager.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func email() {
        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        PhotoPreserveReactiveCompatible.userCompletion { succeed, _, _ in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: guard succeed else { return }
            guard succeed else { return }
            //: ElementEarnReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: AppManager.share.loginUserMode.email, isNewPwd: false)
            ElementEarnReactiveCompatible.share.switcherPwd(type: .EmailLogin, content: AppManager.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension WhiteGoodDataSource: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if AppManager.share.loginUserMode.sex == "2" {
                if AppManager.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingCell.className(), for: indexPath) as! TalkingSettingCell
            let cell: ListEnsureSettingCell = tableView.dequeueReusableCell(withIdentifier: ListEnsureSettingCell.className(), for: indexPath) as! ListEnsureSettingCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.locationRate(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: cell.titleBLB.textColor = UIColor.appTitleColor()
            cell.titleBLB.textColor = UIColor.aircraft()

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == InsideExpressionConvertible.Phone_Number {
                //: if AppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if AppManager.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: AppManager.share.loginUserMode.mobile ?? "")
                    cell.detailsDetail(detail: AppManager.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .aircraft()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == InsideExpressionConvertible.Email_Address {
                //: if AppManager.share.loginUserMode.email.count > 0 {
                if AppManager.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: AppManager.share.loginUserMode.email )
                    cell.detailsDetail(detail: AppManager.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == InsideExpressionConvertible.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == InsideExpressionConvertible.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(userMeasurementData) + userFilterGradeStr.capitalized))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingNotifCell.className(), for: indexPath) as! TalkingSettingNotifCell
            let cell: EffView = tableView.dequeueReusableCell(withIdentifier: EffView.className(), for: indexPath) as! EffView

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.aboardCurrent(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.justTo(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.statementTo(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.statementTo(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.statementTo(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.statementTo(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.statementTo(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == InsideExpressionConvertible.Phone_Number.rawValue {
                //: let mobile = AppManager.share.loginUserMode.mobile ?? ""
                let mobile = AppManager.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = BotYesterdayThen()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == InsideExpressionConvertible.Email_Address.rawValue {
                //: if AppManager.share.loginUserMode.email.count <= 0 {
                if AppManager.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = LoginViewController()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == InsideExpressionConvertible.DeleteA.rawValue {
                //: writeOff()
                mutagenDoing()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == InsideExpressionConvertible.SetPassword.rawValue {
                //: let haveMobile = AppManager.share.appConfigMode.enableSmsLogin && (AppManager.share.loginUserMode.mobile?.count ?? 0 > 0)
                let haveMobile = AppManager.share.appConfigMode.enableSmsLogin && (AppManager.share.loginUserMode.mobile?.count ?? 0 > 0)
                //: let haveEmail = AppManager.share.loginUserMode.email.count > 0
                let haveEmail = AppManager.share.loginUserMode.email.count > 0
                //: if haveMobile, haveEmail {
                if haveMobile, haveEmail { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = OppositePopView(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: constStateTitle.reversed(), encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.pingfangRugularFont(fontSize: 12)
                    vc.titleFont = UIFont.balanceSize(fontSize: 12)
                    //: vc.titleColor = UIColor.appValueDetailColor()
                    vc.titleColor = UIColor.eigenvalueOfAMatrix()
                    //: vc.cellTitleFont = UIFont.pingfangMediumFont(fontSize: 16)
                    vc.cellTitleFont = UIFont.pingfangBusy(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.divideList(cellTitleList: [(String(kPackKey.prefix(5)) + "ied " + String(userExtendStr.suffix(5)) + String(constTaskText)).localized, (String(noti_gradeKey) + String(userCommunicationMsg.suffix(3)) + String(userDrawingKey.prefix(8))).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.resumeLay()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.email()
                        }
                    }

                    //: } else if haveMobile {
                } else if haveMobile {
                    //: req_sendModifyPwdSms()
                    resumeLay()

                    //: } else if haveEmail {
                } else if haveEmail {
                    //: req_sendModifyPwdEmail()
                    email()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension WhiteGoodDataSource {
    /// 注销
    //: private func writeOff() {
    private func mutagenDoing() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard EditThen.beanPlantRecording() == false else { return }
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.diskTerms(showMsg: notiLocalText)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: mainVisibleName.map{innerCity(north: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.infoRange((const_unlessMessage.uppercased() + "VERABLE"))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.limitedDraft(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        PulseReactiveCompatible.momentOfTruth(title: (String(constRecallData) + String(dataStopName) + String(main_filmMessage)).localized, attributedMessage: string, leftBtnTitle: (String(appSearchTempMsg.suffix(6))).localized, rightBtnTitle: (String(noti_mistMsg)).localized) {
            //: TalkingAlertShow.hideAlert()
            PulseReactiveCompatible.carveOutOnicial()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            PulseReactiveCompatible.carveOutOnicial()

            //: self.LockAccount()
            self.editContain()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                PulseReactiveCompatible.serverBy(title: (String(noti_mistMsg)).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                PulseReactiveCompatible.serverBy(title: (String(noti_mistMsg)).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func editContain() {
        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        GuidanceRequestTool.customer(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.shapeResult()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.sirenSong(showMsg: (String(main_cooperativeKey.prefix(6)) + " suc" + String(notiVariationText)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func shapeResult() {
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.diskTerms(showMsg: notiLocalText)
            //: return
            return
        }

        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingLoginRequestTool.req_loginOut { t in
        PhotoPreserveReactiveCompatible.lap { t in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: main_domainColorErrorStr, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension WhiteGoodDataSource {
    //: private func designView() {
    private func owner() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: TalkingSettingCell.className())
        MainTable.register(ListEnsureSettingCell.self, forCellReuseIdentifier: ListEnsureSettingCell.className())
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: TalkingSettingNotifCell.className())
        MainTable.register(EffView.self, forCellReuseIdentifier: EffView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
