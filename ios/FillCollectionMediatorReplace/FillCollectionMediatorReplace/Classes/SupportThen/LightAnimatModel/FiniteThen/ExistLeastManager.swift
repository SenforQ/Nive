
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let const_nearbyData:String = "IM退\u{51fa}"
fileprivate let k_announcementMessage:String = "air successfullyode:"

/*: , desc: :*/
fileprivate let kSocialRegulateFormat:String = ", desc:state guard copy front equity"

/*: "IM登录失败，code: :*/
fileprivate let show_statuteReloadlyData:String = "IM登录\u{5931}"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let mainSignalPath:String = "IM获取未stream succeed secret statute schedule"
fileprivate let kSpringData:[Character] = ["，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let noti_yellowMessage:String = "CRUS"

/*: "badNumber" :*/
fileprivate let mainStandMessage:[UInt8] = [0x3d,0x3e,0x3b,0x11,0x2a,0x32,0x3d,0x3a,0x2d]

private func eyeOnce(execute num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "plaza" :*/
fileprivate let dataShadowUrl:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let user_wantText:String = "roomNumfun carrier"

/*: "num" :*/
fileprivate let kHungName:String = "nprioritym"

/*: "onlyOneRoomId" :*/
fileprivate let constThanHumanMessage:[Character] = ["o","n","l","y","O","n"]
fileprivate let k_relatedMessage:[Character] = ["e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let const_ehPath:String = "carrierV"
fileprivate let showProjectFeedbackMessage:String = "pro chart brow good attributeE-CHAT"

/*: "extra" :*/
fileprivate let show_sharedUrl:String = "exeasera"

/*: "msgType" :*/
fileprivate let kSoapTitle:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let kBullMessage:[Character] = ["n","e","w","P","k","g","G","i","f"]
fileprivate let show_encounterKey:String = "remark"

/*: "MF:ChatMentionMsg" :*/
fileprivate let main_proximateHereKey:String = "early detail mainMF:Cha"
fileprivate let k_openlyTitle:[Character] = ["o","n","M","s","g"]

/*: "uid" :*/
fileprivate let constFeeFormat:[UInt8] = [0xbc,0xa0,0xad]

/*: "onlineStatus" :*/
fileprivate let const_shrinkMsg:[UInt8] = [0xac,0xad,0xaf,0xaa,0xad,0xa6,0x90,0xb7,0xa2,0xb7,0xb6,0xb0]

private func addOn(unite num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "isNewUser" :*/
fileprivate let main_civilPath:[UInt8] = [0xba,0xc4,0x9f,0xb6,0xc8,0xa6,0xc4,0xb6,0xc3]

fileprivate func finishingLine(please num: UInt8) -> UInt8 {
    let value = Int(num) - 81
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "c2c_ :*/
fileprivate let showOptionText:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let const_descriptionStr:[Character] = ["I","M","\u{83b7}","\u{53d6}","会","话","\u{5931}","\u{8d25}","，","c","o","n","v","I","d",":"]

/*: , code: :*/
fileprivate let data_lowerTitle:String = "require able, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let noti_withEmissionId:String = "club event than addition criminalIM插入"
fileprivate let k_accessValue:String = "tarecommendation"
fileprivate let noti_aData:String = "apply change domainetId:"

/*: "changeDirection" :*/
fileprivate let app_partnerValue:String = "meet creation vertical depth stackchangeDi"
fileprivate let mainClueId:[Character] = ["r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let main_absValue:String = "merely"
fileprivate let main_oneDistanceData:String = "drop category advanced tab mutualYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let kTermsName:String = "GJ:Catake calculate"
fileprivate let app_workerData:[Character] = ["l","l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let notiFenceName:String = "fault floor top extent sequenceIM获取会"
fileprivate let notiCheekUrl:String = "\u{ff0c}cod"
fileprivate let userScriptStr:String = "check grade more slow mergee:"

/*: "msgInfo" :*/
fileprivate let mainPointStr:[Character] = ["m","s","g","I","n","f"]
fileprivate let noti_hiUrl:[Character] = ["o"]

/*: "needFold" :*/
fileprivate let data_recommendFormat:[Character] = ["n"]
fileprivate let userBecomeStr:String = "cheek receive situation true givingeedFold"

/*: "group_ :*/
fileprivate let user_principalData:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let k_securityFileRollingMsg:String = "IM\u{5220}"
fileprivate let showPlanMessage:String = "除\u{4f1a}\u{8bdd}失败"

/*: "IM置顶会话失败，convId: :*/
fileprivate let notiHemTitle:[Character] = ["I","M","置","顶","会","话","失","败"]
fileprivate let noti_thinkStr:String = "\u{ff0c}"
fileprivate let show_shapeMessage:String = "convId:within supposed sh operate"

/*: "SDK 正在连接到服务器" :*/
fileprivate let user_computeExamineStr:[Character] = ["S","D","K"," ","正","在","连","接","到","服","\u{52a1}"]
fileprivate let appFastId:[Character] = ["器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let noti_appBottleTitle:String = "SDK 已\u{7ecf}"
fileprivate let dataTargetName:String = "成功连接"

/*: "IM连接服务器失败，code: :*/
fileprivate let showHemMsg:[Character] = ["I","M","\u{8fde}","接","服","务","器","失","败"]
fileprivate let noti_boyValidName:String = "\u{ff0c}code:"

/*: , error: :*/
fileprivate let noti_associationDismissFormat:String = ","
fileprivate let noti_lipMessage:String = "guard need special error:"

/*: "IM账号被挤下线。" :*/
fileprivate let k_discoverKey:String = "IM账chase talk mobile recover skin"
fileprivate let main_roundingTitle:String = "号被挤下线。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let mainCarveMessage:[UInt8] = []

fileprivate func usedCrease(robot num: UInt8) -> UInt8 {
    let value = Int(num) + 255
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let const_plungeTitle:[Character] = ["O","f","f","l","i","n","e"]
fileprivate let user_lowName:String = " Notbottle make offer care"

/*: "Cancel" :*/
fileprivate let k_hiValue:String = "blue essentialCancel"

/*: "Re Login" :*/
fileprivate let notiNoseName:String = "Re Logcriminal beauty manage"
fileprivate let kNeverData:String = "violationn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExistLeastManager.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ElementBarObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func allowTask(data: [MiddleThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func occurSuite(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func radiogram(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum MigrationDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class ExistLeastManager: NSObject {
@objcMembers public class ExistLeastManager: NSObject {
    //: public static let shared = ExistLeastManager()
    public static let shared = ExistLeastManager()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [MiddleThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [MiddleThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [MiddleThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [MiddleThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [MiddleThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: MiddleThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.commentToNumber()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func nameMentionInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(user_saveStorageKey, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func commentToNumber() {
        //: func__removeAllDelegate()
        merely()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.headShape()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func headShape() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = MiddleThen.fun(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = MiddleThen.fun(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func advertisingOff() -> [MiddleThen] {
        // 审核模式，在消息列表移除官方客服
        //: if AppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if AppManager.share.appStatus == EarnLetterCustomReflectable.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == DelineateThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == DelineateThen.enterId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension ExistLeastManager {
extension ExistLeastManager {
    //: func func__removeAllDelegate() {
    func merely() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func magnitudeDoinge(_ delegate: ElementBarObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func smooth(_ delegate: ElementBarObjectProtocol) {
        //: self.func__invokeDelegate {
        self.verticalResolution {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func verticalResolution(_ invocation: (ElementBarObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ElementBarObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension ExistLeastManager {
extension ExistLeastManager {
    /// 退出登录IM
    //: func func__LogingOut() {
    func antiquity() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (const_nearbyData + "登录失败\u{ff0c}c" + String(k_announcementMessage.suffix(4))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.commentToNumber()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func recordingDomain(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = AppManager.share.appUserConfigMode.uSig
        let uSigStr = AppManager.share.appUserConfigMode.uSig
        //: let uidStr = AppManager.share.loginUserMode.userID
        let uidStr = AppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.writtenOpen()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (show_statuteReloadlyData + "败，code:") + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func laughDoing() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || AppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || AppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.diskTerms(showMsg: show_titleOfName)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension ExistLeastManager {
extension ExistLeastManager {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func writtenOpen() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (String(mainSignalPath.prefix(5)) + "\u{8bfb}消息总数失败" + String(kSpringData)) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func cur() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        HouseThen.takeQuit { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(noti_yellowMessage.lowercased() + "hBadge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: constButtonUrl, object: self, userInfo: [String(bytes: mainStandMessage.map{eyeOnce(execute: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func orientationCompletion(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        HouseThen.operation { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(dataShadowUrl))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(user_wantText.prefix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = MiddleThen.fun(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(kHungName.replacingOccurrences(of: "priority", with: "u"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(constThanHumanMessage) + String(k_relatedMessage))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension ExistLeastManager: V2TIMAdvancedMsgListener {
extension ExistLeastManager: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((const_ehPath.replacingOccurrences(of: "carrier", with: "LI") + String(showProjectFeedbackMessage.suffix(6))))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            HouseBalloonDanmuManager.charmMatch().extraWeek(msg: msg)
            //: return
            return
        }
        //: if msg.sender == AppManager.share.loginUserMode.userID { return }
        if msg.sender == AppManager.share.loginUserMode.userID { return }

        //: let dic = SerpentesChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = SerpentesChatManager.nodCustomAccountingData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(show_sharedUrl.replacingOccurrences(of: "ease", with: "t"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? BelowModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kUserUrl, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(kSoapTitle))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kBullMessage) + show_encounterKey.replacingOccurrences(of: "remark", with: "t")) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: appLiveNextUrl, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if SerpentesChatManager.checkVoiceMessage(message: msg) {
            if SerpentesChatManager.fine(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = StillThen.headInside(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                FactoryThen.shapeAcross(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.force(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SizeReactiveCompatible.shared.statuteTitleMultiSendFlag(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SizeReactiveCompatible.shared.weekUnless(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(main_proximateHereKey.suffix(6)) + "tMenti" + String(k_openlyTitle)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            RateThen.share.receivedRangeDegreeProgram(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: LetterChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: LetterChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != DelineateThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != DelineateThen.slowModify() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.verticalResolution { $0.radiogram?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ProvideThen.shared.validOfAlongside(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: constFeeFormat.map{$0^201}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: const_shrinkMsg.map{addOn(unite: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: main_civilPath.map{finishingLine(please: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: const_transformMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            FiniteInfoManager.present(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            LetterChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: k_nearbyData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: PressureReplyCellData, isPrivete: Bool) {
    @objc public func shared(targetId: String, msgCellData: PressureReplyCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.hypothetical(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(showOptionText)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.length(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.versation(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                NiftyLogTool.paragraphFromRank(msg: (String(const_descriptionStr)) + "\(convId)" + (String(data_lowerTitle.suffix(7))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func hypothetical(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = AppManager.share.loginUserMode.userID
        let senderStr = AppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
//                NiftyLogTool.paragraphFromRank(msg: (String(noti_withEmissionId.suffix(4)) + "\u{6d88}息失\u{8d25}，" + k_accessValue.replacingOccurrences(of: "recommendation", with: "rg") + String(noti_aData.suffix(5))) + "\(targetId)" + (String(data_lowerTitle.suffix(7))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func force(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(app_partnerValue.suffix(8)) + String(mainClueId))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && ElementEarnReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(main_absValue.replacingOccurrences(of: "merely", with: "r") + String(main_oneDistanceData.suffix(8)))].stringValue == (String(kTermsName.prefix(5)) + String(app_workerData)) && isSelf && ElementEarnReactiveCompatible.share.modal()!.isKind(of: ChatViewDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: data_successReasonValue) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: data_successReasonValue)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension ExistLeastManager: V2TIMConversationListener {
extension ExistLeastManager: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.length(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.versation(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.length(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.versation(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: AppManager.share.unreadMessageNum = Int(totalUnreadCount)
        AppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.verticalResolution {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.occurSuite?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func versation(conVersationList: [MiddleThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if FiniteInfoManager.customAbout().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                FiniteInfoManager.customAbout().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.verticalResolution {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.allowTask?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func doing(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [MiddleThen] = []

        // V2TIMConversation->MiddleThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = MiddleThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.pick(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [MiddleThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = LightMagnitudeensityInfoTable.strengthActualWith(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.addZone(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension ExistLeastManager {
extension ExistLeastManager {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func discOrBright(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.doing(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.versation(conVersationList: arr as? [MiddleThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (String(notiFenceName.suffix(5)) + "话列表\u{5931}败" + notiCheekUrl + String(userScriptStr.suffix(2))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func fragment(targetId: String) -> MiddleThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func length(convList: [V2TIMConversation]) -> [MiddleThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [MiddleThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = MiddleThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.pick(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.addZone(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func addZone(convModel: MiddleThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.headShape()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: MigrationDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = FiniteInfoManager.doingeAgain(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = SerpentesChatManager.parseTXMessageData(data: elemData)
            let dict = SerpentesChatManager.nodCustomAccountingData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(show_sharedUrl.replacingOccurrences(of: "ease", with: "t"))][(String(mainPointStr) + String(noti_hiUrl))][(String(data_recommendFormat) + String(userBecomeStr.suffix(7)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.gesture(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.gesture(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func gesture(convList: [MiddleThen], dataType: MigrationDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func highlightPan(targetId: String, dataType: MigrationDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(showOptionText)) + "\(targetId)" : (String(user_principalData)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            FiniteInfoManager.compute(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(AppManager.share.loginUserMode.userID, toUid: targetId)
            FactoryThen.link(AppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.writtenOpen()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (k_securityFileRollingMsg + showPlanMessage + "，convID:") + "\(convID)" + (String(data_lowerTitle.suffix(7))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func previousTo(convModel: MiddleThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            NiftyLogTool.paragraphFromRank(msg: (String(notiHemTitle) + noti_thinkStr + String(show_shapeMessage.prefix(7))) + "\(convModel.conversationID)" + (String(data_lowerTitle.suffix(7))) + "\(code)" + (String(kSocialRegulateFormat.prefix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.gesture(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.gesture(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension ExistLeastManager: V2TIMSDKListener {
extension ExistLeastManager: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(user_computeExamineStr) + String(appFastId)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (noti_appBottleTitle + dataTargetName.capitalized + "\u{5230}服务器"))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        NiftyLogTool.paragraphFromRank(msg: (String(showHemMsg) + noti_boyValidName) + "\(code)" + (noti_associationDismissFormat.capitalized + String(noti_lipMessage.suffix(7))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        NiftyLogTool.paragraphFromRank(msg: (String(k_discoverKey.prefix(3)) + main_roundingTitle.capitalized))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        MigrationSocketDelegate.shared.limitedSame(userLoginOut: true)
        //: ExistLeastManager.shared.isConnection = false
        ExistLeastManager.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (WhiteThen.shared as! WhiteThen).albumTo()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: mainCarveMessage.map{usedCrease(robot: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = EditAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if YesterdayAnotherAddrTool.share.interfaceLang == RealmNameLiteral.ar.rawValue || YesterdayAnotherAddrTool.share.interfaceLang == RealmNameLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            YesterdayAnotherAddrTool.share.interfaceLang == RealmNameLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.pingfangBusy(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        PulseReactiveCompatible.contentConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        PulseReactiveCompatible.contentConfig(title: (String(const_plungeTitle) + String(user_lowName.prefix(4)) + "ification").localized, message: msg, leftBtnTitle: (String(k_hiValue.suffix(6))).localized, rightBtnTitle: (String(notiNoseName.prefix(6)) + kNeverData.replacingOccurrences(of: "violation", with: "i")).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AfterPushListener.at().attributeAll()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            PhotoPreserveReactiveCompatible.lap { t in
                //: ProgressHUD.dismiss()
                UniversalProgressHUD.coverBetween()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: main_domainColorErrorStr, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            PhotoPreserveReactiveCompatible.now { t in
                //: if t {
                if t {
                    //: ExistLeastManager.shared.func__LogingIn { _ in
                    ExistLeastManager.shared.recordingDomain { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if AfterPushListener.at().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(AfterPushListener.at().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    MigrationSocketDelegate.shared.barb()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
