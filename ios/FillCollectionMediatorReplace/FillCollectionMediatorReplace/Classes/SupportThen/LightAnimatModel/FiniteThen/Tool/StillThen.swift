
//: Declare String Begin

/*: "msgType" :*/
fileprivate let app_targetData:String = "msgTypbucket click"
fileprivate let showMatterFormat:String = "E"

/*: "tips" :*/
fileprivate let app_hiId:String = "runningps"

/*: "extra" :*/
fileprivate let noti_seaText:String = "signalxtra"

/*: "msgInfo" :*/
fileprivate let main_equityName:String = "msgInfcover conclude"
fileprivate let notiYardText:[Character] = ["o"]

/*: "user" :*/
fileprivate let data_reasonKey:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let main_teamData:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let main_exposureName:[UInt8] = [0xac,0xab,0xa1,0xa9,0xac,0xa3,0xaf,0xa7]

private func recognizeStrip(segment num: UInt8) -> UInt8 {
    return num ^ 194
}

/*: "headPic" :*/
fileprivate let k_bubbleTitle:[UInt8] = [0x90,0x8d,0x89,0x8c,0x78,0x91,0x8b]

fileprivate func magnituderoJob(unknown num: UInt8) -> UInt8 {
    let value = Int(num) - 40
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "age" :*/
fileprivate let dataLiverFormat:[UInt8] = [0xc8,0xce,0xcc]

private func capacityThat(device num: UInt8) -> UInt8 {
    return num ^ 169
}

/*: "gift" :*/
fileprivate let mainChapterData:String = "giffresh"

/*: "isGroup" :*/
fileprivate let show_mistBeforeValue:String = "each resign none administrativeisGroup"

/*: "WantGift" :*/
fileprivate let k_resolutionKey:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let k_choiceToneId:String = "ditt"

/*: "img" :*/
fileprivate let kCharmStr:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let k_labelBridgeStr:String = "VIDEO"

/*: "audio" :*/
fileprivate let kRateNameContent:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let appEntryInformUrl:String = "messageTcustom seek contact cooperative capacity"
fileprivate let showPlainFormat:[Character] = ["y","p","e"]

/*: "contentType" :*/
fileprivate let show_outerName:[Character] = ["c","o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let const_minuteData:[Character] = ["I","m","g","M","s"]
fileprivate let showTransportFormat:String = "solution"

/*: "imageUri" :*/
fileprivate let notiExecuteMsg:String = "iproperly"
fileprivate let show_splitPath:[Character] = ["a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let noti_directionMessage:String = "TYPE"

/*: "pic" :*/
fileprivate let mainDimensionStr:String = "presistancec"

/*: "isBurn" :*/
fileprivate let notiPrisonFormat:String = "restriction pi show currency exactlyisBurn"

/*: "giftId" :*/
fileprivate let constContextFormat:String = "G"
fileprivate let notiTheoryId:String = "draft tumble orientation jobiftId"

/*: "params" :*/
fileprivate let dataSeedUpKey:String = "pbecomerbecomems"

/*: "audioLength" :*/
fileprivate let data_wrapGoingFormat:String = "depth state administrative dropaudioLe"
fileprivate let data_illegalLengthName:[Character] = ["n","g","t","h"]

/*: "len" :*/
fileprivate let user_denyId:String = "laround"

/*: "audioSandbox" :*/
fileprivate let data_ratingUrl:String = "astackdi"
fileprivate let appKingStr:String = "opan"

/*: "senderId" :*/
fileprivate let user_submitTitle:String = "SEND"

/*: "AudioMsg" :*/
fileprivate let main_originalKey:[Character] = ["A"]
fileprivate let main_menuPushName:String = "maybe normal along homeudioMsg"

/*: "content" :*/
fileprivate let showBaseballData:String = "cowavee"
fileprivate let noti_upperMatchPrintValue:String = "NT"

/*: "TxtMsg" :*/
fileprivate let appPullFormat:String = "TxtMsgwhether guidance language"

/*: "message" :*/
fileprivate let constHoodValue:String = "mremindersag"
fileprivate let data_dismissUrl:String = "consequence"

/*: "quoteMsgId" :*/
fileprivate let userQualityData:[Character] = ["q","u","o","t","e"]
fileprivate let showAcrossContent:[Character] = ["M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let dataFindNailContent:String = "performot"
fileprivate let k_leaveStr:String = "combine"

/*: "rYMsgType" :*/
fileprivate let k_bagPath:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let main_visualText:[Character] = ["G","J",":","S","e","s","s"]
fileprivate let constChallengeTitle:[Character] = [":","A","u","t","o","G","r","e"]
fileprivate let notiModelReaderUrl:String = "et:Tcircle domain camp snow"

/*: "changeDirection" :*/
fileprivate let user_processingKey:String = "multiple budgetchangeDir"
fileprivate let k_thatStr:String = "ectconnectn"

/*: "system" :*/
fileprivate let appReadingPath:[Character] = ["s","y","s","t","e"]
fileprivate let user_spanContent:String = "tip"

/*: "title" :*/
fileprivate let main_totalFormat:String = "titlblue"

/*: "icon" :*/
fileprivate let showCompositionStr:String = "icworldn"

/*: "all" :*/
fileprivate let show_halfKey:String = "bullll"

/*: "icon_chats_pcr" :*/
fileprivate let dataIdentifyName:[Character] = ["i","c","o","n","_","c","h"]
fileprivate let const_resultData:String = "bottom export join canats_pcr"

/*: "status" :*/
fileprivate let user_calculationPath:String = "cloudtatu"
fileprivate let main_shareFullKey:String = "S"

/*: "msgIncome" :*/
fileprivate let show_currentlyFormat:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let dataHeritageStr:String = "msgIdsegment who"

/*: "ElementCustomReflectable" :*/
fileprivate let dataDistributionName:String = "Tpopularity"
fileprivate let notiUrgeContent:[Character] = ["s","g","T","r","a","n","s","l","a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let main_allKey:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","d","C","o","n","t","e","n"]
fileprivate let dataColumnKey:[Character] = ["t"]

/*: "formatType" :*/
fileprivate let show_statSpringTryValue:String = "forleading"
fileprivate let constCalledData:String = "atTypetemp home"

/*: "plain" :*/
fileprivate let mainDateName:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let mainMidValue:[Character] = ["i","m","a","g","e","D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let noti_countrySituationTitle:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let appConcernContent:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let dataMarriedGenderMessage:String = "except his temporaryuserInt"
fileprivate let kSecondStr:String = "rinside"

/*: "targetId" :*/
fileprivate let app_firstUrl:String = "tlatrg"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let data_planTitle:String = "approve join option guidance atyyyy-"
fileprivate let mainFineStr:[Character] = ["d"," ","H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let app_cuteValue:String = "reserve or theoretical neversendTi"
fileprivate let userCloseValue:String = "mvolume"

/*: "isRead" :*/
fileprivate let data_fromText:String = "isReadpopularity application"

/*: "audioData" :*/
fileprivate let noti_startDefineData:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let kByPrimaryPath:String = "scene"
fileprivate let dataMeetingPartnerTitle:String = "local segment convert lashudioUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StillThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum TypeSendable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - StillThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class StillThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func cookie() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PressureReplyCellData? {
    public class func sewageDisposalPlantToConclude(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> PressureReplyCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = estimated(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: ExistLeastManager.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            ExistLeastManager.shared.shared(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PressureReplyCellData? {
    public class func estimated(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> PressureReplyCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] = (app_hiId.replacingOccurrences(of: "running", with: "ti"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(noti_seaText.replacingOccurrences(of: "signal", with: "e")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = maleData(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SimultaneouslyThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = BelowModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? TemporaryModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(main_equityName.prefix(6)) + String(notiYardText))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(data_reasonKey))] = [
            //: "uid": AppManager.share.loginUserMode.userID,
            (String(main_teamData)): AppManager.share.loginUserMode.userID,
            //: "nickname": AppManager.share.loginUserMode.nickname,
            String(bytes: main_exposureName.map{recognizeStrip(segment: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.nickname,
            //: "headPic": AppManager.share.loginUserMode.headPic,
            String(bytes: k_bubbleTitle.map{magnituderoJob(unknown: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.headPic,
            //: "age": AppManager.share.loginUserMode.age,
            String(bytes: dataLiverFormat.map{capacityThat(device: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(noti_seaText.replacingOccurrences(of: "signal", with: "e")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = maleData(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (mainChapterData.replacingOccurrences(of: "fresh", with: "t")) {
            //: var data: PressureReplyCellData
            var data: PressureReplyCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(show_mistBeforeValue.suffix(7)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PulseCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = StayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = StayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(k_resolutionKey)) {
            //: let data = FiniteThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = FiniteThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (k_choiceToneId.replacingOccurrences(of: "dit", with: "tx")) {
            //: var data: PressureReplyCellData
            var data: PressureReplyCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(show_mistBeforeValue.suffix(7)))] as? String == "1" { // 群聊
                //: data = ListCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ListCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = MirrorCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = MirrorCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(kCharmStr)) {
            //: let data = MigrationCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MigrationCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (k_labelBridgeStr.lowercased()) {
            //: let data = ButtonReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ButtonReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(kRateNameContent)) {
            //: let data = MeditationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MeditationThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(mainChapterData.replacingOccurrences(of: "fresh", with: "t"))] != nil && (msgInfo[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int) == 4 {
            //: let data = MirrorCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = MirrorCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? BelowModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: PressureReplyCellData) -> PressureReplyCellData? {
    public class func listen(withData cellData: PressureReplyCellData) -> PressureReplyCellData? {
        // 图片
        //: if let reCellData = cellData as? MigrationCellData {
        if let reCellData = cellData as? MigrationCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(DelineateThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(DelineateThen.engageLikeAppellation())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(show_outerName))] = (String(const_minuteData) + showTransportFormat.replacingOccurrences(of: "solution", with: "g"))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] = (String(kCharmStr))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(notiExecuteMsg.replacingOccurrences(of: "properly", with: "m") + String(show_splitPath))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_equityName.prefix(6)) + String(notiYardText))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(noti_directionMessage.lowercased())] = (String(const_minuteData) + showTransportFormat.replacingOccurrences(of: "solution", with: "g"))
            //: params["pic"] = filePath
            params[(mainDimensionStr.replacingOccurrences(of: "resistance", with: "i"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(notiPrisonFormat.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(notiPrisonFormat.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(constContextFormat.lowercased() + String(notiTheoryId.suffix(5)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(constContextFormat.lowercased() + String(notiTheoryId.suffix(5)))] = "0"
            }

            //: dic["params"] = params
            dic[(dataSeedUpKey.replacingOccurrences(of: "become", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = estimated(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? ButtonReactiveCompatible {
        if let reCellData = cellData as? ButtonReactiveCompatible {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? MeditationThen {
        if let reCellData = cellData as? MeditationThen {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = FiniteInfoManager.regain(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(data_wrapGoingFormat.suffix(7)) + String(data_illegalLengthName))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(user_denyId.replacingOccurrences(of: "around", with: "en"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(data_ratingUrl.replacingOccurrences(of: "stack", with: "u") + "oSandb" + appKingStr.replacingOccurrences(of: "pan", with: "x"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(data_ratingUrl.replacingOccurrences(of: "stack", with: "u") + "oSandb" + appKingStr.replacingOccurrences(of: "pan", with: "x"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] = (String(kRateNameContent))
            //: msgInfo["senderId"] = AppManager.share.loginUserMode.userID
            msgInfo[(user_submitTitle.lowercased() + "erId")] = AppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_equityName.prefix(6)) + String(notiYardText))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(noti_directionMessage.lowercased())] = (String(main_originalKey) + String(main_menuPushName.suffix(7)))
            //: dic["params"] = params
            dic[(dataSeedUpKey.replacingOccurrences(of: "become", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = estimated(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? MirrorCellData {
        if let reCellData = cellData as? MirrorCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(showBaseballData.replacingOccurrences(of: "wave", with: "nt") + noti_upperMatchPrintValue.lowercased())] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] = (k_choiceToneId.replacingOccurrences(of: "dit", with: "tx"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_equityName.prefix(6)) + String(notiYardText))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(noti_directionMessage.lowercased())] = (String(appPullFormat.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(constHoodValue.replacingOccurrences(of: "reminder", with: "es") + data_dismissUrl.replacingOccurrences(of: "consequence", with: "e"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.loadTitle() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(userQualityData) + String(showAcrossContent))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(dataFindNailContent.replacingOccurrences(of: "perform", with: "qu") + "eMsgInf" + k_leaveStr.replacingOccurrences(of: "combine", with: "o"))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(dataSeedUpKey.replacingOccurrences(of: "become", with: "a"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = estimated(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PressureReplyCellData? {
    public class func sumDraft(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> PressureReplyCellData? {
        //: var cellData: PressureReplyCellData?
        var cellData: PressureReplyCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(noti_seaText.replacingOccurrences(of: "signal", with: "e"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(main_equityName.prefix(6)) + String(notiYardText))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(data_reasonKey))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? BelowModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == AppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == AppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == AppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == AppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(k_bagPath))] as? String == (String(main_visualText) + String(constChallengeTitle) + String(notiModelReaderUrl.prefix(4)) + "xtCustom") {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(user_processingKey.suffix(9)) + k_thatStr.replacingOccurrences(of: "connect", with: "io"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = successTo(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(appReadingPath) + user_spanContent.replacingOccurrences(of: "tip", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = BelowModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? TemporaryModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SimultaneouslyThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(showBaseballData.replacingOccurrences(of: "wave", with: "nt") + noti_upperMatchPrintValue.lowercased())] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = MigrationCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MigrationCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = ButtonReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ButtonReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PulseCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = StayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = StayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = aggregation(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(noti_seaText.replacingOccurrences(of: "signal", with: "e"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(main_equityName.prefix(6)) + String(notiYardText))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? ChatModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = FiniteThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = FiniteThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = MeditationThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = MeditationThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? MeditationThen {
            if let audioCellData = cellData as? MeditationThen {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = naturalEvent(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = FactoryThen.screenNeed(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = FactoryThen.shapeAcross(headInside(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ReplyCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(main_totalFormat.replacingOccurrences(of: "blue", with: "e"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(showBaseballData.replacingOccurrences(of: "wave", with: "nt") + noti_upperMatchPrintValue.lowercased())] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(kCharmStr))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GoodCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ListCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ListCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = MirrorCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = MirrorCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? ExperimentalModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(showCompositionStr.replacingOccurrences(of: "world", with: "o"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: AppManager.share.loginUserMode.headPic ?? ""
                        AppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = ChatModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(showBaseballData.replacingOccurrences(of: "wave", with: "nt") + noti_upperMatchPrintValue.lowercased())] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(noti_seaText.replacingOccurrences(of: "signal", with: "e"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = SimultaneouslyModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (show_halfKey.replacingOccurrences(of: "bull", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(dataIdentifyName) + String(const_resultData.suffix(7)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.beforeJson(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(user_calculationPath.replacingOccurrences(of: "cloud", with: "s") + main_shareFullKey.lowercased())] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(show_currentlyFormat))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(dataHeritageStr.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(dataDistributionName.replacingOccurrences(of: "popularity", with: "M") + String(notiUrgeContent))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = ElementCustomReflectable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(main_allKey) + String(dataColumnKey))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = LaterModel.post()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = LaterModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = SchemeExcludeModel.line()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = SchemeExcludeModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = PleaseModel.rootRunning()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func successTo(_ extraDic: [String: Any]) -> TypeSendable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(noti_seaText.replacingOccurrences(of: "signal", with: "e"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(main_equityName.prefix(6)) + String(notiYardText))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? BelowModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(show_statSpringTryValue.replacingOccurrences(of: "leading", with: "m") + String(constCalledData.prefix(6)))] as? String == (String(mainDateName)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(notiExecuteMsg.replacingOccurrences(of: "properly", with: "m") + String(show_splitPath))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(mainMidValue))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(noti_countrySituationTitle)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = ta(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(appConcernContent))] as? Int) == 1 || (msgInfo?[(String(appConcernContent))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (app_hiId.replacingOccurrences(of: "running", with: "ti")) || (dic?[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(show_statSpringTryValue.replacingOccurrences(of: "leading", with: "m") + String(constCalledData.prefix(6)))] as? String == "mf") && ((dic?[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int) == 15 || (dic?[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(dataMarriedGenderMessage.suffix(7)) + kSecondStr.replacingOccurrences(of: "inside", with: "o")) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String == (String(k_resolutionKey)) || (msgInfo?[(String(appEntryInformUrl.prefix(8)) + String(showPlainFormat))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func headInside(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.beforeJson(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(noti_seaText.replacingOccurrences(of: "signal", with: "e"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(main_equityName.prefix(6)) + String(notiYardText))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(data_reasonKey))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(user_submitTitle.lowercased() + "erId")] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == AppManager.share.loginUserMode.userID {
        if senderId == AppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(user_submitTitle.lowercased() + "erId")] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(app_firstUrl.replacingOccurrences(of: "lat", with: "a") + "etId")] = message.userID ?? (user?[(app_firstUrl.replacingOccurrences(of: "lat", with: "a") + "etId")] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(user_submitTitle.lowercased() + "erId")] = "\(msgInfo?[(String(main_teamData))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(app_firstUrl.replacingOccurrences(of: "lat", with: "a") + "etId")] = "\(msgInfo?[(String(main_teamData))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(dataHeritageStr.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(dataHeritageStr.prefix(5)))] = "\(msgInfo?[(String(dataHeritageStr.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.beforeJson(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(dataHeritageStr.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(dataHeritageStr.prefix(5)))] = "\(customDic[(String(dataHeritageStr.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(dataHeritageStr.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((data_ratingUrl.replacingOccurrences(of: "stack", with: "u") + "oSandb" + appKingStr.replacingOccurrences(of: "pan", with: "x"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(data_ratingUrl.replacingOccurrences(of: "stack", with: "u") + "oSandb" + appKingStr.replacingOccurrences(of: "pan", with: "x"))] = "\(msgInfo?[(data_ratingUrl.replacingOccurrences(of: "stack", with: "u") + "oSandb" + appKingStr.replacingOccurrences(of: "pan", with: "x"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(data_wrapGoingFormat.suffix(7)) + String(data_illegalLengthName))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(data_wrapGoingFormat.suffix(7)) + String(data_illegalLengthName))] = "\(msgInfo?[(String(data_wrapGoingFormat.suffix(7)) + String(data_illegalLengthName))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(data_planTitle.suffix(5)) + "MM-d" + String(mainFineStr))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(app_cuteValue.suffix(6)) + userCloseValue.replacingOccurrences(of: "volume", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == AppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(data_fromText.prefix(6)))] = senderId == AppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func naturalEvent(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = headInside(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(dataHeritageStr.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func aggregation(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func maleData(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func ta(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(app_targetData.prefix(6)) + showMatterFormat.lowercased())] as? String, msgType == (String(kRateNameContent)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(show_outerName))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(main_originalKey) + String(main_menuPushName.suffix(7)))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(noti_startDefineData))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(kByPrimaryPath.replacingOccurrences(of: "scene", with: "a") + String(dataMeetingPartnerTitle.suffix(7)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
