
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_writtenValue:[UInt8] = [0xc3,0xc4,0xc3,0xde,0x82,0xc9,0xc5,0xce,0xcf,0xd8,0x90,0x83,0x8a,0xc2,0xcb,0xd9,0x8a,0xc4,0xc5,0xde,0x8a,0xc8,0xcf,0xcf,0xc4,0x8a,0xc3,0xc7,0xda,0xc6,0xcf,0xc7,0xcf,0xc4,0xde,0xcf,0xce]

private func statisticalSource(estimated num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "F5F5F5" :*/
fileprivate let noti_feedData:String = "f5f5f5"

/*: "#128CFF" :*/
fileprivate let constManTitle:String = "ret asset path word#128CFF"

/*: "Feedback" :*/
fileprivate let k_sequenceUrl:String = "Feedexclusive s emission cell feedback"

/*: "#8370FF" :*/
fileprivate let main_recentName:[Character] = ["#","8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let app_iconMessage:[Character] = ["Q","u","i","c","k"," ","G","r","e","e"]
fileprivate let app_gestureMsg:[Character] = ["t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let data_findMessage:String = "Becapital cycle"
fileprivate let data_forceKey:[Character] = ["a","u","t","y"," ","P","h","o","t","o","s"]

/*: "url" :*/
fileprivate let main_easeName:String = "urserver"

/*: "unlockGift" :*/
fileprivate let main_subjectActiveMsg:[Character] = ["u","n","l","o","c"]
fileprivate let kPremiumId:[Character] = ["k","G","i","f","t"]

/*: "isBurn" :*/
fileprivate let k_beforeData:[UInt8] = [0x6e,0x72,0x75,0x42,0x73,0x69]

/*: "giftId" :*/
fileprivate let k_positKey:String = "bound can chasegiftId"

/*: "rYMsgType" :*/
fileprivate let show_seekData:String = "rYMsgTypebad drop"

/*: "GJ:CallCustom" :*/
fileprivate let constAllLashUrl:String = "GJ:Caexit wed entry native"
fileprivate let main_divisionOutputTitle:String = "llCudescription selection"

/*: "changeDirection" :*/
fileprivate let data_characterUrl:String = "chauniversale"
fileprivate let user_viaUrl:[Character] = ["t","i","o","n"]

/*: "extra" :*/
fileprivate let noti_handleUrl:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let kWelcomeId:String = "msgInfobeam adjustment ta text drop"

/*: "toUid" :*/
fileprivate let app_givenName:String = "toUidspring increase recommend"

/*: "wantGift" :*/
fileprivate let constServiceFormat:String = "wantGifequal warn"
fileprivate let userSaltData:String = "listen"

/*: "jumpKey" :*/
fileprivate let k_breezePath:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let user_statuteText:String = "remain conclude clip must whotrueP"
fileprivate let noti_bounceName:[Character] = ["e","r","s","o","n","A","u","t","h"]

/*: "logType" :*/
fileprivate let constReplyContent:[Character] = ["l","o","g","T","y","p"]
fileprivate let dataInfoId:[Character] = ["e"]

/*: "msgType" :*/
fileprivate let user_pairFormat:[UInt8] = [0x4,0x1a,0xe,0x3d,0x10,0x19,0xc]

private func baseballClub(spring num: UInt8) -> UInt8 {
    return num ^ 105
}

/*: "txt" :*/
fileprivate let constPushMessage:String = "tgroup"

/*: "type" :*/
fileprivate let main_scoreMessage:String = "typscreen"

/*: "QuickMsg" :*/
fileprivate let const_honeyChamberName:String = "Quicdry clue comply"

/*: "params" :*/
fileprivate let k_refugeUrl:[UInt8] = [0x3d,0x2c,0x3f,0x2c,0x20,0x3e]

private func throughYear(clearly num: UInt8) -> UInt8 {
    return num ^ 77
}

/*: "content" :*/
fileprivate let userPendingSortPath:[UInt8] = [0xf0,0xfc,0xfb,0x1,0xf2,0xfb,0x1]

fileprivate func pictureCrop(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 141
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "txtId" :*/
fileprivate let main_depthName:String = "txtIdsource who violation"

/*: "message" :*/
fileprivate let mainCurrencyName:[UInt8] = [0xaa,0xa2,0xb4,0xb4,0xa6,0xa0,0xa2]

/*: "TxtMsg" :*/
fileprivate let kGoodStr:String = "presentation"
fileprivate let user_hoodGreenFormat:String = "xtMsgdry break basic"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let notiTickGreetStr:[UInt8] = [0xdf,0xe6,0xe0,0xea,0xec,0xa9,0xe1,0xec,0xe5,0xe5,0xe6,0xa9,0xed,0xe6,0xfe,0xe7,0xe5,0xe6,0xe8,0xed,0xa9,0xef,0xe8,0xe0,0xe5,0xec,0xed,0xa7,0xa9,0xd9,0xfb,0xe6,0xea,0xec,0xed,0xfc,0xfb,0xec]

/*: "length" :*/
fileprivate let appTakeData:[Character] = ["l"]
fileprivate let app_senseDragId:String = "ENGTH"

/*: "audio" :*/
fileprivate let main_appealPrepareFormat:String = "aukito"

/*: "audioId" :*/
fileprivate let notiWhenProspectUrl:String = "read nevertheless privacy ok corneraudioId"

/*: "audioLength" :*/
fileprivate let data_wholeBlueUrl:[UInt8] = [0x33,0x27,0x36,0x3b,0x3d,0x1e,0x37,0x3c,0x35,0x26,0x3a]

/*: "audioSandbox" :*/
fileprivate let constPositionTwoKey:String = "bounceudio"
fileprivate let user_balanceMessage:[Character] = ["S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let main_secretId:[UInt8] = [0xa6,0x98,0xa1,0x97,0x98,0xa5,0x7c,0x97]

fileprivate func nearPass(trace num: UInt8) -> UInt8 {
    let value = Int(num) - 51
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let noti_ignoreTitle:String = "pretty"
fileprivate let app_harassmentEnoughComplaintUrl:[Character] = ["u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let mainHappyUrl:String = "Can\'t "
fileprivate let const_smoothName:String = " blanksuccessfully after"
fileprivate let app_pubName:String = "slibrarye"

/*: "quoteMsgId" :*/
fileprivate let app_increaseName:String = "quomaterial"

/*: "quoteMsgInfo" :*/
fileprivate let showSuiteMessage:String = "quoparticle"
fileprivate let appPicValue:String = "nvalido"

/*: "contentType" :*/
fileprivate let user_constraintName:[UInt8] = [0xc8,0xc4,0xc5,0xdf,0xce,0xc5,0xdf,0xff,0xd2,0xdb,0xce]

private func errorEqually(clip num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "ImgMsg" :*/
fileprivate let showSmallUrl:String = "processing superior then slimImgMsg"

/*: "img" :*/
fileprivate let dataLifeTingFormat:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let const_generateMessage:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let noti_labName:[UInt8] = [0x97,0xa9,0xa7,0xa8,0xa3,0xa1,0x87,0x99,0xa2,0x98]

fileprivate func wantBig(po num: UInt8) -> UInt8 {
    let value = Int(num) - 52
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let dataRegionAddContent:String = "PIC"

/*: "VideoMsg" :*/
fileprivate let appPortionTitle:[Character] = ["V","i","d","e","o","M"]
fileprivate let k_loopData:String = "spath"

/*: "video" :*/
fileprivate let kNapMessage:String = "videaccuracy"

/*: "coverImg" :*/
fileprivate let appDiscountPath:[UInt8] = [0xe9,0xe5,0xfc,0xef,0xf8,0xc3,0xe7,0xed]

private func inPublic(message num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "videoUrl" :*/
fileprivate let noti_conditionMsg:[UInt8] = [0xfd,0xe2,0xef,0xee,0xe4,0xde,0xf9,0xe7]

private func narrowMouthed(scope num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "duration" :*/
fileprivate let dataConnectName:[UInt8] = [0xfb,0xea,0xed,0xfe,0xeb,0xf6,0xf0,0xf1]

/*: "len" :*/
fileprivate let const_teamName:[UInt8] = [0xb1,0xb8,0xb3]

private func serviceMax(support num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "Documents/User/voice/" :*/
fileprivate let constHangMsg:String = "Documsucceed quote"
fileprivate let data_sinceReplaceValue:String = "return again/User"
fileprivate let dataDesignValue:[Character] = ["/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let constWantTitle:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","r","e","c","o"]
fileprivate let k_versionManMsg:[Character] = ["r","d","/"]

/*: "msgIncome" :*/
fileprivate let constResourceId:String = "clothes two lam begin pomsgIncom"
fileprivate let appFinishKey:[Character] = ["e"]

/*: "mfBean" :*/
fileprivate let kPressKey:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let user_memberSkipMessage:[Character] = ["m","e","s","s","a"]
fileprivate let user_consequenceMsg:String = "property certaingeType"

/*: "WantGift" :*/
fileprivate let appJudgeEditionControlFormat:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let noti_delayKey:[Character] = ["_","P","r","i","v","a","t","e"]
fileprivate let noti_announcementName:String = "ChatTego string speaker vertical translate"
fileprivate let showLikeData:String = "tle_bullet star again previous find"

/*: "msgId" :*/
fileprivate let dataWhatUrl:String = "msgIdload method"

/*: "totalMfCoin" :*/
fileprivate let notiProximateMsg:String = "male"
fileprivate let userShapeId:String = "otalMfsurgery accounting block bullet anima"

/*: "attachSysMsg" :*/
fileprivate let constControlConditionFormat:[Character] = ["a","t","t","a","c","h","S"]
fileprivate let mainForthStr:[Character] = ["y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let mainYetMinuteStr:[Character] = ["i","n","t","i","m","a","t","e","P","h","o"]
fileprivate let kCooperativeTitle:String = "TO"

/*: "totalIntimate" :*/
fileprivate let appSuccessfulTitle:String = "technical load chapter focustotalInt"
fileprivate let showTapRemindFormat:String = "imaassistante"

/*: "uid" :*/
fileprivate let notiAnyoneStr:[UInt8] = [0xf,0x13,0x1e]

private func tailPick(urge num: UInt8) -> UInt8 {
    return num ^ 122
}

/*: "intimate" :*/
fileprivate let mainPrimaryTitle:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let const_cancerName:[UInt8] = [0x7,0x25,0x2a,0x63,0x30,0x64,0x31,0x37,0x21,0x64,0x30,0x2c,0x21,0x64,0x22,0x31,0x2a,0x27,0x30,0x2d,0x2b,0x2a,0x64,0x20,0x31,0x21,0x64,0x30,0x2b,0x64,0x3d,0x2b,0x31,0x36,0x64,0x28,0x25,0x27,0x2f,0x64,0x2b,0x22,0x64,0x2d,0x2a,0x30,0x2d,0x29,0x25,0x27,0x3d,0x64,0x28,0x21,0x32,0x21,0x28,0x6a]

private func layRemark(before num: UInt8) -> UInt8 {
    return num ^ 68
}

/*: "giftNum" :*/
fileprivate let mainNegativePath:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let show_nightValue:String = "pkgItewe replacement permission tee standard"

/*: "from" :*/
fileprivate let dataMediaValue:String = "FROM"

/*: "wantGiftId" :*/
fileprivate let data_crossValue:String = "witt"
fileprivate let main_existKey:String = "Idrefuge cheese"

/*: "WantGiftMsg" :*/
fileprivate let main_electActualPath:String = "WantGsimultaneously global enter server convert"
fileprivate let show_noteUrl:String = "hood key meet estimate owniftMsg"

/*: "gift" :*/
fileprivate let noti_femaleFutureOrangeKey:String = "gifher"

/*: "View info" :*/
fileprivate let show_quickNodCognitiveUrl:String = "Viewenvironment container assert"
fileprivate let app_missingTransitionTitle:String = " infocoordinate interval"

/*: "Notes" :*/
fileprivate let kCommentData:String = "Notesion star label feature"

/*: "Report" :*/
fileprivate let main_rejectUrl:String = "Reportdismiss running present put"

/*: "View read receipts?" :*/
fileprivate let showCalendarName:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "She" :*/
fileprivate let show_networkStr:String = "import hood theoretical don examineShe"

/*: "%@ is using too low a version" :*/
fileprivate let k_heartGapPath:String = "%@ isspread equal previous"
fileprivate let noti_labData:String = "g tooedge electric of drag"
fileprivate let user_clingLaterKey:String = "practical beam first cut a v"
fileprivate let mainProductionTitle:String = "ersibonen"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let data_launchPath:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," "]
fileprivate let data_negativeTitle:String = "advice unable makeat l"
fileprivate let k_emptyTitle:String = "5 secondsrecognize implement merely still book"

/*: "Upload failed" :*/
fileprivate let user_onlyId:String = "join edition function componentUplo"
fileprivate let main_insideLengthThatStr:String = "round"

/*: "播放状态: :*/
fileprivate let main_sumUrl:[Character] = ["播","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let show_yardMessage:String = "four var invitation cheese headc2c_"

/*: "msg" :*/
fileprivate let mainTitleCircleStr:String = "writingsg"

/*: "tips" :*/
fileprivate let main_assistSparkPath:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let user_additionalMsg:[UInt8] = [0xd9,0xef,0xf5,0xa0,0xe3,0xe1,0xee,0xa0,0xf5,0xf3,0xe5,0xa0,0xf4,0xe8,0xe5,0xa0,0xf6,0xe9,0xe4,0xe5,0xef,0xa0,0xe3,0xe8,0xe1,0xf4,0xa0,0xe6,0xe5,0xe1,0xf4,0xf5,0xf2,0xe5,0xa0,0xf4,0xef,0xa0,0xe7,0xe9,0xf6,0xe5,0xa0,0xa5,0xc0,0xa0,0xe1,0xa0,0xe3,0xe1,0xec,0xec]

fileprivate func historyVisitor(encounter num: UInt8) -> UInt8 {
    let value = Int(num) - 128
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let userLinkStr:[Character] = ["c","e","l","l","D","a"]
fileprivate let k_publiclyValue:String = "tgovern"

/*: "System notification" :*/
fileprivate let const_exShallName:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c","a"]
fileprivate let showSizeMsg:String = "tticketn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChatViewDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum LabelFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ChatViewDelegate: MirrorRealmReactiveCompatible {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = AccomplishThen()
    private var userWrap = AccomplishThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = LabelFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: AccomplishThen = AccomplishThen()) {
    init(chatID: String, isFrom: LabelFromEnum = .Normal, userWrap: AccomplishThen = AccomplishThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ChatViewDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        LetterChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == DelineateThen.getXiaoMiID() {
        if targetId == DelineateThen.slowModify() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == DelineateThen.getCustomerServiceID() {
        } else if targetId == DelineateThen.enterId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_writtenValue.map{statisticalSource(estimated: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ArgumentView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        centerAnimation()
        //: createUI()
        chemicalAgent()
        //: createUIConstraints()
        reject()
        //: bindInteraction()
        remainPicture()
        //: addTapGestureRecognizer()
        decideRecognizer()
        //: func__requestChatInfo()
        stop()
        //: func__getFillDraftText()
        isExamine()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        RanchHousePlayManager.shared.recall()
        //: func__addVoiceNotification()
        character()
        //: addReadPeceiptNotification()
        renderCos()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            quantityo()
            //: func__TipsNotification()
            remote()
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                //: func__setReplyTipsView()
                outTotalerest()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        upwardly(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        upwardly(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            oldUpon()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            SchemeRatingManager.shared.roundAppealRating()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: EasyTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = EasyTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (noti_feedData.uppercased()))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(DomainViewCell.self, forCellReuseIdentifier: DomainViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(SuckMsgCell.self, forCellReuseIdentifier: SuckMsgCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(EchoSnakeViewCell.self, forCellReuseIdentifier: EchoSnakeViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(ErrorLabelMsgCell.self, forCellReuseIdentifier: ErrorLabelMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(NeighborMsgCell.self, forCellReuseIdentifier: NeighborMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ProposeMsgCell.self, forCellReuseIdentifier: ProposeMsgCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(EffBotThen.self, forCellReuseIdentifier: EffBotThen.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(OtherMessageCell.self, forCellReuseIdentifier: OtherMessageCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(FinishMessageCell.self, forCellReuseIdentifier: FinishMessageCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: SuckThen.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(CompartmentViewCell.self, forCellReuseIdentifier: SuckThen.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ErrorMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = ErrorMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.jumpDown()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: LabelViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: NeighbourViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = LabelViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(constManTitle.suffix(7))))!, .font: UIFont.shareOff(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_sequenceUrl.prefix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(feedbackWithinSpeak), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ResolutionReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ResolutionReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: StigmatizeThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = StigmatizeThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.shareOff(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(main_recentName))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(app_iconMessage) + String(app_gestureMsg)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.retirementProgram().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(direct), for: .touchUpInside)
        //: btn.isHidden = !AppManager.share.appUserConfigMode.showDirectGreet || AppManager.share.loginUserMode.sex == Gender.male.rawValue || AppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !AppManager.share.appUserConfigMode.showDirectGreet || AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue || AppManager.share.appStatus == EarnLetterCustomReflectable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: SuckView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = SuckView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: LightColoredView = {
        //: let v = TalkingSelectTranslationView()
        let v = LightColoredView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.appearField(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ExistResultView = {
        //: let v = TalkingTranslationResultView()
        let v = ExistResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.situationModel(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.shareOff(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(main_recentName))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(data_findMessage.prefix(2)) + String(data_forceKey)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.retirementProgram().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(versionClick), for: .touchUpInside)
        //: btn.isHidden = AppManager.share.loginUserMode.sex == Gender.male.rawValue || AppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue || AppManager.share.appStatus == EarnLetterCustomReflectable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: FrameworkReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = FrameworkReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: HangEnableView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = HangEnableView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: OppositeThen = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = OppositeThen()
        //: v.isHidden = (AppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (AppManager.share.appStatus == EarnLetterCustomReflectable.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: PriceCustomReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = PriceCustomReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.spreadView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ChatViewDelegate: ReportingThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func bequest(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.chooseEarlyView(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ChatViewDelegate: CalculationDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func direct() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        LetterChatManager.save(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.diskTerms(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.crinkle(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func versionClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        LetterChatManager.install(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = HarassmentViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().commonClose(urlStr: dict?[(main_easeName.replacingOccurrences(of: "server", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(main_subjectActiveMsg) + String(kPremiumId))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ElectricModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: k_beforeData.reversed(), encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(k_positKey.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.reflexion(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> PressureReplyCellData! {
    func soap(_: EasyTableViewController!, noneUser data: V2TIMMessage!) -> PressureReplyCellData! {
        //: let dic = SerpentesChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = SerpentesChatManager.nodCustomAccountingData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = StillThen.sumDraft(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ReplyCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(show_seekData.prefix(9)))].stringValue == (String(constAllLashUrl.prefix(5)) + String(main_divisionOutputTitle.prefix(4)) + "stom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(data_characterUrl.replacingOccurrences(of: "universal", with: "ng") + "Direc" + String(user_viaUrl))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(noti_handleUrl))][(String(kWelcomeId.prefix(7)))][(String(app_givenName.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = MirrorCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = MirrorCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: MeditationThen.self) == true ||
        if cellData?.isKind(of: MeditationThen.self) == true ||
            //: cellData?.isKind(of: MigrationCellData.self) == true ||
            cellData?.isKind(of: MigrationCellData.self) == true ||
            //: cellData?.isKind(of: MirrorCellData.self) == true ||
            cellData?.isKind(of: MirrorCellData.self) == true ||
            //: cellData?.isKind(of: ButtonReactiveCompatible.self) == true {
            cellData?.isKind(of: ButtonReactiveCompatible.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            LetterChatManager.chatLaunch(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            LetterChatManager.nameParty(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: PressureReplyCellData!) -> SuckThen! {
    func cover(_: EasyTableViewController!, dark data: PressureReplyCellData!) -> SuckThen! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = worldWide(data: data)

        //: if data.isKind(of: MirrorCellData.self) {
        if data.isKind(of: MirrorCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = DomainViewCell(style: .default, reuseIdentifier: DomainViewCell.className())
            //: let cellData = data as! MirrorCellData
            let cellData = data as! MirrorCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.loadTitle() {
                //: if cellData.msgModel.quoteMsgInfo.uid == AppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == AppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = AppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = AppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: MigrationCellData.self) {
        if data.isKind(of: MigrationCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = SuckMsgCell(style: .default, reuseIdentifier: SuckMsgCell.className())
            //: let cellData = data as! MigrationCellData
            let cellData = data as! MigrationCellData
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ButtonReactiveCompatible.self) {
        if data.isKind(of: ButtonReactiveCompatible.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = EchoSnakeViewCell(style: .default, reuseIdentifier: EchoSnakeViewCell.className())
            //: let cellData = data as! ButtonReactiveCompatible
            let cellData = data as! ButtonReactiveCompatible
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: StayCellData.self) {
        if data.isKind(of: StayCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = ErrorLabelMsgCell(style: .default, reuseIdentifier: ErrorLabelMsgCell.className())
            //: let cellData = data as! StayCellData
            let cellData = data as! StayCellData
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: FiniteThen.self) {
        if data.isKind(of: FiniteThen.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = NeighborMsgCell(style: .default, reuseIdentifier: NeighborMsgCell.className())
            //: let cellData = data as! FiniteThen
            let cellData = data as! FiniteThen
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = PrepareDisplayTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.dryUp(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(constServiceFormat.prefix(7)) + userSaltData.replacingOccurrences(of: "listen", with: "t")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: MeditationThen.self) {
        if data.isKind(of: MeditationThen.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = OtherMessageCell(style: .default, reuseIdentifier: OtherMessageCell.className())
            //: let cellData = data as! MeditationThen
            let cellData = data as! MeditationThen
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: ReplyCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = EffBotThen(style: .default, reuseIdentifier: EffBotThen.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! ReplyCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SimultaneouslyThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ProposeMsgCell(style: .default, reuseIdentifier: ProposeMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SimultaneouslyThen
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: GoodCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = FinishMessageCell(style: .default, reuseIdentifier: FinishMessageCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! GoodCellData
            //: cell.fill(with: cellData)
            cell.metallicElement(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: SuckThen!) {
    func file(_: EasyTableViewController!, expanse cell: SuckThen!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != DelineateThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != DelineateThen.enterId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: ElementEarnReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            ElementEarnReactiveCompatible.share.datePress(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: SuckThen!) {
    func line(_: EasyTableViewController!, message cell: SuckThen!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SuckMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! MigrationCellData
            let imageCellData = cell.messageData as! MigrationCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = MugShotReactiveCompatible(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.resignLock(imageCellData, by: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.camera(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: userSenseMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.toteUp(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.multiLoad(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pushMove()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(userPurchasePath)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = BalloonWhiteTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = OphidiaMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [BalloonWhiteTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = StatusPhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pushMove()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: EchoSnakeViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! ButtonReactiveCompatible
            let videoCellData = cell.messageData as! ButtonReactiveCompatible
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = VideoViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pushMove()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = SupportEnsureViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.circumferent(videoCellData, brightRemote: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.camera(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: userSenseMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.toteUp(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.multiLoad(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                pushMove()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: OtherMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! OtherMessageCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! MeditationThen, cellView: audioCell)
            compartment(cellData: audioCell.cellData as! MeditationThen, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DomainViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DomainViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = SerpentesChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = SerpentesChatManager.nodCustomAccountingData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_handleUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_breezePath))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(k_breezePath))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(user_statuteText.suffix(5)) + String(noti_bounceName)) {
                    //: ElementEarnReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                    ElementEarnReactiveCompatible.share.driveSecond(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AssignTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ElementEarnReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ElementEarnReactiveCompatible.share.periodOfTimeModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(show_seekData.prefix(9)))] as? String == (String(constAllLashUrl.prefix(5)) + String(main_divisionOutputTitle.prefix(4)) + "stom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(kWelcomeId.prefix(7)))][(String(constReplyContent) + String(dataInfoId))].intValue == 1 {
                    //: checkAndCallVoice()
                    query()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    dismissMoment()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ProposeMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ProposeMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = SerpentesChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = SerpentesChatManager.nodCustomAccountingData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_handleUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_breezePath))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AssignTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ElementEarnReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ElementEarnReactiveCompatible.share.periodOfTimeModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: EffBotThen.self) {
            //: ElementEarnReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            ElementEarnReactiveCompatible.share.datePress(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: SuckThen!) {
    func pic(_: EasyTableViewController!, along cell: SuckThen!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: PlanOfActionCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: SuckMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! SuckMsgCell
            //: cellData = imageCell.cellData as! MigrationCellData
            cellData = imageCell.cellData as! MigrationCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: EchoSnakeViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! EchoSnakeViewCell
            //: cellData = videoCell.cellData as! ButtonReactiveCompatible
            cellData = videoCell.cellData as! ButtonReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: OtherMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! OtherMessageCell
            //: cellData = audioCell.cellData as! MeditationThen
            cellData = audioCell.cellData as! MeditationThen
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: DomainViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! DomainViewCell
            //: cellData = textCell.cellData as! MirrorCellData
            cellData = textCell.cellData as! MirrorCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: PressureReplyCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: PressureReplyCellData? = StillThen.listen(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: ButtonReactiveCompatible.self) {
                if reCellData!.isKind(of: ButtonReactiveCompatible.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    sunna(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    purchaseFit(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: PressureReplyCellData!) {
    func marginShrink(_: EasyTableViewController!, blunt cellData: PressureReplyCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = LetterChatManager.speakerCapacity(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: PressureReplyCellData!) {
    func heliogramMessage(_: EasyTableViewController!, jaw cellData: PressureReplyCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        LetterChatManager.onceSnap(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: PressureReplyCellData) -> PressureReplyCellData {
    private func worldWide(data: PressureReplyCellData) -> PressureReplyCellData {
        //: let isSelf = data.msgModel.user.uid == Int(AppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(AppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if AppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if AppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = AppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = AppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = AppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = AppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func dayDoingGreeting() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (constPushMessage.replacingOccurrences(of: "group", with: "xt"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (String(const_honeyChamberName.prefix(4)) + "kMsg")]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.purchaseFit(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func crinkle(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: userPendingSortPath.map{pictureCrop(gift: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: userPendingSortPath.map{pictureCrop(gift: $0)}, encoding: .utf8)!: content, (String(main_depthName.prefix(5))): greetId, String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (constPushMessage.replacingOccurrences(of: "group", with: "xt"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: mainCurrencyName.map{$0^199}, encoding: .utf8)!: content, (String(main_depthName.prefix(5))): greetId, (main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (kGoodStr.replacingOccurrences(of: "presentation", with: "T") + String(user_hoodGreenFormat.prefix(5)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.purchaseFit(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            MirrorSessionDelegate.shared.lock(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.diskTerms(showMsg: String(bytes: notiTickGreetStr.map{$0^137}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        LetterChatManager.aggregationSince(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(appTakeData) + app_senseDragId.lowercased())]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (main_appealPrepareFormat.replacingOccurrences(of: "kit", with: "di")),
                               //: "audioId": greetId,
                               (String(notiWhenProspectUrl.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: data_wholeBlueUrl.map{$0^82}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage)): model.unZipFilePath,
                               //: "senderId": AppManager.share.loginUserMode.userID]
                               String(bytes: main_secretId.map{nearPass(trace: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(notiWhenProspectUrl.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: data_wholeBlueUrl.map{$0^82}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (noti_ignoreTitle.replacingOccurrences(of: "pretty", with: "A") + String(app_harassmentEnoughComplaintUrl))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.purchaseFit(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func situationModel(msgStr: String, quoteModel: ConModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            diskTerms(showMsg: (mainHappyUrl + "send" + String(const_smoothName.prefix(6)) + " mes" + app_pubName.replacingOccurrences(of: "library", with: "ag")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: userPendingSortPath.map{pictureCrop(gift: $0)}, encoding: .utf8)!: temStr, String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (constPushMessage.replacingOccurrences(of: "group", with: "xt"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: mainCurrencyName.map{$0^199}, encoding: .utf8)!: temStr, (main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (kGoodStr.replacingOccurrences(of: "presentation", with: "T") + String(user_hoodGreenFormat.prefix(5)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(kWelcomeId.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(app_increaseName.replacingOccurrences(of: "material", with: "te") + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(showSuiteMessage.replacingOccurrences(of: "particle", with: "t") + "eMsgI" + appPicValue.replacingOccurrences(of: "valid", with: "f"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? ConModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            purchaseFit(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func reflexion(img: UIImage, sendModel: ElectricModel) {
        //: let imagePath = MigrationCellData.getChatImagePath(image: img)
        let imagePath = MigrationCellData.sessionStud(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: user_constraintName.map{errorEqually(clip: $0)}, encoding: .utf8)!: (String(showSmallUrl.suffix(6))), String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (String(dataLifeTingFormat)), String(bytes: const_generateMessage.reversed(), encoding: .utf8)!: imagePath, String(bytes: noti_labName.map{wantBig(po: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(dataRegionAddContent.lowercased()): imagePath,
                                        //: "type": "ImgMsg",
                                        (main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (String(showSmallUrl.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: k_beforeData.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_positKey.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            purchaseFit(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: PressureReplyCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func sunna(msgData: PressureReplyCellData?, sendModel: ElectricModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: user_constraintName.map{errorEqually(clip: $0)}, encoding: .utf8)!: (String(appPortionTitle) + k_loopData.replacingOccurrences(of: "path", with: "g")), String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (kNapMessage.replacingOccurrences(of: "accuracy", with: "o")), String(bytes: noti_labName.map{wantBig(po: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (String(appPortionTitle) + k_loopData.replacingOccurrences(of: "path", with: "g")),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: appDiscountPath.map{inPublic(message: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: noti_conditionMsg.map{narrowMouthed(scope: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: dataConnectName.map{$0^159}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: k_beforeData.reversed(), encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_positKey.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = StillThen.estimated(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.eEr(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.towardSoften(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.stopCamera(cellData, section: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.towardSoften(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.soften(dict, restateCan: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        purchaseFit(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func labelInstall(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (main_appealPrepareFormat.replacingOccurrences(of: "kit", with: "di")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: data_wholeBlueUrl.map{$0^82}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage)): voicePath,
                       //: "senderId": AppManager.share.loginUserMode.userID]
                       String(bytes: main_secretId.map{nearPass(trace: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage)): voicePath,
                         //: "type": "AudioMsg",
                         (main_scoreMessage.replacingOccurrences(of: "screen", with: "e")): (noti_ignoreTitle.replacingOccurrences(of: "pretty", with: "A") + String(app_harassmentEnoughComplaintUrl)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: const_teamName.map{serviceMax(support: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            purchaseFit(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: PressureReplyCellData, reSend: Bool) {
    private func purchaseFit(msgData: PressureReplyCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            diskTerms(showMsg: show_titleOfName)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(app_givenName.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: ButtonReactiveCompatible.self) == false && msgData.isKind(of: FiniteThen.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: ButtonReactiveCompatible.self) == false, msgData.isKind(of: FiniteThen.self) == false, params[(main_scoreMessage.replacingOccurrences(of: "screen", with: "e"))] as? String != (String(const_honeyChamberName.prefix(4)) + "kMsg") {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.eEr(msgData)
        }
        //: let recordTime = SerpentesChatManager.getMessageInsertTime()
        let recordTime = SerpentesChatManager.limitedAdjust()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((dataRegionAddContent.lowercased())) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(dataRegionAddContent.lowercased())] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(dataRegionAddContent.lowercased())] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(main_scoreMessage.replacingOccurrences(of: "screen", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (noti_ignoreTitle.replacingOccurrences(of: "pretty", with: "A") + String(app_harassmentEnoughComplaintUrl)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (constPositionTwoKey.replacingOccurrences(of: "bounce", with: "a") + String(user_balanceMessage)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(constHangMsg.prefix(5)) + "ents" + String(data_sinceReplaceValue.suffix(5)) + String(dataDesignValue))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = k_cameraCellData + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(constWantTitle) + String(k_versionManMsg))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kConversationName + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(notiWhenProspectUrl.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(main_appealPrepareFormat.replacingOccurrences(of: "kit", with: "di"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        HouseThen.pullLike(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(const_honeyChamberName.prefix(4)) + "kMsg") {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.position(msgData, estimatedStatus: nil, view: NSNumber(value: 0), survival: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.camera(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appGreetMsg)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(main_depthName.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    LetterChatManager.aggregationSince(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.methodMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(kWelcomeId.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(constResourceId.suffix(8)) + String(appFinishKey))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(constResourceId.suffix(8)) + String(appFinishKey))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if AfterPushListener.at().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: noti_saveTitle, object: nil, userInfo: [String(bytes: kPressKey.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.response(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(user_memberSkipMessage) + String(user_consequenceMsg.suffix(6)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(const_honeyChamberName.prefix(4)) + "kMsg") {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!] = (String(appJudgeEditionControlFormat))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    noti_nearbyStr.tamperConnect(eventID: main_indexCountervalName)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(const_honeyChamberName.prefix(4)) + "kMsg") {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!] = (constPushMessage.replacingOccurrences(of: "group", with: "xt"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(kWelcomeId.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.eEr(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(AppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(AppManager.share.loginUserMode.userID)" + (String(noti_delayKey) + String(noti_announcementName.prefix(5)) + "ipsTi" + String(showLikeData.prefix(4))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue, type != (String(const_honeyChamberName.prefix(4)) + "kMsg") {
                //: self.removeReplyTipsView()
                self.charm()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.position(updateMsgData, estimatedStatus: msgInfo[(String(dataWhatUrl.prefix(5)))] as? String, view: msgIncome, survival: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            LetterChatManager.nameParty(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((notiProximateMsg.replacingOccurrences(of: "male", with: "t") + String(userShapeId.prefix(6)) + "Coin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(notiProximateMsg.replacingOccurrences(of: "male", with: "t") + String(userShapeId.prefix(6)) + "Coin")] as! NSNumber
                //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(constControlConditionFormat) + String(mainForthStr))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(constControlConditionFormat) + String(mainForthStr))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.columnOwner(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(mainYetMinuteStr) + kCooperativeTitle.lowercased())) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(mainYetMinuteStr) + kCooperativeTitle.lowercased())] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? LaterModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.resignLock(updateMsgData, by: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((kNapMessage.replacingOccurrences(of: "accuracy", with: "o"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(kNapMessage.replacingOccurrences(of: "accuracy", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? SchemeExcludeModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.circumferent(updateMsgData, brightRemote: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        beyondMargin()

        //: updateSession()
        process()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func process() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        FiniteInfoManager.greet(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func camera(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard AppManager.share.loginUserMode.status != 1 else {
            guard AppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    diskTerms(showMsg: errorStr)
                }
                //: return
                return
            }

            //: ElementEarnReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            ElementEarnReactiveCompatible.share.recharge(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.oppositeness()
            //: AppManager.share.loginUserMode.freeMsgTimes = ""
            AppManager.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.jumpDown()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RateGuidanceThen(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey))
            //: view.show()
            view.pendingMic()
            //: } else if errorCode == 60003, !AppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !AppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            PopupWindowManager.shared.verification()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                diskTerms(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func multiLoad(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(kWelcomeId.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.response(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func aboardInner() {
        //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.usufructuaryOpenViewModifyNameTheoryPrivate(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(AppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(AppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (data_cameraFormalValue.array(forKey: k_dismissMsg)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.animationHideWant()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                data_cameraFormalValue.set(intimateCacheData, forKey: k_dismissMsg)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func yesterdayDiscount(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = SerpentesChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = SerpentesChatManager.nodCustomAccountingData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(noti_handleUrl))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(kWelcomeId.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(appSuccessfulTitle.suffix(8)) + showTapRemindFormat.replacingOccurrences(of: "assistant", with: "t"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.locationBy(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func response(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(appSuccessfulTitle.suffix(8)) + showTapRemindFormat.replacingOccurrences(of: "assistant", with: "t"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(appSuccessfulTitle.suffix(8)) + showTapRemindFormat.replacingOccurrences(of: "assistant", with: "t"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.locationBy(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func locationBy(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = FiniteInfoManager.doingeAgain(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= AppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.usufructuaryOpenViewModifyNameTheoryPrivate(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.scheduleTapTable(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.closeDescription(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.closeDescription(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        FiniteInfoManager.ghb(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: notiAnyoneStr.map{tailPick(urge: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: mainPrimaryTitle.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kFileDepthData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ChatViewDelegate: ViewThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func totalerval() {
        //: guard AppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.alongLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func planSituation(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func oppositionBegin(actionType: HarassmentRelatableTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    funcBy()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    pullInMonth()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                funcBy()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if AppManager.share.loginUserMode.loungePlus {
                if AppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.eachReply(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                pullInMonth()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    input()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    pullInMonth()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                input()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.official(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func pullInMonth() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.diskTerms(showMsg: String(bytes: const_cancerName.map{layRemark(before: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func movePer(giftModel _: PrepareDisplayTransformable, giftNum _: String, model _: PossibilityUponReactiveCompatible) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func mTheory(giftModel: PrepareDisplayTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        dryUp(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: FiniteThen?, fromStr: String?) {
    func dryUp(giftModel: PrepareDisplayTransformable, giftNum: String, cellData: FiniteThen?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            diskTerms(showMsg: show_titleOfName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(app_givenName.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(k_positKey.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(mainNegativePath))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(show_nightValue.prefix(6)) + "msetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(dataMediaValue.lowercased())] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        HouseThen.crushCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.camera(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: data_meetingUrl)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.crepe(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.toteUp(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.process()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(kWelcomeId.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(appSuccessfulTitle.suffix(8)) + showTapRemindFormat.replacingOccurrences(of: "assistant", with: "t"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(appSuccessfulTitle.suffix(8)) + showTapRemindFormat.replacingOccurrences(of: "assistant", with: "t"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.locationBy(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.last(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                noti_nearbyStr.tamperConnect(eventID: userDeviceData)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func limit(giftModel: PrepareDisplayTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        noti_nearbyStr.tamperConnect(eventID: kConversationTimeRestoreStr)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            diskTerms(showMsg: show_titleOfName)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= AppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            pullInMonth()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(data_crossValue.replacingOccurrences(of: "it", with: "an") + "Gift" + String(main_existKey.prefix(2)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(main_scoreMessage.replacingOccurrences(of: "screen", with: "e"))] = (String(main_electActualPath.prefix(5)) + String(show_noteUrl.suffix(6)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (String(appJudgeEditionControlFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(kWelcomeId.prefix(7))): msgInfo, String(bytes: k_refugeUrl.map{throughYear(clearly: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = StillThen.estimated(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            purchaseFit(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func price(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(show_contentAppStr - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.without(true)
            self.msgView.without(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func slide(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.without(true)
            self.msgView.without(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func lateRecognizer(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.lateRecognizer(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                futurism(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func dramaticWork() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if RanchHousePlayManager.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            RanchHousePlayManager.shared.tempSegment()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func toteUp(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(kWelcomeId.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!] = (noti_femaleFutureOrangeKey.replacingOccurrences(of: "her", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kWelcomeId.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = StillThen.estimated(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.eEr(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(constResourceId.suffix(8)) + String(appFinishKey))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(constResourceId.suffix(8)) + String(appFinishKey))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.position(cellData, estimatedStatus: msgInfoDic[(String(dataWhatUrl.prefix(5)))] as? String, view: msgIncome, survival: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        alliterator(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((notiProximateMsg.replacingOccurrences(of: "male", with: "t") + String(userShapeId.prefix(6)) + "Coin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(notiProximateMsg.replacingOccurrences(of: "male", with: "t") + String(userShapeId.prefix(6)) + "Coin")] as! NSNumber
            //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.fact(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func quantityo() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.weeklyCommon(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func alliterator(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.portionMesohippus(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func advertisingTo(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.buttonRunning(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func limpidity() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func query() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.pickSeekMin { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.process()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func dismissMoment() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.aboutComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.process()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func feedbackWithinSpeak() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = SubsequentlyFieldDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pushMove()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func announcementFlowClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = OppositePopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.divideList(cellTitleList: [(String(show_quickNodCognitiveUrl.prefix(4)) + String(app_missingTransitionTitle.prefix(5))).localized, (String(kCommentData.prefix(5))).localized, (String(main_rejectUrl.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .aircraft()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: ElementEarnReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                ElementEarnReactiveCompatible.share.datePress(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                spreadView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = ExtraView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.showThroughView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func spreadView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = CreaseView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.everyAccess(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.observe(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func hairClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            futurism(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            pushMove()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func runningRecover(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = RealmTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.strength(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func renderCos() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(record(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: k_nearbyData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(logConstraint),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: dataDismissName,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func record(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard AppManager.share.loginUserMode.loungePlus == true else { return }
        guard AppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func logConstraint() {
        //: AppManager.share.loginUserMode.loungePlus = true
        AppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func mainEntryWord() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? PressureReplyCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? PressureReplyCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if LetterChatManager.belowTarget(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.tipLocal(tipStr: (String(showCalendarName)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ChatViewDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func funcBy() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        EditThen.visitorBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MiddlePickTool.nevertheless(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.screenMemoryHalf(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func domainBy() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        EditThen.grossBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.screenMemoryHalf(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func screenMemoryHalf(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: AppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ElectricModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.reflexion(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = StatusPhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ElectricModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.reflexion(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pushMove()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func input() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard LetterChatManager.way(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == SharedMirrorKingCustomReflectable.male.rawValue ? "He".localized : (String(show_networkStr.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.diskTerms(showMsg: (String(k_heartGapPath.prefix(5)) + " usin" + String(noti_labData.prefix(5)) + " low" + String(user_clingLaterKey.suffix(4)) + mainProductionTitle.replacingOccurrences(of: "bone", with: "o")).queryionLocalized(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.minPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func minPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = MiddlePickTool.nevertheless(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            MiddlePickTool.incite(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.gapVideo(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.diskTerms(showMsg: (String(data_launchPath) + String(data_negativeTitle.suffix(4)) + "east " + String(k_emptyTitle.prefix(9))).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func gapVideo(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: SkilledWorkerEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = ButtonTvThen(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ElectricModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.sunna(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        UniversalProgressHUD.coverBetween()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        pushMove()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: PressureReplyCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func towardSoften(msgData: PressureReplyCellData, sendModel: ElectricModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ListCreateListener.shared.oppositeDownCompletion(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.diskTerms(showMsg: (String(user_onlyId.suffix(4)) + "ad faile" + main_insideLengthThatStr.replacingOccurrences(of: "round", with: "d")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.position(msgData, estimatedStatus: nil, view: NSNumber(value: 0), survival: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.sunna(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    //: func func__addVoiceNotification() {
    func character() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(elicit),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(reEnter),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(exceptMp(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(stateCharacter(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: user_lessName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(yesterdayDiscount(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kUserUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func upwardly(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            RanchHousePlayManager.shared.recall()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            RanchHousePlayManager.shared.minRemove()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func elicit() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        upwardly(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func reEnter() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        upwardly(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func exceptMp(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            upwardly(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            upwardly(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: MeditationThen, cellView: TalkingChatAudioMsgCell) {
    func compartment(cellData: MeditationThen, cellView: OtherMessageCell) {
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.diskTerms(showMsg: notiLocalText)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? PressureReplyCellData)!.isKind(of: MeditationThen.self) == false {
            if (indexModel as? PressureReplyCellData)!.isKind(of: MeditationThen.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! MeditationThen
            let audioModel = indexModel as! MeditationThen
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = FiniteInfoManager.regain(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        RanchHousePlayManager.shared.tempSegment()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if RanchHousePlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(main_sumUrl)) + "\(RanchHousePlayManager.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if RanchHousePlayManager.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                RanchHousePlayManager.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        RanchHousePlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        RanchHousePlayManager.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: PressureReplyCellData = self.msgView.uiMsgs.object(at: index) as! PressureReplyCellData
                let subModel: PressureReplyCellData = self.msgView.uiMsgs.object(at: index) as! PressureReplyCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: MeditationThen.self) {
                if subModel.isKind(of: MeditationThen.self) {
                    //: let audioModel = subModel as! MeditationThen
                    let audioModel = subModel as! MeditationThen
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = FiniteInfoManager.regain(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FactoryThen.goOfMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        RanchHousePlayManager.shared.contend(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func stop() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.minimizeShade()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.crinkle(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? PressureReplyCellData
                let indexModel = model as? PressureReplyCellData
                //: if indexModel?.isKind(of: MeditationThen.self) == true || indexModel?.isKind(of: MigrationCellData.self) == true || indexModel?.isKind(of: MirrorCellData.self) == true || indexModel?.isKind(of: ButtonReactiveCompatible.self) == true {
                if indexModel?.isKind(of: MeditationThen.self) == true || indexModel?.isKind(of: MigrationCellData.self) == true || indexModel?.isKind(of: MirrorCellData.self) == true || indexModel?.isKind(of: ButtonReactiveCompatible.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.beyondMargin()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.cannulise()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.mainEntryWord()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func minimizeShade() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.scrubFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.outPartner(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.aboardInner()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == AppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.observe(userWrap)
    }

    //: func func__getFillDraftText() {
    func isExamine() {
        //: let userID = "c2c_"+targetId
        let userID = (String(show_yardMessage.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.totalerval()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func oldUpon() {
        //: let userID = "c2c_"+targetId
        let userID = (String(show_yardMessage.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    //: func func__TipsNotification() {
    func remote() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(outputNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: app_routeUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func outputNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SimultaneouslyThen = userinfo[(mainTitleCircleStr.replacingOccurrences(of: "writing", with: "m"))] as! SimultaneouslyThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.without(true)
        self.msgView.without(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func tipLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: userPendingSortPath.map{pictureCrop(gift: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: user_pairFormat.map{baseballClub(spring: $0)}, encoding: .utf8)!: (String(main_assistSparkPath))]
        //: var message: PressureReplyCellData?
        var message: PressureReplyCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = StillThen.sewageDisposalPlantToConclude(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = StillThen.estimated(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.without(true)
        self.msgView.without(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func cannulise() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        LetterChatManager.too { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.tipLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func columnOwner(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.tipLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func beyondMargin() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        LetterChatManager.tool(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.tipLocal(tipStr: String(bytes: user_additionalMsg.map{historyVisitor(encounter: $0)}, encoding: .utf8)!.queryionLocalized(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func stateCharacter(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: PressureReplyCellData = dict?["cellData"] as! PressureReplyCellData
        let cellData: PressureReplyCellData = dict?[(String(userLinkStr) + k_publiclyValue.replacingOccurrences(of: "govern", with: "a"))] as! PressureReplyCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.without(true)
        self.msgView.without(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ChatViewDelegate: ResolutionThen {
    //: func func__setReplyTipsView() {
    func outTotalerest() {
        //: let key =  "\(AppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(AppManager.share.loginUserMode.userID)" + (String(noti_delayKey) + String(noti_announcementName.prefix(5)) + "ipsTi" + String(showLikeData.prefix(4))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.allEye()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            bequest(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            charm()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func disappear(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        situationModel(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        charm()
    }

    //: func removeReplyTipsView() {
    func charm() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            bequest(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func centerAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: showOutputKey, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: userSystemKey - show_contentAppStr, width: show_explainId, height: userSystemKey)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: show_explainId,
                                     //: height: ScreenHeight)
                                     height: userSystemKey)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func futurism(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.reasonWindow() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: WhiteObjectProtocol.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: userSystemKey - show_contentAppStr,
                                     //: width: ScreenWidth,
                                     width: show_explainId,
                                     //: height: ScreenHeight)
                                     height: userSystemKey)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ChatViewDelegate {
    /// 创建UI
    //: private func createUI() {
    private func chemicalAgent() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(const_exShallName) + showSizeMsg.replacingOccurrences(of: "ticket", with: "io")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func reject() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? show_contentAppStr : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.glide(width: contentView.width,
                              //: height: contentView.height,
                              height: contentView.height,
                              //: corners: [.topLeft, .topRight],
                              corners: [.topLeft, .topRight],
                              //: cornerRadii: .init(width: 8, height: 8))
                              cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_routeValue : main_hugeFormat)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_videoMsg)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? k_routeValue : main_hugeFormat)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_videoMsg)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + user_videoMsg)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func remainPicture() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.hairClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.announcementFlowClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.oppositionBegin(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                noti_nearbyStr.tamperConnect(eventID: kMomentMsg, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
