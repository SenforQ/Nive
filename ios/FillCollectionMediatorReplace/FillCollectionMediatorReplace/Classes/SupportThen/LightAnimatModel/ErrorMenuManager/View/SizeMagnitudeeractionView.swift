
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let main_writeKey:[UInt8] = [0xd5,0xd2,0xd5,0xc8,0x94,0xdf,0xd3,0xd8,0xd9,0xce,0x86,0x95,0x9c,0xd4,0xdd,0xcf,0x9c,0xd2,0xd3,0xc8,0x9c,0xde,0xd9,0xd9,0xd2,0x9c,0xd5,0xd1,0xcc,0xd0,0xd9,0xd1,0xd9,0xd2,0xc8,0xd9,0xd8]

/*: "icon_home_position" :*/
fileprivate let kWorldData:String = "icovid"
fileprivate let k_pressPath:String = "_positinet slow bear cover resistance"
fileprivate let noti_appealPairFitStr:[Character] = ["o","n"]

/*: "btn_video_minimize" :*/
fileprivate let appWayData:[Character] = ["b","t","n","_","v","i","d","e","o"]
fileprivate let app_tieText:String = "conclude everyone satisfy_mini"

/*: "btn_video_drop_nor" :*/
fileprivate let constAgentTotalerestedText:String = "text match buttonbtn_v"
fileprivate let show_prepareName:String = "_drop_ncheek job pattern active"
fileprivate let dataGenerateValue:String = "trigger"

/*: "btn_video_call_nor" :*/
fileprivate let app_listMarkUrl:String = "compose liver banbtn_v"
fileprivate let constCommunicationFormat:String = "weekly next_cal"

/*: "transform.scale" :*/
fileprivate let noti_addressSuccessName:[Character] = ["t","r","a","n","s","f","o","r","m",".","s","c","a","l","e"]

/*: "animation" :*/
fileprivate let notiCountTitle:String = "nose"
fileprivate let main_exposureMessage:[Character] = ["n","i","m","a","t","i","o","n"]

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let appRollingSolarTitle:[UInt8] = [0x47,0x4d,0x41,0x40,0x71,0x58,0x47,0x4a,0x4b,0x41,0x4d,0x4f,0x42,0x42,0x71,0x48,0x4b,0x43,0x4f,0x42,0x4b,0x71,0x4f,0x40,0x5d,0x59,0x4b,0x5c,0x71,0x4c,0x49]

private func dosageIt(activity num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "icon_videocall_female_answer" :*/
fileprivate let kMastPaperPath:String = "iegon"
fileprivate let dataHangUrl:String = "share becomecall_f"
fileprivate let main_figureValue:String = "westarting"

/*: "icon_calltalk_points" :*/
fileprivate let constDateMicTitle:String = "icon_cost assert sure remove film"
fileprivate let showAgencyFormat:String = "talk_minimize pair draft"

/*: "get json error" :*/
fileprivate let dataImageValue:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r","o"]
fileprivate let app_spanMessage:[Character] = ["r"]

/*: "btn_video_turn_nor" :*/
fileprivate let notiLightRePath:String = "bsoul"
fileprivate let main_essentialUnableValue:String = "enough negative voice littlen_vide"
fileprivate let user_toleranceId:String = "pressure framework signaturern_nor"

/*: "btn_video_turn_pre" :*/
fileprivate let constLiteralText:[Character] = ["b","t","n","_","v","i","d","e","o","_","t"]
fileprivate let show_statusMessage:[Character] = ["u","r","n","_","p","r","e"]

/*: "btn_video_start_nor" :*/
fileprivate let show_withoutValue:[Character] = ["b","t","n","_","v","i","d","e"]
fileprivate let constDismissUrl:String = "your origino_st"
fileprivate let showMatchValue:String = "art_norprovider together"

/*: "btn_video_start_pre" :*/
fileprivate let show_responseUrl:String = "disk screen clingbtn_"
fileprivate let data_flowValue:String = "_starbrake aircraft"
fileprivate let noti_soapName:[Character] = ["e"]

/*: "icon_complaint" :*/
fileprivate let dataGreenPath:String = "readercon"
fileprivate let mainItsTitle:String = "ainbuilder"

/*: "showBullet" :*/
fileprivate let k_rateMoveKey:[Character] = ["s","h","o","w","B","u","l","l","e","t"]

/*: "icon_videoCall_coin_nor" :*/
fileprivate let app_dotUrl:String = "op editor equityicon_"
fileprivate let const_areaLocalMsg:String = "oCall_far fore cooperative statement"
fileprivate let mainCountFormat:[Character] = ["_","n","o","r"]

/*: "btn_video_gift_nor" :*/
fileprivate let app_dragFormat:String = "btn_videoextend will yes"
fileprivate let notiWeightId:String = "_giminute do gross number"
fileprivate let dataTailAddedPath:String = "accounting pressure once adopt wordft_nor"

/*: "Say something...     " :*/
fileprivate let main_globalTitle:String = "Say sanyone matter average general"
fileprivate let showMmMsg:String = "read wideing... "
fileprivate let const_proStr:String = "    "

/*: "waiting for the other party to accept the video invitation..." :*/
fileprivate let data_observerFormat:[UInt8] = [0x70,0x66,0x6e,0x73,0x6e,0x69,0x60,0x27,0x61,0x68,0x75,0x27,0x73,0x6f,0x62,0x27,0x68,0x73,0x6f,0x62,0x75,0x27,0x77,0x66,0x75,0x73,0x7e,0x27,0x73,0x68,0x27,0x66,0x64,0x64,0x62,0x77,0x73,0x27,0x73,0x6f,0x62,0x27,0x71,0x6e,0x63,0x62,0x68,0x27,0x6e,0x69,0x71,0x6e,0x73,0x66,0x73,0x6e,0x68,0x69,0x29,0x29,0x29]

private func guardDisplay(single num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "Invite you to a video call..." :*/
fileprivate let app_disappearName:[Character] = ["I","n","v","i","t","e"," ","y","o","u"," ","t"]
fileprivate let data_thePath:[Character] = ["o"," ","a"," ","v","i","d","e","o"," ","c","a","l","l",".",".","."]

/*: "yyyy-MM-dd" :*/
fileprivate let notiTensionName:[Character] = ["y","y","y","y","-"]
fileprivate let userCeremonyId:String = "center pureMM-dd"

/*: "00:00" :*/
fileprivate let dataDynamicsMessage:[Character] = ["0","0",":","0","0"]

/*: "%@ points/min" :*/
fileprivate let showAgainstUnknownId:[Character] = ["%","@"," ","p","o","i","n","t","s","/","m","i","n"]

/*: "Camera must be on" :*/
fileprivate let app_accessMessage:String = "Camercriminal unite private"
fileprivate let data_oxygenFarUrl:String = "st be ostock too"
fileprivate let k_remainKey:String = "interrupt"

/*: "msgId" :*/
fileprivate let noti_prettyStr:[UInt8] = [0xe8,0xf6,0xe2,0xcc,0xe1]

/*: "senderUid" :*/
fileprivate let k_sharedName:[UInt8] = [0x7,0x11,0x1a,0x10,0x11,0x6,0x21,0x1d,0x10]

private func policyCommit(design num: UInt8) -> UInt8 {
    return num ^ 116
}

/*: "toUid" :*/
fileprivate let k_stockLimitedName:String = "cancel invitetoUid"

/*: "nickname" :*/
fileprivate let constPrincipalStr:[UInt8] = [0x1e,0x19,0x13,0x1b,0x1e,0x11,0x1d,0x15]

fileprivate func soapStarting(second num: UInt8) -> UInt8 {
    let value = Int(num) - 176
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let kDestroyKey:[UInt8] = [0x13,0x1f,0x1e,0x4,0x15,0x1e,0x4]

/*: "isMySender" :*/
fileprivate let app_ownerHimPath:[UInt8] = [0x4,0x1e,0x20,0x14,0x3e,0x8,0x3,0x9,0x8,0x1f]

private func stallLanguagePeer(sibling num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "hasFace" :*/
fileprivate let userRecognizeKey:[Character] = ["h","a","s","F","a"]
fileprivate let main_secretKey:[Character] = ["c","e"]

/*: "Automatically answer for you in %@s" :*/
fileprivate let appExecuteTitle:[UInt8] = [0x73,0x40,0x25,0x20,0x6e,0x69,0x20,0x75,0x6f,0x79,0x20,0x72,0x6f,0x66,0x20,0x72,0x65,0x77,0x73,0x6e,0x61,0x20,0x79,0x6c,0x6c,0x61,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41]

/*: "giftId" :*/
fileprivate let constMarginExplainTitle:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let show_stillData:String = "vertical theme request in exgiftNu"
fileprivate let main_phaseName:[Character] = ["m"]

/*: "from" :*/
fileprivate let k_teamBubblePath:[Character] = ["f","r","o","m"]

/*: "videoCall" :*/
fileprivate let app_middleData:String = "videcurrent"

/*: "programSort" :*/
fileprivate let noti_valueText:String = "pmedal"
fileprivate let kUniformKey:String = "ogramSorthere habit i"

/*: "pkgItemsetId" :*/
fileprivate let user_busyMessage:String = "sphere communicatepkgIt"
fileprivate let data_narrowName:String = "tIdanswer launch earn op"

/*: "msgInfo" :*/
fileprivate let dataDraftMessage:String = "laboratory solarmsgInfo"

/*: "msgType" :*/
fileprivate let userNorValue:String = "bill regain itemmsgType"

/*: "gift" :*/
fileprivate let appZzValue:String = "gisession"

/*: "totalMfCoin" :*/
fileprivate let const_honeyId:[Character] = ["t","o","t","a","l"]
fileprivate let mainGalleryKey:String = "do spec notice opening spanMfCoin"

/*: "cellData" :*/
fileprivate let constHomeMysteryValue:[UInt8] = [0x88,0x8a,0x91,0x91,0x69,0x86,0x99,0x86]

fileprivate func helloMine(gender num: UInt8) -> UInt8 {
    let value = Int(num) + 219
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "icon_close_nor" :*/
fileprivate let mainContendPath:[Character] = ["i","c","o","n","_","c","l","o"]
fileprivate let notiTaTracePatternTitle:String = "se_norwhat one sure tender tab"

/*: "FaceActionEvent_Interval_12_frame" :*/
fileprivate let notiBoneTitle:[UInt8] = [0x7f,0x58,0x5a,0x5c,0x78,0x5a,0x4d,0x50,0x56,0x57,0x7c,0x4f,0x5c,0x57,0x4d,0x66,0x70,0x57,0x4d,0x5c,0x4b,0x4f,0x58,0x55,0x66,0x8,0xb,0x66,0x5f,0x4b,0x58,0x54,0x5c]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SizeMagnitudeeractionView.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VideoInteractionViewDelegate: NSObjectProtocol {
protocol AnotherViewDelegate: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func destinationDown()

    /// 接听视频通话
    //: func interactionView_shouldAcceptCall()
    func resourceTag()

    /// 旋转摄像头
    /// - Parameter isFront: 是否前置（默认前置）
    //: func interactionView_shouldChangeCamera(isFront: Bool)
    func faceTotal(isFront: Bool)

    /// 禁用/开启摄像头
    /// - Parameter forbidden: 是否禁用摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool)
    func hold(forbidden: Bool)

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func beyondRid()

    /// 投诉成功
    //: func interactionView_reportSucceed()
    func occurSession()
}

//: class Talking1v1VideoInteractionView: UIView {
class SizeMagnitudeeractionView: UIView {
    //: let place = (ScreenWidth-actualWidth(w: 45)*5)/6
    let place = (show_explainId - actualWidth(w: 45) * 5) / 6
    //: weak var delegate: Talking1v1VideoInteractionViewDelegate?
    weak var delegate: AnotherViewDelegate?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = SimulationChatModel() // 引用类型，整个视频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器
    //: private var freeCountTimer: Timer?
    private var freeCountTimer: Timer? // 免费通话剩余时间定时器
//    var isShowRecharge  = false                         // 是否展示充值页中
    //: var isShowInput  = false
    var isShowInput = false // 是否在聊天中
    //: private var autoAcceptTimer: DispatchSourceTimer?
    private var autoAcceptTimer: DispatchSourceTimer? // 自动接听定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: SimulationChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        resign()
        //: setupSubViewsConstraint()
        put()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        showBold()
        //: bindInteraction()
        stickToWithReciprocationSumeraction()
        //: initNotificationObserver()
        successfullyWindow()
        //: initCachEffectData()
        cach()
        //: isShowHangupBtn()
        theStates()

        //: TalkingVideoDanmuManager.shared().delegate = self
        LightDanmuManager.itemSession().delegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: main_writeKey.map{$0^188}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
        //: if freeCountTimer != nil {
        if freeCountTimer != nil {
            //: freeCountTimer?.invalidate()
            freeCountTimer?.invalidate()
            //: freeCountTimer = nil
            freeCountTimer = nil
        }
        //: cancelAutoAcceptTimer()
        usedCar()
        //: player.stopPlay()
        player.begin()
        //: invalidateIdleTimer()
        deviceTimer()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(touchHidenBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(soyaBean), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像背景
    //: private lazy var pairInfoBg: UIView = {
    private lazy var pairInfoBg: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        //: v.layer.cornerRadius = actualWidth(w: 50)/2
        v.layer.cornerRadius = actualWidth(w: 50) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: ExtendedView = {
        //: let btn = TalkingButton()
        let btn = ExtendedView()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.scheme(name: (kWorldData.replacingOccurrences(of: "vid", with: "n") + "_home" + String(k_pressPath.prefix(7)) + String(noti_appealPairFitStr))), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.balanceSize(fontSize: 12)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = CombinedLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 40)/2
        v.layer.cornerRadius = actualWidth(w: 40) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 17)
        lab.font = UIFont.pingfangBusy(fontSize: 17)
        //: return lab
        return lab
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.balanceSize(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(appWayData) + String(app_tieText.suffix(5)) + "mize")), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(situationPhone), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(constAgentTotalerestedText.suffix(5)) + "ideo" + String(show_prepareName.prefix(7)) + dataGenerateValue.replacingOccurrences(of: "trigger", with: "or"))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(libraryClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(app_listMarkUrl.suffix(5)) + "ideo" + String(constCommunicationFormat.suffix(4)) + "l_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(recognize), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(noti_addressSuccessName))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (notiCountTitle.replacingOccurrences(of: "nose", with: "a") + String(main_exposureMessage)))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.scheme(name: String(bytes: appRollingSolarTitle.map{dosageIt(activity: $0)}, encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(noti_addressSuccessName))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (notiCountTitle.replacingOccurrences(of: "nose", with: "a") + String(main_exposureMessage)))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.scheme(name: (kMastPaperPath.replacingOccurrences(of: "ego", with: "co") + "_video" + String(dataHangUrl.suffix(6)) + "emale_ans" + main_figureValue.replacingOccurrences(of: "starting", with: "r"))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(recognize), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: TalkingButton = {
    private lazy var femaleAcceptTipsBtn: ExtendedView = {
        //: let btn = TalkingButton()
        let btn = ExtendedView()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(constDateMicTitle.prefix(5)) + "call" + String(showAgencyFormat.prefix(5)) + "points")), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.balanceSize(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = FinishLetterThen.default.detailWork(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataImageValue) + String(app_spanMessage)))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if CombinedLanguageManager.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 旋转摄像头按钮
    //: private lazy var rotateCameraBtn: UIButton = {
    private lazy var rotateCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (notiLightRePath.replacingOccurrences(of: "soul", with: "t") + String(main_essentialUnableValue.suffix(6)) + "o_tu" + String(user_toleranceId.suffix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_pre"), for: .selected)
        btn.setImage(UIImage.scheme(name: (String(constLiteralText) + String(show_statusMessage))), for: .selected)
        //: btn.addTarget(self, action: #selector(rotateCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(keepSlide), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭摄像头按钮
    //: private lazy var openCameraBtn: UIButton = {
    private lazy var openCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(show_withoutValue) + String(constDismissUrl.suffix(4)) + String(showMatchValue.prefix(7)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_pre"), for: .selected)
        btn.setImage(UIImage.scheme(name: (String(show_responseUrl.suffix(4)) + "video" + String(data_flowValue.prefix(5)) + "t_pr" + String(noti_soapName))), for: .selected)
        //: btn.addTarget(self, action: #selector(openCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(success), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 投诉按钮（男性）
    //: private lazy var reportBtn: UIButton = {
    private lazy var reportBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_complaint"), for: .normal)
        btn.setImage(UIImage.scheme(name: (dataGreenPath.replacingOccurrences(of: "reader", with: "i") + "_compl" + mainItsTitle.replacingOccurrences(of: "builder", with: "t"))), for: .normal)
        //: btn.addTarget(self, action: #selector(reportButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mutualSmall), for: .touchUpInside)
        //: let data = JSON(AppManager.share.appConfigMode.videoReport)
        let data = JSON(AppManager.share.appConfigMode.videoReport)
        //: if data["showBullet"].boolValue == true {
        if data[(String(k_rateMoveKey))].boolValue == true {
            //: btn.isEnabled = true
            btn.isEnabled = true
            //: } else {
        } else {
            //: btn.isEnabled = false
            btn.isEnabled = false
        }
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(app_dotUrl.suffix(5)) + "vide" + String(const_areaLocalMsg.prefix(6)) + "coin" + String(mainCountFormat))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .selected)
        btn.setImage(UIImage.scheme(name: (String(app_dotUrl.suffix(5)) + "vide" + String(const_areaLocalMsg.prefix(6)) + "coin" + String(mainCountFormat))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(reduceClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.scheme(name: (String(app_dragFormat.prefix(9)) + String(notiWeightId.prefix(3)) + String(dataTailAddedPath.suffix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.scheme(name: (String(app_dragFormat.prefix(9)) + String(notiWeightId.prefix(3)) + String(dataTailAddedPath.suffix(6)))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(nonnegative), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.pingfangBusy(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var freeTimeView: TalkingFreeCallRechargeTipsView = {
    private lazy var freeTimeView: RenameThen = {
        //: let view = TalkingFreeCallRechargeTipsView()
        let view = RenameThen()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: view.touchRechargeBtnBlock = { [weak self] in
        view.touchRechargeBtnBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
//            self.isShowRecharge = true
        }
        //: return view
        return view
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: GrossRevenueDataSource = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = GrossRevenueDataSource(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ResolutionReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = ResolutionReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: ProvideView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = ProvideView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: StraddleAnimationView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = StraddleAnimationView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager = {
    private lazy var player: SalesReactiveCompatible = {
        //: let player = TalkingVideoPlayerManager()
        let player = SalesReactiveCompatible()
        //: player.setMute(bEnable: true)
        player.skipSuite(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    // 视频封面
    //: private lazy var coverImg: UIImageView = {
    private lazy var coverImg: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    private let title: String = (String(main_globalTitle.prefix(5)) + "ometh" + String(showMmMsg.suffix(7)) + const_proStr.capitalized).localized
    /// 1.7.2 聊天消息
    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.backgroundColor = UIColor.init(white: 0, alpha: 0.25)
        btn.backgroundColor = UIColor(white: 0, alpha: 0.25)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.setTitle("   "+"Say something...     ".localized, for: .normal)
        btn.setTitle("   " + title, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 14)
        btn.titleLabel?.font = UIFont.balanceSize(fontSize: 14)
        //: btn.setTitleColor(UIColor.init(white: 1, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(white: 1, alpha: 0.8), for: .normal)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = CombinedLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(minute), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var danmuView: TalkingVideoDanmuMsgListTableView = {
    private lazy var danmuView: ExistDataSource = {
        //: let view = TalkingVideoDanmuMsgListTableView.init()
        let view = ExistDataSource()
        //: view.touchTableGesblock = { [weak self] in
        view.touchTableGesblock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.touchHidenBtnClick()
            self.soyaBean()
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commInputView: TalkingVideoCallInputView = {
    private lazy var commInputView: BarMigrationThen = {
        //: let text = TalkingVideoCallInputView.init()
        let text = BarMigrationThen()
        //: text.delegate = self
        text.delegate = self
        //: text.isHidden = true
        text.isHidden = true
        //: return text
        return text
        //: }()
    }()

    // 自动接听文案描述（女性）
    //: private lazy var autoAcceptDesLab: UILabel = {
    private lazy var autoAcceptDesLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.balanceSize(fontSize: 17)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func showBold() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "waiting for the other party to accept the video invitation...".localized
            desLab.text = String(bytes: data_observerFormat.map{guardDisplay(single: $0)}, encoding: .utf8)!.localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
        //: case .Recieved:
        case .Recieved:
            //: desLab.text = "Invite you to a video call...".localized
            desLab.text = (String(app_disappearName) + String(data_thePath)).localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = data_cameraFormalValue.object(forKey: show_dailyId)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.threeSince(date: Date(), dateFormat: (String(notiTensionName) + String(userCeremonyId.suffix(5))))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    data_cameraFormalValue.set(today, forKey: show_dailyId)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
                //: autoAcceptDesLab.isHidden = false
                autoAcceptDesLab.isHidden = false
                // 直播时自动接听
                //: if AppManager.share.appConfigMode.autoAnswer == 2 &&
                if AppManager.share.appConfigMode.autoAnswer == 2 &&
                    //: NEED_RESTORE_LIVE == true {
                    showSelectCornerWithoutMessage == true
                {
                    //: showOrHidenAutoRecieved(autoAccept: true)
                    theAccept(autoAccept: true)
                    //: } else {
                } else {
                    //: showOrHidenAutoRecieved()
                    theAccept()
                }
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: pairInfoBg.isHidden = true
            pairInfoBg.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: rotateCameraBtn.isHidden = false
            rotateCameraBtn.isHidden = false
            //: openCameraBtn.isHidden = false
            openCameraBtn.isHidden = false
            //: reportBtn.isHidden = !reportBtn.isEnabled || AppManager.share.loginUserMode.sex == Gender.female.rawValue
            reportBtn.isHidden = !reportBtn.isEnabled || AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: chatBtn.isHidden = false
            chatBtn.isHidden = false
            //: danmuView.isHidden = false
            danmuView.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                album()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(dataDynamicsMessage))
            }
            //: startTalkCount()
            hangUp()
            //: touchHiddenTimer()
            wisecrack()
            //: if TalkingSocketManager.shared.isFreeCall {
            if MigrationSocketDelegate.shared.isFreeCall {
                //: setFreeView()
                progressSet()
                //: setFreeCountTimer()
                boardTimer()
            }
            //: if reportBtn.isHidden {
            if reportBtn.isHidden {
                //: sendGiftBtn.snp.remakeConstraints { make in
                sendGiftBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview().offset(-10)
                    make.trailing.equalToSuperview().offset(-10)
                    //: make.bottom.equalTo(reportBtn)
                    make.bottom.equalTo(reportBtn)
                    //: make.size.equalTo(reportBtn)
                    make.size.equalTo(reportBtn)
                }
            }
        }

        //: updateLayout()
        reloadStack()
        //: afterGetInfo()
        menuOf()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func menuOf() {
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(showAgainstUnknownId)).queryionLocalized(chatModel.points), for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.commonClose(urlStr: chatModel.pairHeadPic, options: optionsInfo)

        // 男性展示视频封面 【非通话中】
        //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            coverImg.scrubFinish(urlStr: chatModel.pairHeadPic)
            //: if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
            if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
                //: player.delegate = self
                player.delegate = self
                //: player.playerWithUrlAndVideoView(url: chatModel.videoUrl, view: self)
                player.occur(url: chatModel.videoUrl, view: self)
            }
        }
        //: if chatModel.videoStage == .Calling {
        if chatModel.videoStage == .Calling {
            //: coverImg.isHidden = true
            coverImg.isHidden = true
            //: player.stopPlay()
            player.begin()
            //: player.removeVideoWidget()
            player.eraseFemaleWidget()
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func situationPhone() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.beyondRid()
    }

    /// 添加视频通话画面窗口
    /// - Parameter view: 视频画面窗口
    //: func addWindowView(view: UIView) {
    func todayBy(view: UIView) {
        //: insertSubview(view, belowSubview: pairPicV)
        insertSubview(view, belowSubview: pairPicV)
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func libraryClick() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.destinationDown()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func recognize() {
        //: self.cancelAutoAcceptTimer()
        self.usedCar()
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.resourceTag()
    }

    /// 旋转摄像头事件
    //: @objc private func rotateCameraButtonClick() {
    @objc private func keepSlide() {
        //: if openCameraBtn.isSelected == true {
        if openCameraBtn.isSelected == true { // 腾讯美颜 && 摄像头关闭时
            //: func__showStatusBarErrorMsg(showMsg: "Camera must be on".localized)
            diskTerms(showMsg: (String(app_accessMessage.prefix(5)) + "a mu" + String(data_oxygenFarUrl.prefix(7)) + k_remainKey.replacingOccurrences(of: "interrupt", with: "n")).localized)
            //: return
            return
        }
        //: rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        //: delegate?.interactionView_shouldChangeCamera(isFront: !rotateCameraBtn.isSelected)
        delegate?.faceTotal(isFront: !rotateCameraBtn.isSelected)
    }

    /// 是否禁用摄像头事件
    //: @objc private func openCameraButtonClick() {
    @objc private func success() {
        //: openCameraBtn.isSelected = !openCameraBtn.isSelected
        openCameraBtn.isSelected = !openCameraBtn.isSelected
        //: delegate?.interactionView_shouldForbiddenCamera(forbidden: openCameraBtn.isSelected)
        delegate?.hold(forbidden: openCameraBtn.isSelected)
    }

    /// 举报按钮点击事件（男性）
    //: @objc private func reportButtonClick() {
    @objc private func mutualSmall() {
        //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        let reportView = ExtraView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        //: reportView.showReportViewIn(view: nil)
        reportView.showThroughView(view: nil)
        //: reportView.reportHideBlock = { [weak self] succeed in
        reportView.reportHideBlock = { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            // 投诉成功
            //: self.reportBtn.isEnabled = false
            self.reportBtn.isEnabled = false
            //: self.reportBtn.isHidden = true
            self.reportBtn.isHidden = true
            //: self.delegate?.interactionView_reportSucceed()
            self.delegate?.occurSession()
        }
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func nonnegative() {
        //: func__sendGift()
        afterRange()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func reduceClick() {
//        self.isShowRecharge = true
        //: ElementEarnReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        ElementEarnReactiveCompatible.share.repulse(webViewType: .RechargeHalfPage)
    }

    //: @objc private func touchHidenBtnClick() {
    @objc private func soyaBean() {
        //: changeInteractionShowState()
        skip()
        //: if isShowInput {
        if isShowInput {
            //: dismissClick()
            turnABlindEyeSecond()
        }
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func skip() {
        //: let isHidden = !rotateCameraBtn.isHidden
        let isHidden = !rotateCameraBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: rotateCameraBtn.isHidden = isHidden
        rotateCameraBtn.isHidden = isHidden
        //: openCameraBtn.isHidden = isHidden
        openCameraBtn.isHidden = isHidden
        //: reportBtn.isHidden = (reportBtn.isEnabled ? isHidden:true) ||  AppManager.share.loginUserMode.sex == Gender.female.rawValue
        reportBtn.isHidden = (reportBtn.isEnabled ? isHidden : true) || AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: timeLab.isHidden = isHidden
        timeLab.isHidden = isHidden
        //: if TalkingSocketManager.shared.isFreeCall {
        if MigrationSocketDelegate.shared.isFreeCall {
            //: self.freeTimeView.isHidden = isHidden
            self.freeTimeView.isHidden = isHidden
        }
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            deviceTimer()
            //: } else {
        } else {
            //: touchHiddenTimer()
            wisecrack()
        }
    }

    /// 去电，x秒后才显示挂断按钮
    //: private func isShowHangupBtn() {
    private func theStates() {
        //: if chatModel.isCaller && chatModel.videoStage == .Waiting && AppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0 && AppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if chatModel.isCaller, chatModel.videoStage == .Waiting, AppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0, AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
            //: hangupBtn.isHidden = true
            hangupBtn.isHidden = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+Double(AppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + Double(AppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
                //: self.hangupBtn.isHidden = false
                self.hangupBtn.isHidden = false
            }
        }
    }

    /// 取消GCD自动接听定时器
    //: private func cancelAutoAcceptTimer() {
    private func usedCar() {
        //: if autoAcceptTimer != nil {
        if autoAcceptTimer != nil {
            //: autoAcceptTimer?.cancel()
            autoAcceptTimer?.cancel()
            //: autoAcceptTimer = nil
            autoAcceptTimer = nil
        }
    }
}

// MARK: - LightDanmuManager

//: extension Talking1v1VideoInteractionView: TalkingVideoDanmuManagerDelegate {
extension SizeMagnitudeeractionView: StudyThen {
    //: func func__DanmuRecvNewModel(Msg: TalkingVideoCallDanmuModel) {
    func driveSun(Msg: LanguageHandyJSON) {
        //: danmuView.addNewMsg(msgModel: Msg)
        danmuView.press(msgModel: Msg)
    }

    //: @objc private func commentBtnClick() {
    @objc private func minute() {
        /// 拉起弹幕评论
        //: self.bringSubviewToFront(commInputView)
        self.bringSubviewToFront(commInputView)
        //: commInputView.updatePlaceholder()
        commInputView.reason()
    }
}

// MARK: - 发表弹幕代理

//: extension Talking1v1VideoInteractionView: TalkingVideoCallInputViewDelegate {
extension SizeMagnitudeeractionView: CustomViewDelegate {
    //: @objc func dismissClick() {
    @objc func turnABlindEyeSecond() {
        //: self.isShowInput = false
        self.isShowInput = false
//        commInputView.inputTextView.text = ""
        //: commInputView.resignkeyBoard()
        commInputView.segmentExplain()
    }

    //: func func__sendTextMsg(msgStr: String) {
    func dealFit(msgStr: String) {
        //: self.isShowInput = false
        self.isShowInput = false
        //: sendToTextMsg(msgStr: msgStr)
        finishStr(msgStr: msgStr)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func fragmentApp(heightToBottom: CGFloat) {
        //: if heightToBottom > 0 {
        if heightToBottom > 0 {
            //: self.isShowInput = true
            self.isShowInput = true
        }
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self).offset(-heightToBottom)
            make.bottom.equalTo(self).offset(-heightToBottom)
        }
        //: danmuView.snp.updateConstraints { make in
        danmuView.snp.updateConstraints { make in
            //: let height = heightToBottom == 0 ? -10 : -(heightToBottom-chatBtn.height+commInputView.height-kDeviceSafeBottomHeight+10)
            let height = heightToBottom == 0 ? -10 : -(heightToBottom - chatBtn.height + commInputView.height - user_videoMsg + 10)
            //: make.bottom.equalTo(chatBtn.snp.top).offset(height)
            make.bottom.equalTo(chatBtn.snp.top).offset(height)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func stripHeight(height: CGFloat) {
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }

        //: self.needsUpdateConstraints()
        self.needsUpdateConstraints()
        //: self.updateConstraintsIfNeeded()
        self.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.layoutIfNeeded()
            self.layoutIfNeeded()
            //: })
        })
    }

    /// 发送文本消息
    //: func sendToTextMsg(msgStr: String) {
    func finishStr(msgStr: String) {
        //: TalkingVideoDanmuManager.uploadToTextMsg(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, errorModel in
        LightDanmuManager.want(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let json = JSON(result!)
                let json = JSON(result!)
                //: let msgId = json["msgId"].string
                let msgId = json[String(bytes: noti_prettyStr.map{$0^133}, encoding: .utf8)!].string
                //: var toUid: String?
                var toUid: String?
                //: if self.chatModel.iUid == AppManager.share.loginUserMode.userID {
                if self.chatModel.iUid == AppManager.share.loginUserMode.userID {
                    //: toUid = self.chatModel.pairUid
                    toUid = self.chatModel.pairUid
                    //: } else {
                } else {
                    //: toUid = self.chatModel.iUid
                    toUid = self.chatModel.iUid
                }
                //: let data = ["msgId": msgId, "senderUid": AppManager.share.loginUserMode.userID, "toUid": toUid, "nickname": AppManager.share.loginUserMode.nickname, "content": msgStr, "isMySender": "1"]
                let data = [String(bytes: noti_prettyStr.map{$0^133}, encoding: .utf8)!: msgId, String(bytes: k_sharedName.map{policyCommit(design: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.userID, (String(k_stockLimitedName.suffix(5))): toUid, String(bytes: constPrincipalStr.map{soapStarting(second: $0)}, encoding: .utf8)!: AppManager.share.loginUserMode.nickname, String(bytes: kDestroyKey.map{$0^112}, encoding: .utf8)!: msgStr, String(bytes: app_ownerHimPath.map{stallLanguagePeer(sibling: $0)}, encoding: .utf8)!: "1"]
                //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data as [String: Any])
                LightDanmuManager.itemSession().botch(msg: data as [String: Any])
                //: self.rotateCameraBtn.isHidden = true
                self.rotateCameraBtn.isHidden = true
                //: self.changeInteractionShowState()
                self.skip()
                //: self.commInputView.inputTextView.attributedText = nil
                self.commInputView.inputTextView.attributedText = nil
            }
        }
    }
}

// MARK: - GamePlanPlayerDelegate【视频播放器】

//: extension Talking1v1VideoInteractionView: TalkingVideoPlayerDelegate {
extension SizeMagnitudeeractionView: GamePlanPlayerDelegate {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func full(player _: SalesReactiveCompatible, status: MaskerPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.player.setRenderMode(renderMode: .FILL_SCREEN)
            self.player.spread(renderMode: .FILL_SCREEN)
            //: self.coverImg.isHidden = true
            self.coverImg.isHidden = true
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func identityAppearance(player _: SalesReactiveCompatible, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func starBright(player _: SalesReactiveCompatible, progress _: CGFloat) {}
}

// MARK: - 通知事件

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    /// 商汤美颜是否捕获到人脸通知（女性）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func submit(notification: NSNotification) {
        // 女性 && 等待接听
        //: guard AppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue else { return }
        //: guard self.chatModel.videoStage == .Recieved else { return }
        guard self.chatModel.videoStage == .Recieved else { return }

        //: let userInfo = notification.userInfo as! [String: AnyObject]
        let userInfo = notification.userInfo as! [String: AnyObject]
        //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
        let hasFace = (userInfo[(String(userRecognizeKey) + String(main_secretKey))] as! NSNumber).description.boolValue
        //: showOrHidenAutoRecieved(autoAccept: hasFace)
        theAccept(autoAccept: hasFace)
    }

    /// 是否展示自动接听按钮
    /// - Parameter autoAccept: 是否自动接听（默认：false）
    //: private func showOrHidenAutoRecieved(autoAccept: Bool = false) {
    private func theAccept(autoAccept: Bool = false) {
        //: var isActive = false
        var isActive = false

        //: if AppManager.share.appConfigMode.autoAnswer == 1 {
        if AppManager.share.appConfigMode.autoAnswer == 1 {
            //: let timeDifference = Date().timeIntervalSince1970 - (TalkingApplication.shared as! TalkingApplication).lastEventTime
            let timeDifference = Date().timeIntervalSince1970 - (WhiteThen.shared as! WhiteThen).lastEventTime
            //: if timeDifference < 5 {
            if timeDifference < 5 {
                //: isActive = true
                isActive = true
            }
        }

        //: DispatchQueue.main.async { [weak self] in
        DispatchQueue.main.async { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if autoAccept || isActive {
            if autoAccept || isActive { // 自动接听
                //: self.autoAcceptDesLab.isHidden = false
                self.autoAcceptDesLab.isHidden = false

                //: var countdown = AppManager.share.appConfigMode.autoAnswerCountdownTime
                var countdown = AppManager.share.appConfigMode.autoAnswerCountdownTime
                // 防止多次初始化
                //: guard self.autoAcceptTimer == nil else { return }
                guard self.autoAcceptTimer == nil else { return }
                //: self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                //: self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                //: self.autoAcceptTimer?.setEventHandler {
                self.autoAcceptTimer?.setEventHandler {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: guard countdown > 0 else {
                        guard countdown > 0 else {
                            //: if self.chatModel.videoStage == .Recieved {
                            if self.chatModel.videoStage == .Recieved {
                                //: self.acceptButtonClick()
                                self.recognize()
                            }
                            //: return
                            return
                        }
                        //: self.autoAcceptDesLab.text = "Automatically answer for you in %@s".localizedArguments(countdown)
                        self.autoAcceptDesLab.text = String(bytes: appExecuteTitle.reversed(), encoding: .utf8)!.queryionLocalized(countdown)
                        //: countdown -= 1
                        countdown -= 1
                    }
                }
                //: self.autoAcceptTimer?.resume()
                self.autoAcceptTimer?.resume()
            }
        }
    }

    /// 半屏充值页退下
    //: @objc private func changeRechargeStatus() {
    @objc private func modificationIconStatus() {}
}

// MARK: -

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    //: func showPointAnimaData(point: String) {
    func noNote(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.metadataOf(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    //: func func__sendGift() {
    func afterRange() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        LabelGiftManager.share.magnitudeervalCompletion {
            //: self.func__showGiftChoiceView()
            self.cameraToView()
        }
    }

    //: func func__showGiftChoiceView() {
    func cameraToView() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: AppManager.share.loginUserMode.mf_coin)
        giftView.fact(needReload: true, mf_coin: AppManager.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.file()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: PrepareDisplayTransformable, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
            self.generateer(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
        }
    }

    //: func func_giftViewDismiss() {
    func motionAway() {
        //: giftView.dismissView()
        giftView.oppositeness()
    }

    //: func func__sendGiftMsg(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
    func generateer(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            diskTerms(showMsg: show_titleOfName)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == AppManager.share.loginUserMode.userID {
        if chatModel.iUid == AppManager.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(k_stockLimitedName.suffix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(k_stockLimitedName.suffix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftId
        params[(String(constMarginExplainTitle))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(show_stillData.suffix(6)) + String(main_phaseName))] = giftNum
        //: params["from"] = "videoCall"
        params[(String(k_teamBubblePath))] = (app_middleData.replacingOccurrences(of: "current", with: "o") + "Call")
        //: if programSort != nil {
        if programSort != nil {
            //: params["programSort"] = programSort
            params[(noti_valueText.replacingOccurrences(of: "medal", with: "r") + String(kUniformKey.prefix(9)))] = programSort
        }
        //: if pkgItemsetId != nil {
        if pkgItemsetId != nil {
            //: params["pkgItemsetId"] = pkgItemsetId
            params[(String(user_busyMessage.suffix(5)) + "emse" + String(data_narrowName.prefix(3)))] = pkgItemsetId
        }

        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        HouseThen.crushCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.shaving(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if pkgItemsetId != nil {
            if pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.crepe(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.atConvert(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func atConvert(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(dataDraftMessage.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(userNorValue.suffix(7)))] = (appZzValue.replacingOccurrences(of: "session", with: "ft"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(dataDraftMessage.suffix(7)))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        primaryListen(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(const_honeyId) + String(mainGalleryKey.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(const_honeyId) + String(mainGalleryKey.suffix(6)))] as! NSNumber
            //: AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)
        giftView.fact(needReload: false, mf_coin: AppManager.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == AppManager.share.loginUserMode.userID {
        if chatModel.iUid == AppManager.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)
        let cellData = StillThen.sewageDisposalPlantToConclude(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: user_lessName, object: self, userInfo: [String(bytes: constHomeMysteryValue.map{helloMine(gender: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func shaving(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard AppManager.share.loginUserMode.status != 1 else {
            guard AppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    diskTerms(showMsg: errorStr)
                }
                //: return
                return
            }
            //: ElementEarnReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ElementEarnReactiveCompatible.share.recharge(clickEvent: data_meetingUrl, sufficient: false)
            //: giftView.dismissView()
            giftView.oppositeness()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = RateGuidanceThen(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey))
            //: view.show()
            view.pendingMic()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                diskTerms(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func cach() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == AppManager.share.loginUserMode.userID {
        if chatModel.iUid == AppManager.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.weeklyCommon(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func primaryListen(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.chooseRoundMedium(dict: giftDic) ?? LightAnimatModel()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in LabelGiftManager.share.charmQuote() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SchemeTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.boxPositive(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.portionMesohippus(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func makeFull(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in LabelGiftManager.share.charmQuote() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SchemeTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.boxPositive(model: giftModel as! LightAnimatModel)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.buttonRunning(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func hangUp() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.album()
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 免费视频剩余时间倒计时
    //: private func setFreeCountTimer() {
    private func boardTimer() {
        //: guard freeCountTimer == nil else { return }
        guard freeCountTimer == nil else { return }
        //: var index = AppManager.share.loginUserMode.rechargePopCountdown
        var index = AppManager.share.loginUserMode.rechargePopCountdown
        //: freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: index -= 1
            index -= 1
            //: self.freeTimeView.setTipsMessage(count: index)
            self.freeTimeView.stratfordOpenAvon(count: index)
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func album() {
        //: let duration = getCurrentTalkingTime()
        let duration = originalSelected()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func stepPremium(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        album()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func originalSelected() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func wisecrack() {
        //: invalidateIdleTimer()
        deviceTimer()
        //: perform(#selector(hideInteractionView), afterDelay: 10)
        perform(#selector(sightView), afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func deviceTimer() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(sightView), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func sightView() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.skip()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VideoInteractionView {
extension SizeMagnitudeeractionView {
    /// 添加视图
    //: private func setupSubviews() {
    private func resign() {
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(coverImg)
        addSubview(coverImg)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairInfoBg)
        addSubview(pairInfoBg)
        //: pairInfoBg.addSubview(pairPicV)
        pairInfoBg.addSubview(pairPicV)
        //: pairInfoBg.addSubview(pairNameLab)
        pairInfoBg.addSubview(pairNameLab)
        //: pairInfoBg.addSubview(locationBtn)
        pairInfoBg.addSubview(locationBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(rotateCameraBtn)
        addSubview(rotateCameraBtn)
        //: addSubview(openCameraBtn)
        addSubview(openCameraBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(reportBtn)
        addSubview(reportBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(chatBtn)
        addSubview(chatBtn)
        //: addSubview(danmuView)
        addSubview(danmuView)
        //: addSubview(commInputView)
        addSubview(commInputView)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
        //: addSubview(autoAcceptDesLab)
        addSubview(autoAcceptDesLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func put() {
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - user_videoMsg)
        }
        //: coverImg.snp.makeConstraints { make in
        coverImg.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairInfoBg.snp.makeConstraints { make in
        pairInfoBg.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(dataInfoTrustName + 16)
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
            make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 5))
            make.leading.equalTo(actualWidth(w: 5))
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
            make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.height.equalTo(17)
            make.height.equalTo(17)
            //: make.trailing.equalToSuperview().offset(-7)
            make.trailing.equalToSuperview().offset(-7)
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(pairNameLab)
            make.leading.equalTo(pairNameLab)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight+86)
            make.top.equalToSuperview().offset(dataInfoTrustName + 86)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(dataInfoTrustName + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }

        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(hangupBtn)
            make.bottom.equalTo(hangupBtn)
            //: make.size.equalTo(hangupBtn)
            make.size.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+20)
            make.top.equalTo(dataInfoTrustName + 20)
        }
        //: autoAcceptDesLab.snp.makeConstraints { make in
        autoAcceptDesLab.snp.makeConstraints { make in
            //: make.center.width.equalToSuperview()
            make.center.width.equalToSuperview()
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-user_videoMsg)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
            //: make.height.equalTo(actualWidth(w: 34))
            make.height.equalTo(actualWidth(w: 34))
        }
        //: danmuView.snp.makeConstraints { make in
        danmuView.snp.makeConstraints { make in
            //: make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(VideoMsgTableViewHeight)
            make.height.equalTo(const_adjustFormat)
            //: make.width.equalTo(VideoMsgTableViewWidth)
            make.width.equalTo(userInfoTitle)
        }
        //: reportBtn.snp.makeConstraints { make in
        reportBtn.snp.makeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.size.equalTo(actualWidth(w: 33))
            make.size.equalTo(actualWidth(w: 33))
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }

        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-user_videoMsg - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: commInputView.snp.makeConstraints { make in
        commInputView.snp.makeConstraints { make in
            //: make.bottom.equalTo(self)
            make.bottom.equalTo(self)
            //: make.leading.trailing.equalTo(0)
            make.leading.trailing.equalTo(0)
            //: make.height.equalTo(actualWidth(w: 55))
            make.height.equalTo(actualWidth(w: 55))
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func reloadStack() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if AppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(show_explainId / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-user_videoMsg - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(show_explainId / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-user_videoMsg - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-user_videoMsg - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
                //: openCameraBtn.snp.makeConstraints { make in
                openCameraBtn.snp.makeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+12)
                    make.leading.equalTo(show_explainId / 2 + 12)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                    make.bottom.equalTo(-user_videoMsg - 15)
                    //: make.size.equalTo(actualWidth(w: 45))
                    make.size.equalTo(actualWidth(w: 45))
                }
                //: rotateCameraBtn.snp.makeConstraints { make in
                rotateCameraBtn.snp.makeConstraints { make in
                    //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    //: make.centerY.equalTo(openCameraBtn)
                    make.centerY.equalTo(openCameraBtn)
                    //: make.size.equalTo(openCameraBtn)
                    make.size.equalTo(openCameraBtn)
                }
            }
            //: } else if chatModel.videoStage == .Calling {
        } else if chatModel.videoStage == .Calling {
            //: hangupBtn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
            hangupBtn.setImage(UIImage.scheme(name: (String(mainContendPath) + String(notiTaTracePatternTitle.prefix(6)))), for: .normal)
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: openCameraBtn.snp.remakeConstraints { make in
            openCameraBtn.snp.remakeConstraints { make in
                //: make.top.equalTo(StatusBarHeight+actualHeight(h: 25)+20)
                make.top.equalTo(dataInfoTrustName + actualHeight(h: 25) + 20)
                //: make.trailing.equalToSuperview().offset(-20)
                make.trailing.equalToSuperview().offset(-20)
                //: make.size.equalTo(actualWidth(w: 33))
                make.size.equalTo(actualWidth(w: 33))
            }
            //: rotateCameraBtn.snp.remakeConstraints { make in
            rotateCameraBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.makeConstraints { make in
            hangupBtn.snp.makeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-user_videoMsg - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
            //: openCameraBtn.snp.makeConstraints { make in
            openCameraBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+12)
                make.leading.equalTo(show_explainId / 2 + 12)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                make.bottom.equalTo(-user_videoMsg - 15)
                //: make.size.equalTo(actualWidth(w: 45))
                make.size.equalTo(actualWidth(w: 45))
            }
            //: rotateCameraBtn.snp.makeConstraints { make in
            rotateCameraBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
        }
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func stickToWithReciprocationSumeraction() {
        // 视频等待...动画
        //: waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
        waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard var text = self.desLab.text else { return }
            guard var text = self.desLab.text else { return }

            //: if text.hasSuffix("...") {
            if text.hasSuffix("...") {
                //: text = text.replacingOccurrences(of: "...", with: ".")
                text = text.replacingOccurrences(of: "...", with: ".")
                //: } else if text.hasSuffix("..") {
            } else if text.hasSuffix("..") {
                //: text = text.replacingOccurrences(of: "..", with: "...")
                text = text.replacingOccurrences(of: "..", with: "...")
                //: } else {
            } else {
                //: text = text.replacingOccurrences(of: ".", with: "..")
                text = text.replacingOccurrences(of: ".", with: "..")
            }
            //: self.desLab.text = text
            self.desLab.text = text

            //: }, repeats: true)
        }, repeats: true)
        //: waitTimer?.fire()
        waitTimer?.fire()
    }

    //: private func setFreeView() {
    private func progressSet() {
        //: addSubview(freeTimeView)
        addSubview(freeTimeView)
        //: freeTimeView.snp.remakeConstraints { make in
        freeTimeView.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            //: make.height.equalTo(111)
            make.height.equalTo(111)
            //: make.width.equalTo(126)
            make.width.equalTo(126)
        }
    }

    /// 添加通知
    //: private func initNotificationObserver() {
    private func successfullyWindow() {
        // 女性用户 && 使用商汤 && 人脸检测自动接听
        //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue &&
        if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue,
           //: SenseTime_Use == true &&
           notiMaxMsg == true,
           //: AppManager.share.appConfigMode.autoAnswer == 1 {
           AppManager.share.appConfigMode.autoAnswer == 1
        {
            /// 监听 是否捕获到人脸
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(submit(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_12_frame"),
                                                   name: NSNotification.Name(rawValue: String(bytes: notiBoneTitle.map{$0^57}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        //: NotificationCenter.default.addObserver(self, selector: #selector(changeRechargeStatus), name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(modificationIconStatus), name: user_cleanPath, object: nil)
    }
}
