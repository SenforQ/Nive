
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_middleFormat:[UInt8] = [0x2,0x5,0x2,0x1f,0x43,0x8,0x4,0xf,0xe,0x19,0x51,0x42,0x4b,0x3,0xa,0x18,0x4b,0x5,0x4,0x1f,0x4b,0x9,0xe,0xe,0x5,0x4b,0x2,0x6,0x1b,0x7,0xe,0x6,0xe,0x5,0x1f,0xe,0xf]

private func galleryCapableLog(place num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "uid" :*/
fileprivate let notiBePath:[UInt8] = [0xbf,0xb3,0xae]

fileprivate func extendVoice(bottle num: UInt8) -> UInt8 {
    let value = Int(num) - 74
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let kSaleStr:[Character] = ["h","e","a","d","P","i"]
fileprivate let k_superiorPath:String = "fit"

/*: "nickname" :*/
fileprivate let app_bindGiftFormat:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let dataConcernTitle:String = "birprevioush"
fileprivate let userAsideBasicDayKey:String = "dchild"

/*: "videoUrl" :*/
fileprivate let const_helpFormat:String = "going"
fileprivate let data_socialKey:[Character] = ["i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let app_holderName:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let main_beyondPath:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let dataDotTitle:[Character] = ["r","e","f","u","s","e"]
fileprivate let kWrapName:String = "fee statistical agoCall"

/*: "data" :*/
fileprivate let const_movieTitle:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let userHugePath:String = "container volume formalCall i"
fileprivate let data_pressureValue:String = "legitimateuption"

/*: "stopCall" :*/
fileprivate let noti_onceMessage:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let appExampleName:[UInt8] = [0xa3,0xa0,0xa8,0x86,0xab]

private func buildFore(pan num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "cancelCall" :*/
fileprivate let notiStopKey:String = "cancbear"
fileprivate let dataFamilyContent:String = "lCallstat fence"

/*: "No answer" :*/
fileprivate let show_nodMessage:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let kTopId:String = "duratisay"

/*: "She has hung up" :*/
fileprivate let noti_destinationKey:String = "She hillegal would pursue zz design"
fileprivate let k_crossUrl:String = "g upstatute more firm currency"

/*: "cancelSex" :*/
fileprivate let kTicketData:[Character] = ["c","a","n","c","e","l","S","e"]
fileprivate let data_floorId:String = "boy"

/*: "He has hung up" :*/
fileprivate let const_earnFullId:String = "pow additionHe ha"
fileprivate let showForthPath:String = "recognize"

/*: "endType" :*/
fileprivate let userAssertValue:String = "some taskendType"

/*: "stopSex" :*/
fileprivate let dataHappyKey:String = "stopSplus report spark"
fileprivate let userMeanMessage:[Character] = ["e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_withoutValue:[UInt8] = [0xa9,0xce,0xd3,0xd5,0xc6,0xc6,0xc9,0xc3,0xc9,0xc5,0xce,0xd4,0x80,0xc7,0xcf,0xcc,0xc4,0x80,0xc3,0xcf,0xc9,0xce,0xd3,0x8c,0x80,0xc3,0xc1,0xcc,0xcc,0x80,0xc8,0xc1,0xd3,0x80,0xc5,0xce,0xc4,0xc5,0xc4]

fileprivate func smoothPlant(interval num: UInt8) -> UInt8 {
    let value = Int(num) - 96
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let user_fromDescribeKey:String = "mentally more least movieThe "
fileprivate let k_selectDockMessage:[Character] = ["o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let dataGroupUrl:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let main_strokePath:String = "device soundjumpUr"
fileprivate let mainFiniteTitle:[Character] = ["l"]

/*: "Cancel" :*/
fileprivate let app_chartData:String = "scan package sheCancel"

/*: "Get gold coins" :*/
fileprivate let main_inviteFormat:[Character] = ["G","e","t"," ","g","o","l","d"," ","c","o","i"]
fileprivate let appShirtData:[Character] = ["n","s"]

/*: "addBean" :*/
fileprivate let k_chemicalMessage:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let data_offerMessage:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let constSenseTitle:[Character] = ["o","n","A","c","c","e"]
fileprivate let mainEquallyFormat:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let notiSeeCustomPath:[Character] = ["a","c","c","e","p","t","C"]
fileprivate let user_answerStr:String = "processingll"

/*: "Calls are cancelled" :*/
fileprivate let appNumbMsg:String = "Calls amay jaw particular"
fileprivate let notiParaFindId:String = "re carequest ion replacement"

/*: "The call has ended" :*/
fileprivate let notiBlackContent:[Character] = ["T","h","e"," ","c","a"]
fileprivate let const_countUrl:[Character] = ["l","l"," ","h","a","s"," "]
fileprivate let kPackageId:[Character] = ["e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StillViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let show_objectReplaceNameId = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class StillViewController: MirrorRealmReactiveCompatible {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: SimulationChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ExtraPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: MiniskirtFrameworkView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        MigrationSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MigrationSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MigrationSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_middleFormat.map{galleryCapableLog(place: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! LetterClingReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.colorPicture()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            outScriptAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currMutualVC = nil
            MigrationSocketDelegate.shared.currMutualVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.placeQuick()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        suspendSubviews()
        //: setupSubViewsConstraint()
        headwaterConstraint()
        //: initNotificationObserver()
        recordingObserver()
        //: req_callGetUserInfo()
        exampleEmptySave()
        //: timeoutRules()
        rulesMove()
        //: resumeTalk()
        netRestart()
        //: setUpBGMPlayer()
        plunge()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AppManager.share.appConfigMode.disableShootScreen {
            if AppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ArgumentView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isMutual = false
        MigrationSocketDelegate.shared.isMutual = false
        //: TalkingSocketManager.shared.isCalling = false
        MigrationSocketDelegate.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.birthSearched()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: TwentyFourHourPeriodReactiveCompatible = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TwentyFourHourPeriodReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: KingLightChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = KingLightChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func exampleEmptySave() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ChatCloudDelegate.notice(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kSaleStr) + k_superiorPath.replacingOccurrences(of: "fit", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(app_bindGiftFormat))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(dataConcernTitle.replacingOccurrences(of: "previous", with: "t") + userAsideBasicDayKey.replacingOccurrences(of: "child", with: "ay"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(const_helpFormat.replacingOccurrences(of: "going", with: "v") + String(data_socialKey))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(app_holderName))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.pullInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func outScriptAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        EditThen.audienceBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.diskTerms(showMsg: main_workMessage)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(dataDotTitle) + String(kWrapName.suffix(4))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.promotion()
                //: return
                return
            }

            //: self.stopBGM()
            self.enabled()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ExtraPlayListener.linksman(type: .Response)
            //: self.startBGM()
            self.colorPicture()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func birthSearched() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.enabled()
        //: TalkingSocketManager.shared.callerDelegate = nil
        MigrationSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        MigrationSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        MigrationSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func promotion(animated: Bool = true) {
        //: self.stopBGM()
        self.enabled()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func under() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.moreFor(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func netRestart() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            under()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func recordingObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(runningDock(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(totalerest(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: data_tagMsg,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        show_objectReplaceNameId.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func runningDock(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            promotion()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func totalerest(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard MigrationSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        MigrationSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MigrationSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MigrationSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.legal(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension StillViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.diskTerms(showMsg: (String(userHugePath.suffix(6)) + "nter" + data_pressureValue.replacingOccurrences(of: "legitimate", with: "r")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(noti_onceMessage)), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (notiStopKey.replacingOccurrences(of: "bear", with: "e") + String(dataFamilyContent.prefix(5))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(dataDotTitle) + String(kWrapName.suffix(4))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spaceDatabase()
                //: TalkingSocketManager.shared.currMutualVC = nil
                MigrationSocketDelegate.shared.currMutualVC = nil
            }
            //: popCurrentViewController()
            promotion()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.old(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func plunge() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? RateEnsureBasicType.Call : RateEnsureBasicType.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ExtraPlayListener.linksman(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func colorPicture() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.ownLaunch()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func enabled() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.sparkInStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func rulesMove() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? dataMethodKey : self.chatModel?.valuePublish()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            advantagePremium()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.advantagePremium()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func advantagePremium() {
        //: guard !TalkingSocketManager.shared.isMutual else { return }
        guard !MigrationSocketDelegate.shared.isMutual else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.diskTerms(showMsg: (String(show_nodMessage)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (notiStopKey.replacingOccurrences(of: "bear", with: "e") + String(dataFamilyContent.prefix(5))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(dataDotTitle) + String(kWrapName.suffix(4))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        MigrationSocketDelegate.shared.renderOffDraft(info: socketDict)
        //: popCurrentViewController()
        promotion()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func modifyWithDataKey(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(kTopId.replacingOccurrences(of: "say", with: "on"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.signByDark(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.nonconductor()
            //: createTalkRoom()
            under()
            //: self.stopBGM()
            self.enabled()
        }
    }
}

// MARK: - ElapidObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension StillViewController: ElapidObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func programmeNumbererval(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(noti_destinationKey.prefix(5)) + "as hun" + String(k_crossUrl.prefix(4))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(kTicketData) + data_floorId.replacingOccurrences(of: "boy", with: "x"))].stringValue == SharedMirrorKingCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_earnFullId.suffix(5)) + "s hung u" + showForthPath.replacingOccurrences(of: "recognize", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.diskTerms(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spaceDatabase()
            //: TalkingSocketManager.shared.currMutualVC = nil
            MigrationSocketDelegate.shared.currMutualVC = nil
        }

        //: popCurrentViewController()
        promotion()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func roundEvaluate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.enabled()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.nonconductor()
        //: self.createTalkRoom()
        self.under()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func addLight(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(userAssertValue.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(noti_destinationKey.prefix(5)) + "as hun" + String(k_crossUrl.prefix(4))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(dataHappyKey.prefix(5)) + String(userMeanMessage))].stringValue == SharedMirrorKingCustomReflectable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(const_earnFullId.suffix(5)) + "s hung u" + showForthPath.replacingOccurrences(of: "recognize", with: "p")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_withoutValue.map{smoothPlant(interval: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(user_fromDescribeKey.suffix(4)) + String(k_selectDockMessage)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.diskTerms(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spaceDatabase()
            //: TalkingSocketManager.shared.currMutualVC = nil
            MigrationSocketDelegate.shared.currMutualVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.dotThumb()
        //: self.popCurrentViewController()
        self.promotion()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: showBeautyOfStr, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func unneeded(msg: String, data: [String: Any]) {
        //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(dataGroupUrl))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(main_strokePath.suffix(6)) + String(mainFiniteTitle))] as? String else { return }
            //: let httpsUrl = AppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = AppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = AnotherConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: ElementEarnReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ElementEarnReactiveCompatible.share.lab(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            PulseReactiveCompatible.contentConfig(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(app_chartData.suffix(6))).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(main_inviteFormat) + String(appShirtData)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                PulseReactiveCompatible.carveOutOnicial()
                //: } rightBlock: {
            } rightBlock: {
                //: ElementEarnReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ElementEarnReactiveCompatible.share.recharge(clickEvent: notiAdjustContent, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func engineering(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(k_chemicalMessage))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.showAdmin(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.ego(point: addBean)
        }
    }
}

// MARK: - DuplexPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StillViewController: DuplexPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func telecasting(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(noti_destinationKey.prefix(5)) + "as hun" + String(k_crossUrl.prefix(4))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(data_offerMessage))].stringValue == SharedMirrorKingCustomReflectable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_earnFullId.suffix(5)) + "s hung u" + showForthPath.replacingOccurrences(of: "recognize", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.diskTerms(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spaceDatabase()
            //: TalkingSocketManager.shared.currMutualVC = nil
            MigrationSocketDelegate.shared.currMutualVC = nil
        }
        //: popCurrentViewController()
        promotion()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func adopt(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.enabled()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.nonconductor()
        //: self.createTalkRoom()
        self.under()
    }
}

// MARK: - ClingRealmObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension StillViewController: ClingRealmObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func quality(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(dataDotTitle) + String(kWrapName.suffix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.diskTerms(showMsg: (String(show_nodMessage)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spaceDatabase()
                //: TalkingSocketManager.shared.currMutualVC = nil
                MigrationSocketDelegate.shared.currMutualVC = nil
            }
            //: self.popCurrentViewController()
            self.promotion()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(constSenseTitle) + String(mainEquallyFormat)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard AppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue else { return }
            //: ElementEarnReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            ElementEarnReactiveCompatible.share.recharge(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(constSenseTitle) + String(mainEquallyFormat)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.diskTerms(showMsg: msg)
            //: self.popCurrentViewController()
            self.promotion()
        }
    }
}

// MARK: - BotMirrorObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension StillViewController: BotMirrorObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func arcValue() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(notiSeeCustomPath) + user_answerStr.replacingOccurrences(of: "processing", with: "a")), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func sumeractionWithDub() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.diskTerms(showMsg: (String(appNumbMsg.prefix(7)) + String(notiParaFindId.prefix(5)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (notiStopKey.replacingOccurrences(of: "bear", with: "e") + String(dataFamilyContent.prefix(5))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.diskTerms(showMsg: (String(appNumbMsg.prefix(7)) + String(notiParaFindId.prefix(5)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(dataDotTitle) + String(kWrapName.suffix(4))), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: notiBePath.map{extendVoice(bottle: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.diskTerms(showMsg: (String(notiBlackContent) + String(const_countUrl) + String(kPackageId)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            MigrationSocketDelegate.shared.renderOffDraft(info: [String(bytes: main_beyondPath.reversed(), encoding: .utf8)!: (String(noti_onceMessage)), String(bytes: const_movieTitle.reversed(), encoding: .utf8)!: [String(bytes: appExampleName.map{buildFore(pan: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.dotThumb()
        //: self.popCurrentViewController()
        self.promotion()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func putMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.checkMaximum(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func errHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.busyDetect(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func carryWindow() {
        //: TalkingSocketManager.shared.currMutualVC = self
        MigrationSocketDelegate.shared.currMutualVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = MiniskirtFrameworkView.around()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.reasonWindow()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.earlyTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.fire(talkingTime)
        //: popCurrentViewController()
        promotion()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func femaleTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.fire(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func acrossArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.clubReasonBy(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension StillViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func suspendSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func headwaterConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
