
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let const_shakeSupportMouthStr:[Character] = ["m","f","/","b","u","s","i"]
fileprivate let show_socialMessage:[Character] = ["n","e","s","s","/","g","i","f","t","L","i","s","t"]

/*: "type" :*/
fileprivate let show_portionNatureFormat:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "mf/chatRoom/giftList" :*/
fileprivate let const_completePath:String = "mf/cjust form supposed auto"
fileprivate let data_interactionName:[Character] = ["/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let showMarriedNapData:[Character] = ["l","i","v","e","/","g"]
fileprivate let k_hoodTitle:String = "hair range whetheriftList"

/*: "IM/getMessageList" :*/
fileprivate let showSupposedName:String = "ban balance evaluate centerIM/get"
fileprivate let showBothId:String = "ageLlimited disk root"
fileprivate let user_principalMsg:[Character] = ["i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let noti_defenseBoxViewMsg:String = "through remarkmf/bu"
fileprivate let app_yourselfData:[Character] = ["n","d","M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let userHangCampData:[Character] = ["m","f","/","c","h","a"]
fileprivate let mainConferenceMindPath:[Character] = ["t","R","o","o","m","/","s","e"]
fileprivate let kCoreTheoryKey:[Character] = ["n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let app_optionUrl:String = "retirement green king shelf turnmf/bu"
fileprivate let appStreetId:String = "ss/sno moment tool scope harassment"
fileprivate let kGirlId:[Character] = ["e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let appAtMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let show_partUrl:String = "m/sedomain called agree formal far"

/*: "live/sendGift" :*/
fileprivate let constFlowFormat:String = "yellowive"
fileprivate let user_assessmentMonthPath:String = "conclude kit mention publish legitimatedGift"

/*: "mf/user/getChatInfo" :*/
fileprivate let show_publiclyStr:String = "mf/useplan bucket file container secure"
fileprivate let data_electAlwaysTitle:String = "vid act detailr/ge"
fileprivate let user_albumText:String = "proximate primary mistInfo"

/*: "IM/getMessageUserInfoList" :*/
fileprivate let constErrKey:[Character] = ["I","M","/","g","e","t","M","e","s","s","a","g","e","U","s","e","r","I","n","f","o","L","i"]
fileprivate let app_heartKey:[Character] = ["s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let app_mmStatePath:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L"]
fileprivate let mainNoteData:String = "ikeMbetter thread ban resent"

/*: "mf/crush/ignore" :*/
fileprivate let constUponName:[Character] = ["m","f","/","c","r","u","s","h","/","i","g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let userHealthyData:[Character] = ["m","f","/","c","r","u"]
fileprivate let data_careTalkPath:[Character] = ["s","h","/","l","i","k","e"]

/*: "mf/user/getRelation" :*/
fileprivate let app_handlePath:String = "mutual migration regain strokemf/use"
fileprivate let dataHiddenBudgetMsg:String = "latimoden"

/*: "user/attention" :*/
fileprivate let k_ourUrl:[UInt8] = [0x84,0x82,0x94,0x83,0xde,0x90,0x85,0x85,0x94,0x9f,0x85,0x98,0x9e,0x9f]

private func popularityChangeInput(max num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "user/removeAttention" :*/
fileprivate let mainRequestName:String = "usfeature"
fileprivate let main_consequenceValue:[Character] = ["A","t","t","e","n","t","i","o","n"]

/*: "Can't earn points if you follow each other~" :*/
fileprivate let data_seeValue:[UInt8] = [0x8c,0xae,0xa1,0xe8,0xbb,0xef,0xaa,0xae,0xbd,0xa1,0xef,0xbf,0xa0,0xa6,0xa1,0xbb,0xbc,0xef,0xa6,0xa9,0xef,0xb6,0xa0,0xba,0xef,0xa9,0xa0,0xa3,0xa3,0xa0,0xb8,0xef,0xaa,0xae,0xac,0xa7,0xef,0xa0,0xbb,0xa7,0xaa,0xbd,0xb1]

/*: "follow Success" :*/
fileprivate let userProductionMsg:[Character] = ["f","o","l","l","o"]
fileprivate let show_pinionStr:String = "w Successlower contend"

/*: "unfollow Success" :*/
fileprivate let data_resentClueRollingValue:String = "retainfo"
fileprivate let const_diskFreshFormat:String = "interested devote Success"

/*: "translate/msg" :*/
fileprivate let k_pinionValue:[Character] = ["t","r","a","n","s","l","a"]
fileprivate let show_needMemberName:String = "full neighbor approximatelyte/msg"

/*: "msgId" :*/
fileprivate let k_socialStr:[UInt8] = [0xd0,0xce,0xda,0xf4,0xd9]

/*: "targetLng" :*/
fileprivate let show_shotEhMsg:[UInt8] = [0xbe,0xab,0xb8,0xad,0xaf,0xbe,0x86,0xa4,0xad]

private func listBounce(flow num: UInt8) -> UInt8 {
    return num ^ 202
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let dataStartName:String = "mf/gretell particular input the"
fileprivate let show_clipHelloStr:[Character] = ["e","t","/","g","e","t","A","u","t","o","G","r","e","e","t","C"]
fileprivate let showBurnTitle:[Character] = ["o","n","t","e","n","t"]

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let kReaderStretchCenterName:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","I","n"]
fileprivate let k_chooseName:[Character] = ["t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let notiAdditionalPath:[UInt8] = [0x8a,0x85,0x6b,0x7f,0x7a]

fileprivate func curveLife(cos num: UInt8) -> UInt8 {
    let value = Int(num) - 22
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/info" :*/
fileprivate let userNegativeName:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let appSecurePath:[Character] = ["m","f","/","c","h","a","t"]
fileprivate let show_pingContent:[Character] = ["R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let k_hisLevelId:[UInt8] = [0xb7,0xb4,0xb4,0xb2,0x8e,0xa9]

fileprivate func comeMay(something num: UInt8) -> UInt8 {
    let value = Int(num) + 187
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "needExtraInfo" :*/
fileprivate let show_unknownWarningStr:[UInt8] = [0xd6,0xdd,0xdd,0xdc,0xfd,0xc0,0xcc,0xca,0xd9,0xf1,0xd6,0xde,0xd7]

private func rationalMotive(sibling num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "mf/chatRoom/check" :*/
fileprivate let app_willPartMessage:String = "definition analyze page literal methodmf/cha"
fileprivate let constSmoothReportPath:String = "m/chefollow path thread weekly"
fileprivate let k_segmentContent:String = "rolling"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let userMatchId:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","j","o","i","n","O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let userHabitDonValue:String = "mf/chatRpresent beat everyone subject view"
fileprivate let main_lowEventTitle:String = "foundation bubble clothes normaloom/me"

/*: "translate/text" :*/
fileprivate let dataOrangeDamageMeetName:String = "transexpression"
fileprivate let kSaltMessage:String = "te/textstatement unknown earn guidance pro"

/*: "text" :*/
fileprivate let dataGeneralId:[UInt8] = [0xe9,0xda,0xed,0xe9]

fileprivate func rollingAverage(flexible num: UInt8) -> UInt8 {
    let value = Int(num) + 139
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/user/queryStatus" :*/
fileprivate let kNightSatisfyScopeUrl:String = "mf/uscase shrink fade maximum closed"
fileprivate let data_easeKey:String = "erySsibling outcome skip arc safety"

/*: "toUids" :*/
fileprivate let data_saltRateText:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let const_finishKey:[Character] = ["c","a","l","l","/","g"]
fileprivate let userBarCenterData:[Character] = ["e","t","C","a","l","l","T","a","b"]

/*: "mf/user/remark" :*/
fileprivate let main_wayData:String = "drawing master agency andmf/us"

/*: "content" :*/
fileprivate let user_spanData:[UInt8] = [0xb3,0xbf,0xbe,0xa4,0xb5,0xbe,0xa4]

private func recoverBench(each num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "top" :*/
fileprivate let notiMethodUrl:[UInt8] = [0xe3,0xf8,0xe7]

private func revenueRandom(vertical num: UInt8) -> UInt8 {
    return num ^ 151
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HouseThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class TalkingChatRequestTool: NSObject {
public class HouseThen: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [AccomplishThen]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [AccomplishThen]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func childEach(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(const_shakeSupportMouthStr) + String(show_socialMessage))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: show_portionNatureFormat.reversed(), encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func productCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(const_completePath.prefix(4)) + "hatRoom" + String(data_interactionName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func make(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(showMarriedNapData) + String(k_hoodTitle.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func takeQuit(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(showSupposedName.suffix(6)) + "Mess" + String(showBothId.prefix(4)) + String(user_principalMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func pullLike(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(noti_defenseBoxViewMsg.suffix(5)) + "siness/se" + String(app_yourselfData))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func closet(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(userHangCampData) + String(mainConferenceMindPath) + String(kCoreTheoryKey))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func crushCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(app_optionUrl.suffix(5)) + "sine" + String(appStreetId.prefix(4)) + String(kGirlId))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func giftHorse(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(appAtMsg) + String(show_partUrl.prefix(4)) + "ndGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func send(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (constFlowFormat.replacingOccurrences(of: "yellow", with: "l") + "/sen" + String(user_assessmentMonthPath.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func refreshOnce(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(show_publiclyStr.prefix(6)) + String(data_electAlwaysTitle.suffix(4)) + "tChat" + String(user_albumText.suffix(4)))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func product(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(constErrKey) + String(app_heartKey))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<AccomplishThen>.deserialize(from: result as? Array) {
            if let datas = Array<AccomplishThen>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [AccomplishThen])
                completion(succeed, datas as! [AccomplishThen])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func boardStream(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        user_formalMsg.sizeSource(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func usa(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(app_mmStatePath) + String(mainNoteData.prefix(4)) + "eList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func dragQuality(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(constUponName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func heartstrings(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(userHealthyData) + String(data_careTalkPath))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func finishOut(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(app_handlePath.suffix(6)) + "r/getRe" + dataHiddenBudgetMsg.replacingOccurrences(of: "mode", with: "o"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func reiterateUponCompletion(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: k_ourUrl.map{popularityChangeInput(max: $0)}, encoding: .utf8)! : (mainRequestName.replacingOccurrences(of: "feature", with: "er") + "/remove" + String(main_consequenceValue))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
                    if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        user_streamThirdDescriptionValue.sirenSong(showMsg: String(bytes: data_seeValue.map{$0^207}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        user_streamThirdDescriptionValue.sirenSong(showMsg: (String(userProductionMsg) + String(show_pinionStr.prefix(9))).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    user_streamThirdDescriptionValue.sirenSong(showMsg: (data_resentClueRollingValue.replacingOccurrences(of: "retain", with: "un") + "llow" + String(const_diskFreshFormat.suffix(8))).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                user_streamThirdDescriptionValue.diskTerms(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func readingTo(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(k_pinionValue) + String(show_needMemberName.suffix(6)))
        //: reqModel.params = ["msgId": msgId, "targetLng": DelineateThen.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: k_socialStr.map{$0^189}, encoding: .utf8)!: msgId, String(bytes: show_shotEhMsg.map{listBounce(flow: $0)}, encoding: .utf8)!: DelineateThen.termsTransform(), String(bytes: show_portionNatureFormat.reversed(), encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func statDevice(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(dataStartName.prefix(6)) + String(show_clipHelloStr) + String(showBurnTitle))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func reasonAge(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(kReaderStretchCenterName) + String(k_chooseName))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: notiAdditionalPath.map{curveLife(cos: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func operation(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(userNegativeName))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func criminalRecordReplyCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(appSecurePath) + String(show_pingContent))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func bodyToo(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: k_hisLevelId.map{comeMay(something: $0)}, encoding: .utf8)!: roomId, String(bytes: show_unknownWarningStr.map{rationalMotive(sibling: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(app_willPartMessage.suffix(6)) + "tRoo" + String(constSmoothReportPath.prefix(5)) + k_segmentContent.replacingOccurrences(of: "rolling", with: "ck"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func announcementCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(userMatchId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func completeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(userHabitDonValue.prefix(8)) + String(main_lowEventTitle.suffix(6)) + "mbers")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func textCompletion(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (dataOrangeDamageMeetName.replacingOccurrences(of: "expression", with: "la") + String(kSaltMessage.prefix(7)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: dataGeneralId.map{rollingAverage(flexible: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func combineShrink(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(kNightSatisfyScopeUrl.prefix(5)) + "er/qu" + String(data_easeKey.prefix(4)) + "tatus")
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: data_saltRateText.reversed(), encoding: .utf8)!: toUids, String(bytes: show_portionNatureFormat.reversed(), encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tableCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(const_finishKey) + String(userBarCenterData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func sort(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ExplosiveTraceDetectionRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(main_wayData.suffix(5)) + "er/remark")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: user_spanData.map{recoverBench(each: $0)}, encoding: .utf8)!: content, String(bytes: notiMethodUrl.map{revenueRandom(vertical: $0)}, encoding: .utf8)!: isTop, String(bytes: notiAdditionalPath.map{curveLife(cos: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        user_formalMsg.endurance(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
