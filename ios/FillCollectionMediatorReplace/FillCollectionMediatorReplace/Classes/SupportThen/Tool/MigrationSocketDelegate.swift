
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let constDayStr:[Character] = ["S","O","C","K","E","T","_","A","D"]
fileprivate let kShelfKey:[Character] = ["D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let const_beamData:[UInt8] = [0x5e,0x42,0x4e,0x46,0x48,0x59,0x52,0x5b,0x44,0x49,0x48,0x42,0x4e,0x45,0x4c,0x59,0x52,0x5f,0x48,0x5c,0x58,0x48,0x5e,0x59,0x4e,0x4c,0x41,0x41,0x52,0x43,0x42,0x59,0x44,0x4b,0x44,0x4e,0x4c,0x59,0x44,0x42,0x43]

private func concludeClearly(can num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let showNapUrl:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let kInvitationUrl:String = "isCabig resume margin fact"
fileprivate let userYearMessage:String = "lladjustmentg"
 
/*: "SOCKET 连接成功." :*/
fileprivate let dataAsideFormat:String = "SOCKET form measurement"
fileprivate let show_fitInmatePath:String = "连接成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let const_blackName:String = "net balloon depth warn othersSOCKET "
fileprivate let dataScoreId:[Character] = ["连","接"]
fileprivate let appYesValue:String = "失败, erresent explore sick up"

/*: "SOCKET 失败 :*/
fileprivate let noti_ehName:[Character] = ["S","O"]
fileprivate let app_balloonStr:String = "CKET 失败person makeup take shirt outer"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let app_entryStr:String = "次, fore"
fileprivate let show_pressureText:String = "outside sense criminalket域名重新尝试"
fileprivate let notiEgoYesterdayMsg:[Character] = ["\u{ff0c}","地","址","\u{ff1a}"]

/*: "SOCKET 已关闭, code: :*/
fileprivate let app_edgeData:[Character] = ["S","O","C","K","E"]
fileprivate let user_ditText:[Character] = ["T"," ","已","关","闭",","," "]
fileprivate let appCottonMessage:String = "recording possible enable loopcode:"

/*: , reason: :*/
fileprivate let showAgoSayMsg:String = "job appearance regular, re"

/*: /?sid= :*/
fileprivate let noti_beautyName:[Character] = ["/","?","s","i","d","="]

/*: &uid= :*/
fileprivate let notiEhTeeTitle:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let constAbsoluteUrl:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let noti_insertContent:[Character] = ["&","p","a","c","k","a","g","e"]
fileprivate let main_labelData:String = "project quitId="

/*: "1v1server. :*/
fileprivate let show_keyData:[Character] = ["1","v","1","s","e","r","v","e","r"]
fileprivate let dataAverageId:String = "condition"

/*: .com:7766" :*/
fileprivate let data_videoTruthStr:[Character] = [".","c","o","m",":","7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let app_entityName:String = "4visitor.1"
fileprivate let userSocialData:String = ".194:handlehandle"
fileprivate let showStateStr:String = "67"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let user_draftPointName:[UInt8] = [0x14,0x10,0x4,0xc,0x6,0x15,0xe1,0xa8,0x7e,0x52,0xa8,0x7c,0x5d,0x33,0x26,0x22,0x24,0x29,0x22,0x23,0x2a,0x2d,0x2a,0x35,0x3a,0x4,0x29,0x22,0x2f,0x28,0x26,0x25,0xa8,0x5c,0x52,0xa6,0x51,0x6d,0xb0,0x7d,0x4d,0xa7,0x57,0x6e,0xa8,0x7e,0x52,0xa6,0x46,0x74,0xaa,0x58,0x6e,0xef]

fileprivate func professionalBirthday(signal num: UInt8) -> UInt8 {
    let value = Int(num) + 63
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let appRoundUrl:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let userWriteMultiWhereValue:String = "pinevent"

/*: "callHeartbeat" :*/
fileprivate let mainRestrictionEditMsg:[Character] = ["c","a","l","l","H"]
fileprivate let showTalkName:[Character] = ["e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let const_existName:[Character] = ["r","e","c","i","e","v","e"]
fileprivate let k_assessmentUrl:[Character] = ["d","D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let dataCancerDirectMsg:[Character] = ["l","i","v","e"]
fileprivate let noti_phoneText:[Character] = ["H","e","a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let main_calculateBeamData:String = "🔗 \u{5173}闭 "
fileprivate let main_forceKey:String = "sockquotet"

/*: "The user logs out" :*/
fileprivate let appLeadingSlimPath:String = "problem capital one introThe "
fileprivate let user_delayEncounterUrl:[Character] = ["g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let app_resumeData:String = "SOCaroundET"
fileprivate let userScenePath:String = " "

/*: 重连. 重试次数： :*/
fileprivate let constServerData:String = "重连. 重"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let mainProblemSoldierAvailableFormat:String = "SOCKeach"
fileprivate let showJourneyData:String = " \u{4e0d}需\u{8981}重"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let showTransitionValue:String = "socke"
fileprivate let main_recallNativeName:String = " 不需\u{8981}"
fileprivate let noti_problemName:[Character] = ["重","新","连","\u{63a5}","."]

/*: "&resume=1" :*/
fileprivate let main_optionText:String = "two capable day medal&res"

/*: "&lang= :*/
fileprivate let show_delayUrl:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let user_handleMessage:[Character] = ["S","O","C","K","E","T"]
fileprivate let app_combinedMessage:String = " 开始连"

/*: "onNetDisabled" :*/
fileprivate let showExcessReadingPath:[Character] = ["o","n","N","e","t","D","i","s","a","b","l","e","d"]

/*: "errno" :*/
fileprivate let app_logData:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let noti_observeMessage:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let main_showData:[Character] = ["S","O","C","K","E"]
fileprivate let appDivideValue:String = "T 连\u{63a5}保\u{6d3b}c"
fileprivate let show_skinName:String = "nevertheless feed remark custommd: p"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let show_survivalKey:[UInt8] = [0xa3,0xbf,0xb3,0xbb,0xb5,0xa4,0xd0,0x84,0x82,0x84,0x93,0x14,0x4f,0x6d,0x16,0x44,0x4b,0x93,0x9d,0x94,0xca,0xd0,0x93,0x91,0x9c,0x9c,0xb8,0x95,0x91,0x82,0x84,0x92,0x95,0x91,0x84,0xde]

private func messageHalf(illegal num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let main_resolutionTitle:[UInt8] = [0xc2,0xde,0xd2,0xda,0xd4,0xc5,0xb1,0xfd,0xf8,0xe7,0xf4,0x75,0x2e,0xc,0x77,0x25,0x2a,0xf2,0xfc,0xf5,0xab,0xb1,0xfd,0xf8,0xe7,0xf4,0xd9,0xf4,0xf0,0xe3,0xe5,0xd3,0xf4,0xf0,0xe5,0xbf]

private func candleHolderCollect(dimension num: UInt8) -> UInt8 {
    return num ^ 145
}

/*: "onP" :*/
fileprivate let userMenuParaTitle:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let main_beautifulMissUrl:String = "lifesg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let mainKingKey:String = "🔗 处理消息\u{ff1a}c"
fileprivate let userChartRefuseStr:[Character] = ["m","d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let data_rationalMsg:String = "dominant ya better, errNo:"

/*: , msg: :*/
fileprivate let showTapId:String = "fast begin correct, msg"
fileprivate let mainThemId:String = ":"

/*: , data: :*/
fileprivate let show_directionPath:String = "notice "
fileprivate let dataDealBusyTryPath:[Character] = ["d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let notiCornerAirTitle:String = "SOCfail"
fileprivate let notiLimitConName:String = "异常\u{6d88}息\u{ff1a}"

/*: "onReceivedGift" :*/
fileprivate let constParticlePath:[Character] = ["o","n","R","e","c","e","i","v"]
fileprivate let const_wayKey:[Character] = ["e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let constCurrencyPath:[Character] = ["o","n","V","i","d","e","o","M"]
fileprivate let showObserverSongName:String = "ATCH"

/*: "onRequestCall" :*/
fileprivate let noti_fairName:[Character] = ["o","n","R","e","q"]
fileprivate let dataPinSatisfyUrl:[Character] = ["u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let dataStepUrl:[Character] = ["o","n","C","a","n","c","e","l"]
fileprivate let const_fullMessageText:String = "Callserver uniform save"

/*: "onRefuseCall" :*/
fileprivate let notiWithValue:[Character] = ["o","n","R","e","f","u","s"]
fileprivate let noti_socialUrl:String = "pack neighbor king progress readyeCall"

/*: "onAcceptCall" :*/
fileprivate let constArrayBehindDueTitle:String = "O"
fileprivate let mainPauseMessage:String = "nAcchand retain equally premium tolerance"

/*: "onStopCall" :*/
fileprivate let const_randomId:String = "valid"
fileprivate let userBalloonFormat:[Character] = ["n","S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let userRegularPath:String = "freeCaold shall its gallery provider"

/*: "mfCoin" :*/
fileprivate let kCivilPlantPath:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let showMemberInjurySearchionPath:String = "app each speakeronMissC"
fileprivate let user_feedbackPath:String = "cancerin"

/*: "acceptCall" :*/
fileprivate let const_exclusiveName:String = "abonde"
fileprivate let noti_marriedId:String = "handle"

/*: "stopCall" :*/
fileprivate let userShirtBrushData:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let noti_largeValue:[Character] = ["r","e","q","u","e","s","t","C","a"]
fileprivate let user_transportText:String = "LL"

/*: "[Invite you to a video call...]" :*/
fileprivate let appEstimatedName:[UInt8] = [0xbb,0xa9,0xce,0xd6,0xc9,0xd4,0xc5,0x80,0xd9,0xcf,0xd5,0x80,0xd4,0xcf,0x80,0xc1,0x80,0xd6,0xc9,0xc4,0xc5,0xcf,0x80,0xc3,0xc1,0xcc,0xcc,0x8e,0x8e,0x8e,0xbd]

fileprivate func rejectCost(press num: UInt8) -> UInt8 {
    let value = Int(num) + 160
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let app_yesKey:String = "tyqualitye"

/*: "[Invite you to a voice call...]" :*/
fileprivate let user_failureStr:[UInt8] = [0x56,0x44,0x63,0x7b,0x64,0x79,0x68,0x2d,0x74,0x62,0x78,0x2d,0x79,0x62,0x2d,0x6c,0x2d,0x7b,0x62,0x64,0x6e,0x68,0x2d,0x6e,0x6c,0x61,0x61,0x23,0x23,0x23,0x50]

private func schemeSumo(stand num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "uid" :*/
fileprivate let userCreationMessage:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let app_introTitle:String = "nicontentknam"
fileprivate let k_styleFormat:String = "under"

/*: "headPic" :*/
fileprivate let main_filmKey:String = "seed appeal organize evaluate cancelheadPi"
fileprivate let app_introKindValue:String = "already"

/*: "onCallResume" :*/
fileprivate let const_emissionKey:[Character] = ["o","n","C","a","l","l","R","e","s"]
fileprivate let user_pressMessage:String = "ucircle"

/*: "cancelCall" :*/
fileprivate let app_purchasePath:String = "pureanpuree"
fileprivate let k_headKey:String = "song say booklCall"

/*: "refuseCall" :*/
fileprivate let app_absFlagData:String = "refuseCallast language object married"
fileprivate let dataRegularValue:String = "when"

/*: "onCallMotivation" :*/
fileprivate let noti_othersName:String = "unknown selfonCall"
fileprivate let show_secretValue:String = "processingion"

/*: "onLiveWarning" :*/
fileprivate let mainShotMediumId:[Character] = ["o","n","L","i","v","e","W","a","r","n","i","n"]
fileprivate let data_deadlineUrl:String = "mic"

/*: "reason" :*/
fileprivate let userAttractiveName:String = "reassessmenton"

/*: "time" :*/
fileprivate let notiPickPath:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let user_designFormat:String = "onStcondition framework begin"

/*: "onSendProgramGift" :*/
fileprivate let notiLastMsg:String = "onSendPbroad condition doc weak"
fileprivate let show_freshmanRollingData:[Character] = ["r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let k_fadeName:[Character] = ["o","n","G","a","m","e","F"]
fileprivate let user_resultName:[Character] = ["s"]

/*: "broadStatus" :*/
fileprivate let data_progressContent:[Character] = ["b","r","o","a","d","S","t","a","t"]
fileprivate let appPositionTitle:String = "lights"

/*: "onCallChatMsg" :*/
fileprivate let constForceFormat:String = "determination anyonCall"
fileprivate let dataSignalUrl:String = "ChatMsgplan version"

/*: "onNotificationPopupWin" :*/
fileprivate let notiRecoverSFocusId:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n"]
fileprivate let showTotaleractionValue:[Character] = ["P","o"]
fileprivate let const_goldMessage:[Character] = ["p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let const_replaceFormat:String = "onUseapprove definition color"
fileprivate let notiComeLegalAppearName:[Character] = ["r","B","l","o","c","k"]

/*: "isRecharge" :*/
fileprivate let showTextFormat:String = "better want environment core goldisRech"
fileprivate let kHisKey:String = "aliterale"

/*: "source" :*/
fileprivate let showDirectionStr:String = "sourfamilye"

/*: "invitePop" :*/
fileprivate let user_whyTitle:[Character] = ["i","n","v","i","t","e","P","o","p"]

/*: "videoPop" :*/
fileprivate let showLifeName:String = "fast identityvideoPop"

/*: "coin" :*/
fileprivate let notiExhibitKey:[Character] = ["c","o","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MigrationSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kDailyData = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let k_formatKey = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let k_productionUrl = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let k_hugeTitle = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let noti_maleId = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let main_pageValue = (String(constDayStr) + String(kShelfKey))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let showCancelName = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let data_barUrl = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kAgeRouteMessage = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let appTableFormat = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let const_succeedData = NSNotification.Name(rawValue: String(bytes: const_beamData.map{concludeClearly(can: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let user_locationStr = NSNotification.Name(rawValue: String(bytes: showNapUrl.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ClingRealmObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func quality(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol ExtraMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func layerData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func am(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ElapidObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func roundEvaluate(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func addLight(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func programmeNumbererval(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func unneeded(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol DuplexPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func telecasting(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func adopt(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func engineering(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class MigrationSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum StatusLanguage: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currMutualVC: AnyObject?
    var currMutualVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CombinedLanguageManager.shared.direction == .rightToLeft ? 0 : show_explainId,
        //: y: StatusBarHeight+190
        y: dataInfoTrustName + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CombinedLanguageManager.shared.direction == .rightToLeft ? 0 : show_explainId,
        //: y: StatusBarHeight+190
        y: dataInfoTrustName + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ClingRealmObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: ExtraMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ElapidObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: DuplexPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isMutual = false
    @objc dynamic var isMutual = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = MigrationSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(stockPurchasePlan(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(noneAdvancedGroup),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(kInvitationUrl.prefix(4)) + userYearMessage.replacingOccurrences(of: "adjustment", with: "in")))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (WhiteThen.shared as! WhiteThen).homeSplit()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isMutual")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (WhiteThen.shared as! WhiteThen).homeSplit()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension MigrationSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        NiftyLogTool.paragraphFromRank(msg: (String(dataAsideFormat.prefix(7)) + show_fitInmatePath.capitalized))
        //: sendPing()
        installDown()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: data_tagMsg,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: main_pageValue)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        orientationDetect()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        NiftyLogTool.paragraphFromRank(msg: (String(const_blackName.suffix(7)) + String(dataScoreId) + String(appYesValue.prefix(6)) + "ror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = AppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = AppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == k_hugeTitle {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = actual(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            NiftyLogTool.paragraphFromRank(msg: (String(noti_ehName) + String(app_balloonStr.prefix(7))) + "\(k_hugeTitle)" + (app_entryStr.replacingOccurrences(of: "fore", with: "切") + "换过Soc" + String(show_pressureText.suffix(9)) + String(notiEgoYesterdayMsg)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.statisticalForType(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.atGap(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        NiftyLogTool.paragraphFromRank(msg: (String(app_edgeData) + String(user_ditText) + String(appCottonMessage.suffix(5))) + "\(code)" + (String(showAgoSayMsg.suffix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.limitedSame(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == appTableFormat {
            //: return
            return
        }
        //: if let status = AppManager.share.reachability?.connection, status != .unavailable {
        if let status = AppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.statisticalForType(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func actual(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = AppManager.share.appConfigMode.ws
        self.linkAddrArr = AppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = roundTranslate()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: main_pageValue)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = AppManager.share.loginUid
        let uid = AppManager.share.loginUid
        //: let sid = AppManager.share.loginSessionId
        let sid = AppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(noti_beautyName)) + "\(sid)" + (String(notiEhTeeTitle)) + "\(uid)" + (String(constAbsoluteUrl)) + "\(noti_randomValue)" + (String(noti_insertContent) + String(main_labelData.suffix(3))) + "\(mainAppId)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func roundTranslate() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return mainFailureMethodValue ? [(String(show_keyData) + dataAverageId.replacingOccurrences(of: "condition", with: ".")) + "\(data_lineUrl)" + (String(data_videoTruthStr))] : [(app_entityName.replacingOccurrences(of: "visitor", with: "3") + "29.185" + userSocialData.replacingOccurrences(of: "handle", with: "7") + showStateStr.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func stockPurchasePlan(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            NiftyLogTool.paragraphFromRank(msg: String(bytes: user_draftPointName.map{professionalBirthday(signal: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.limitedSame(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.statisticalForType(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func noneAdvancedGroup() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.statisticalForType(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.theData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func installDown() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kDailyData, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.renderOffDraft(info: [(String(appRoundUrl)): (userWriteMultiWhereValue.replacingOccurrences(of: "event", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func basicCognitiveProcess() {
        //: endTalkingPing()
        scopeMethod()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: k_formatKey, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isMutual || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currMutualVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currMutualVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isMutual || (self.reasonWindow()?.isKind(of: TelecastingStartErrorDelegate.self) == false && self.reasonWindow()?.isKind(of: StillViewController.self) == false) && (self.currMutualVC?.isKind(of: TelecastingStartErrorDelegate.self) == false && self.currMutualVC?.isKind(of: StillViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.renderOffDraft(info: [(String(appRoundUrl)): (String(mainRestrictionEditMsg) + String(showTalkName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func scopeMethod() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func theData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(const_existName) + String(k_assessmentUrl))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < const_mineUrl {
                //: AppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                AppManager.share.syllabus(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func encampSoapAbout() {
        //: endLivePing()
        deviceFull()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: k_productionUrl, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.renderOffDraft(info: [(String(appRoundUrl)): (String(dataCancerDirectMsg) + String(noti_phoneText))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func deviceFull() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func orientationDetect() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = LapContainerView()
        //: let keyWindow = DelineateThen.getWindow()
        let keyWindow = DelineateThen.premiumOperation()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isMutual
        self.containerView?.isHidden = !self.isMutual
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func insideOf() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard AfterPushListener.at().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func limitedSame(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (main_calculateBeamData + main_forceKey.replacingOccurrences(of: "quote", with: "e")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: appTableFormat, reason: (String(appLeadingSlimPath.suffix(4)) + "user lo" + String(user_delayEncounterUrl)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        scopeMethod()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            insideOf()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func barb() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = MigrationSocketDelegate.shared.actual(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.statisticalForType(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func statisticalForType(timeInterval: TimeInterval, type: StatusLanguage) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            NiftyLogTool.paragraphFromRank(msg: (app_resumeData.replacingOccurrences(of: "around", with: "K") + userScenePath.capitalized) + "\(type.rawValue)" + (constServerData.capitalized + "试次数\u{ff1a}") + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.smallIndefiniteSumMotivation(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func smallIndefiniteSumMotivation(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                NiftyLogTool.paragraphFromRank(msg: (mainProblemSoldierAvailableFormat.replacingOccurrences(of: "each", with: "ET") + " 连接正常" + showJourneyData + "新连接."))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                NiftyLogTool.paragraphFromRank(msg: (showTransitionValue.uppercased() + "T 正在连接" + main_recallNativeName + String(noti_problemName)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isMutual {
        if self.isMutual {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(main_optionText.suffix(4)) + "ume=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(show_delayUrl)) + "\(YesterdayAnotherAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        limitedSame(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = noti_maleId
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        NiftyLogTool.paragraphFromRank(msg: (String(user_handleMessage) + app_combinedMessage.capitalized + "\u{63a5}, \u{5730}址：") + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func renderOffDraft(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if AppManager.share.reachability?.connection == nil || AppManager.share.reachability?.connection == .unavailable {
        if AppManager.share.reachability?.connection == nil || AppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(appRoundUrl)): (String(showExcessReadingPath)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(app_logData)): "\(kAgeRouteMessage)",
                                       //: "data": [:]]
                                       (String(noti_observeMessage)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            atGap(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(appRoundUrl))] as? String == (userWriteMultiWhereValue.replacingOccurrences(of: "event", with: "g")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                NiftyLogTool.paragraphFromRank(msg: (String(main_showData) + appDivideValue + String(show_skinName.suffix(5)) + "ing."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(appRoundUrl))] as? String == (String(mainRestrictionEditMsg) + String(showTalkName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                NiftyLogTool.paragraphFromRank(msg: String(bytes: show_survivalKey.map{messageHalf(illegal: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(appRoundUrl))] as? String == (String(dataCancerDirectMsg) + String(noti_phoneText)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                NiftyLogTool.paragraphFromRank(msg: String(bytes: main_resolutionTitle.map{candleHolderCollect(dimension: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(appRoundUrl))] as? String == (userWriteMultiWhereValue.replacingOccurrences(of: "event", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(appRoundUrl)): (String(userMenuParaTitle)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(app_logData)): "\(showCancelName)",
                                           //: "data": [:]]
                                           (String(noti_observeMessage)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                atGap(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.statisticalForType(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func atGap(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(appRoundUrl))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(app_logData))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(main_beautifulMissUrl.replacingOccurrences(of: "life", with: "m"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(noti_observeMessage))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (mainKingKey + String(userChartRefuseStr)) + "\(cmdName)" + (String(data_rationalMsg.suffix(8))) + "\(errorNo)" + (String(showTapId.suffix(5)) + mainThemId.capitalized) + "\(msg)" + (show_directionPath.replacingOccurrences(of: "notice", with: ",") + String(dataDealBusyTryPath)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        scaleBody(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            NiftyLogTool.paragraphFromRank(msg: (notiCornerAirTitle.replacingOccurrences(of: "fail", with: "K") + "ET 接收" + notiLimitConName + "cmdName:") + "\(cmdName)" + (String(data_rationalMsg.suffix(8))) + "\(errorNo)" + (String(showTapId.suffix(5)) + mainThemId.capitalized) + "\(msg)" + (show_directionPath.replacingOccurrences(of: "notice", with: ",") + String(dataDealBusyTryPath)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: AppManager.share.loginUserMode.freeCallTimes = 0
                AppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !AppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !AppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                PopupWindowManager.shared.verification()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.quality(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(constParticlePath) + String(const_wayKey)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: appPackageLogErrMessage, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(constCurrencyPath) + showObserverSongName.lowercased()) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.layerData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(noti_fairName) + String(dataPinSatisfyUrl)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.am(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(dataStepUrl) + String(const_fullMessageText.prefix(4))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(notiWithValue) + String(noti_socialUrl.suffix(5))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (constArrayBehindDueTitle.lowercased() + String(mainPauseMessage.prefix(4)) + "eptCall") { // 接收方发送acceptCall回执
            //: self.isMutual = true
            self.isMutual = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            basicCognitiveProcess()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.adopt(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (const_randomId.replacingOccurrences(of: "valid", with: "o") + String(userBalloonFormat)) { // 接收方发送stopCall回执
            //: self.isMutual = false
            self.isMutual = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            scopeMethod()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(userRegularPath.prefix(6)) + "llTimes")] as? Int {
                //: AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: AppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AppManager.share.loginUserMode.mf_coin = json[(String(noti_observeMessage))][(String(kCivilPlantPath))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            ban(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: showBeautyOfStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (WhiteThen.shared as! WhiteThen).employed()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(showMemberInjurySearchionPath.suffix(7)) + user_feedbackPath.replacingOccurrences(of: "cancer", with: "o")) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.unneeded(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (const_exclusiveName.replacingOccurrences(of: "bond", with: "cc") + "ptCa" + noti_marriedId.replacingOccurrences(of: "handle", with: "ll")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isMutual = true
            self.isMutual = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            basicCognitiveProcess()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.roundEvaluate(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(userShirtBrushData)) { // 接收方/拨打方 挂断了通话
            //: self.isMutual = false
            self.isMutual = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            scopeMethod()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.addLight(data: data)
            // 更新余额
            //: AppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AppManager.share.loginUserMode.mf_coin = json[(String(noti_observeMessage))][(String(kCivilPlantPath))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(userRegularPath.prefix(6)) + "llTimes")] as? Int {
                //: AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                AppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            ban(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: showBeautyOfStr, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (WhiteThen.shared as! WhiteThen).employed()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(noti_largeValue) + user_transportText.lowercased()) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: const_succeedData, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: AppManager.share.recieve1v1TalkRequest(info: data)
                AppManager.share.syllabus(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(const_existName) + String(k_assessmentUrl))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: appEstimatedName.map{rejectCost(press: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(noti_observeMessage))][(app_yesKey.replacingOccurrences(of: "quality", with: "p"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: user_failureStr.map{schemeSumo(stand: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                LanguageThen.transitionManager(uid: String(json[(String(noti_observeMessage))][(String(userCreationMessage))].intValue),
                                                    //: title: data["nickname"] as? String,
                                                    title: data[(app_introTitle.replacingOccurrences(of: "content", with: "c") + k_styleFormat.replacingOccurrences(of: "under", with: "e"))] as? String,
                                                    //: body: body,
                                                    body: body,
                                                    //: imageUrl: data["headPic"] as? String)
                                                    imageUrl: data[(String(main_filmKey.suffix(6)) + app_introKindValue.replacingOccurrences(of: "already", with: "c"))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                LanguageThen.shared.builder()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(const_emissionKey) + user_pressMessage.replacingOccurrences(of: "circle", with: "me")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isMutual = true
            self.isMutual = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            basicCognitiveProcess()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(const_emissionKey) + user_pressMessage.replacingOccurrences(of: "circle", with: "me"))] = "1"
            //: AppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            AppManager.share.genderPropaganda(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (WhiteThen.shared as! WhiteThen).homeSplit()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (app_purchasePath.replacingOccurrences(of: "pure", with: "c") + String(k_headKey.suffix(5))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isMutual = false
            self.isMutual = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            scopeMethod()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.programmeNumbererval(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                LanguageThen.minimizeIdentifier(identifier: String(json[(String(noti_observeMessage))][(String(userCreationMessage))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                LanguageThen.shared.confirm()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(app_absFlagData.prefix(9)) + dataRegularValue.replacingOccurrences(of: "when", with: "l")) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isMutual = false
            self.isMutual = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            scopeMethod()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.telecasting(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(noti_othersName.suffix(6)) + "Motiva" + show_secretValue.replacingOccurrences(of: "processing", with: "t")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if AppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.engineering?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(mainShotMediumId) + data_deadlineUrl.replacingOccurrences(of: "mic", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            AfterPushListener.beauty(content: msg, reason: json[(String(noti_observeMessage))][(userAttractiveName.replacingOccurrences(of: "assessment", with: "as"))].stringValue, time: json[(String(noti_observeMessage))][(String(notiPickPath))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(user_designFormat.prefix(4)) + "opLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AfterPushListener.at().attributeAll()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(notiLastMsg.prefix(7)) + String(show_freshmanRollingData)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.sirenSong(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(k_fadeName) + String(user_resultName)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(data_progressContent) + appPositionTitle.replacingOccurrences(of: "light", with: "u"))] as? Int ?? -1
            //: if AppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if AppManager.share.appStatus == EarnLetterCustomReflectable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: user_locationStr, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: appPackageLogErrMessage, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: user_locationStr, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(constForceFormat.suffix(6)) + String(dataSignalUrl.prefix(7))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            LightDanmuManager.itemSession().botch(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(notiRecoverSFocusId) + String(showTotaleractionValue) + String(const_goldMessage)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            PopupWindowManager.shared.smoothSystem(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(const_replaceFormat.prefix(5)) + String(notiComeLegalAppearName)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            AfterPushListener.at().attributeAll()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: main_domainColorErrorStr, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension MigrationSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func scaleBody(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(noti_largeValue) + user_transportText.lowercased()) || cmdName == (String(noti_fairName) + String(dataPinSatisfyUrl)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if AfterPushListener.at().isLive == true {
                //: NEED_RESTORE_LIVE = true
                showSelectCornerWithoutMessage = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                AfterPushListener.black(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                AfterPushListener.at().attributeAll()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard showSelectCornerWithoutMessage == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (app_purchasePath.replacingOccurrences(of: "pure", with: "c") + String(k_headKey.suffix(5))) || cmdName == (String(dataStepUrl) + String(const_fullMessageText.prefix(4))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(app_absFlagData.prefix(9)) + dataRegularValue.replacingOccurrences(of: "when", with: "l")) || cmdName == (String(notiWithValue) + String(noti_socialUrl.suffix(5))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(userShirtBrushData)) || cmdName == (const_randomId.replacingOccurrences(of: "valid", with: "o") + String(userBalloonFormat))
        {
            //: NEED_RESTORE_LIVE = false
            showSelectCornerWithoutMessage = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ClingView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                AfterPushListener.at().aboutThe()
            }
            //: alert.show()
            alert.libraryYearStat()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func ban(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        SchemeRatingManager.shared.roundAppealRating()
        //: guard AppManager.share.loginUserMode.sex == Gender.male.rawValue else {
        guard AppManager.share.loginUserMode.sex == SharedMirrorKingCustomReflectable.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(showTextFormat.suffix(6)) + kHisKey.replacingOccurrences(of: "literal", with: "rg"))] as? Bool == false {
            //: ElementEarnReactiveCompatible.share.func_pushToDiscountRechargeView()
            ElementEarnReactiveCompatible.share.playReceiveApplication()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(showDirectionStr.replacingOccurrences(of: "family", with: "c"))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(user_whyTitle)) || source == (String(showLifeName.suffix(8))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(String(notiExhibitKey))] as? Int) ?? 0 < 60 {
            //: ElementEarnReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            ElementEarnReactiveCompatible.share.repulse(webViewType: .RechargeHalfPage)
        }
    }
}
