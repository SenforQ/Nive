
//: Declare String Begin

/*: "http://" :*/
fileprivate let notiHugeMessage:[Character] = ["h","t","t","p",":","/"]
fileprivate let mainMinimizeUrl:[Character] = ["/"]

/*: "https://" :*/
fileprivate let noti_masterMessage:String = "https://edition host handle yard robot"

/*: "http:// :*/
fileprivate let dataLanguageMessage:String = "http:easy detailed session chamber"
fileprivate let k_centerOntId:[Character] = ["/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let notiStrokeOfText:[Character] = ["s","e","r","v","e","r","S","e","l","c","t","I","n","d","e","x"]
fileprivate let constCareTitle:String = " = "

/*: "serverUrlStr =  :*/
fileprivate let mainMyMsg:[Character] = ["s","e","r","v","e","r","U","r"]
fileprivate let data_factorSenseId:String = "one shall reminder as systemlStr = "

/*: "app. :*/
fileprivate let k_dryName:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let k_easeMakeMsg:String = ".comexplain coat phase you disable"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let const_exposureKey:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let notiShotStr:[Character] = ["m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let app_upperEarnSuiteTitle:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let userSeparateValue:[Character] = ["u","s","e","r","/","a","p","p"]
fileprivate let dataLiverUrl:String = "leLoginobject since vertical low"

/*: "IM/getMyBlackList" :*/
fileprivate let data_localExtraName:String = "IM/gdraw domain insert adopt prison"
fileprivate let showAdvantagePath:[Character] = ["k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let userGroupFormat:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","M"]
fileprivate let const_oxygenFrontStr:String = "SG"

/*: "mf/business/sendGift" :*/
fileprivate let k_prisonConcludeData:String = "mf/bucarve called change carrier"
fileprivate let kShPath:[Character] = ["s","/","s"]
fileprivate let kToneFeeData:String = "scale checkendGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let main_dotMessage:String = "mf/cmid challenge breeze"
fileprivate let appMissingMsg:String = "oom/harassment profile say"

/*: "GJ21FB" :*/
fileprivate let mainTheValue:String = "sustain21FB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let k_allAdditionData:[Character] = ["\u{5b8c}","成","了","检","\u{6d4b}",",","\u{8bf7}","求","状","\u{6001}","正","\u{5e38}"]

/*: "接口请求失败" :*/
fileprivate let dataPersonName:String = "接口\u{8bf7}求失"
fileprivate let user_appId:[Character] = ["败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let show_blueValue:[Character] = ["完","\u{6210}","了","检","测",",","\u{9700}","要","更","换","s","e","r","v","e","r"," ","u","r"]
fileprivate let show_divisionTitle:String = "L"

/*: "HTTPProxy" :*/
fileprivate let showMutualNativeMessage:String = "label quoteHTTPProxy"

/*: "__SCOPED__" :*/
fileprivate let app_surePlaceData:[Character] = ["_","_","S","C","O"]
fileprivate let showMineTopicLimitMessage:[Character] = ["P","E","D","_","_"]

/*: "tap" :*/
fileprivate let noti_bondMsg:String = "tabirthday"

/*: "tun" :*/
fileprivate let mainOpportunityPath:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let showButName:String = "ipsepull"

/*: "ppp" :*/
fileprivate let show_threadMessage:String = "definitiondefinitiondefinition"

/*: "func__updateTalkingCookies =  :*/
fileprivate let data_everyoneWaveLocationKey:String = "menu context discfunc__"
fileprivate let k_draftValue:String = "mirrork"
fileprivate let const_thanksFindPath:String = "ingCunder vacation info pressure"
fileprivate let data_serviceMsg:String = "s = provider equity err container"

/*: "https:// :*/
fileprivate let show_centerKey:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let showScriptData:[UInt8] = [0x75,0x66,0x7d,0x70,0x4c,0x4c,0x77,0x76,0x7f,0x76,0x67,0x76,0x40,0x72,0x7e,0x76,0x50,0x7c,0x7c,0x78,0x7a,0x76,0x60,0x44,0x7a,0x67,0x7b,0x46,0x61,0x7f,0x33,0x2e,0x33]

private func beautifulEgo(regulate num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "UID" :*/
fileprivate let dataInmateKey:String = "UexclusiveD"

/*: "PHPSESSID" :*/
fileprivate let dataPinUrl:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let dataPlantFormat:[Character] = ["f","u","n","c","_","_"]
fileprivate let noti_tooMsg:String = "voiceve"
fileprivate let show_importantMsg:String = "job dialog video say appealookies = "

/*: "func__saveAllCookies  error" :*/
fileprivate let dataMinutePath:String = "fill snow contact statement failfunc_"
fileprivate let show_installId:[Character] = ["l","l","C","o","o"]
fileprivate let kMemoryOfftStr:String = "star find writekies "

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  YesterdayAnotherAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum RealmNameLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class YesterdayAnotherAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = YesterdayAnotherAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        hideInGreet()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(notiHugeMessage) + String(mainMinimizeUrl))) || newValue.hasPrefix((String(noti_masterMessage.prefix(8)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if mainFailureMethodValue && !constConversationName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dataLanguageMessage.prefix(5)) + String(k_centerOntId)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dataLanguageMessage.prefix(5)) + String(k_centerOntId)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    soundAttention(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func hideInGreet() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = data_cameraFormalValue.integer(forKey: show_underText)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(notiStrokeOfText) + constCareTitle.capitalized) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = tagDaily()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiHugeMessage) + String(mainMinimizeUrl))
        //: if Environment_Formal && !Encryption_Request {
        if mainFailureMethodValue && !constConversationName {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiHugeMessage) + String(mainMinimizeUrl))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(mainMyMsg) + String(data_factorSenseId.suffix(7))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func tagDaily() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func unitOfTime(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(k_dryName)) + "\(data_lineUrl)" + (String(k_easeMakeMsg.prefix(4))), (String(k_dryName)) + "\(data_lineUrl)" + (String(k_easeMakeMsg.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(const_exposureKey) + String(notiShotStr)),
         //: "recharge/applePayNotify",
         (String(app_upperEarnSuiteTitle)),
         //: "user/appleLogin"] + slowPathArr1
         (String(userSeparateValue) + String(dataLiverUrl.prefix(7)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(data_localExtraName.prefix(4)) + "etMyBlac" + String(showAdvantagePath)),
         //: "mf/business/sendMsg",
         (String(userGroupFormat) + const_oxygenFrontStr.lowercased()),
         //: "mf/business/sendGift",
         (String(k_prisonConcludeData.prefix(5)) + "sines" + String(kShPath) + String(kToneFeeData.suffix(7))),
         //: "mf/chatRoom/sendMsg"]
         (String(main_dotMessage.prefix(4)) + "hatR" + String(appMissingMsg.prefix(4)) + "sendMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = DelineateThen.getSystemLangCode()
        let lang = DelineateThen.termsTransform()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .bridgeAccelerate(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (mainTheValue.replacingOccurrences(of: "sustain", with: "GJ")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension YesterdayAnotherAddrTool {
    //: func checkServerUrl() {
    func onTogether() {
        //: if AppManager.share.networkStatus != .Unavailable {
        if AppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            promptAdd()
            //: } else {
        } else {
            //: func_starCodeTime()
            labelWithExample()
        }
    }

    //: func func_starCodeTime() {
    func labelWithExample() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.promptAdd()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func comeOut() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func promptAdd() {
        //: if AppManager.share.networkStatus == .Unavailable {
        if AppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        comeOut()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        SchemeManagerRequest.limitedSh { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(k_allAdditionData)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (dataPersonName + String(user_appId)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.labelWithExample()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(show_blueValue) + show_divisionTitle.lowercased()))
                    //: self.func__changeHostUrl()
                    self.cognisantLabel()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func cognisantLabel() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        comeOut()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        data_cameraFormalValue.set(serverSelctIndex, forKey: show_underText)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiHugeMessage) + String(mainMinimizeUrl))
        //: if Environment_Formal && !Encryption_Request {
        if mainFailureMethodValue, !constConversationName {
            //: serverPrefix = "http://"
            serverPrefix = (String(notiHugeMessage) + String(mainMinimizeUrl))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension YesterdayAnotherAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func progressProxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(showMutualNativeMessage.suffix(9)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func ejector() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(app_surePlaceData) + String(showMineTopicLimitMessage))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((noti_bondMsg.replacingOccurrences(of: "birthday", with: "p"))) || keyStr.contains((String(mainOpportunityPath))) || keyStr.contains((showButName.replacingOccurrences(of: "pull", with: "c"))) || keyStr.contains((show_threadMessage.replacingOccurrences(of: "definition", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension YesterdayAnotherAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func soundAttention(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(data_everyoneWaveLocationKey.suffix(6)) + "updateTa" + k_draftValue.replacingOccurrences(of: "mirror", with: "l") + String(const_thanksFindPath.prefix(4)) + "ookie" + String(data_serviceMsg.prefix(4))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = positiveUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(notiHugeMessage) + String(mainMinimizeUrl)), with: (String(noti_masterMessage.prefix(8))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(noti_masterMessage.prefix(8)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(show_centerKey)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            veryButton(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        convertTo()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func veryButton(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: showScriptData.map{beautifulEgo(regulate: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func positiveUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(notiHugeMessage) + String(mainMinimizeUrl))
        //: if Environment_Formal && !Encryption_Request {
        if mainFailureMethodValue, !constConversationName {
            //: serverPrefix = "https://"
            serverPrefix = (String(noti_masterMessage.prefix(8)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(k_dryName)) + "\(data_lineUrl)" + (String(k_easeMakeMsg.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func pulchritude(note _: Notification) {
        //: if AppManager.share.loginUid.isEmptyString {
        if AppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            pairStorage()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            okSucceedVersion()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func pairStorage() {
        //: var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataInmateKey.replacingOccurrences(of: "exclusive", with: "I")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(dataPinUrl)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(dataPinUrl))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataInmateKey.replacingOccurrences(of: "exclusive", with: "I")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(dataPinUrl)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(dataPinUrl))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))), h5Cookie.keys.contains((dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(dataPinUrl))), h5Cookie.keys.contains((String(dataPinUrl))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(dataPinUrl))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(dataPinUrl))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func okSucceedVersion() {
        //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == AppManager.share.loginUserMode.userID {
            if cookie.name == (dataInmateKey.replacingOccurrences(of: "exclusive", with: "I")), cookie.value == AppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(dataPinUrl)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataInmateKey.replacingOccurrences(of: "exclusive", with: "I")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(dataPinUrl)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(dataPinUrl))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(dataInmateKey.replacingOccurrences(of: "exclusive", with: "I"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(dataPinUrl))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(dataPinUrl))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func convertTo() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(dataPlantFormat) + noti_tooMsg.replacingOccurrences(of: "voice", with: "sa") + "AllC" + String(show_importantMsg.suffix(9))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            data_cameraFormalValue.set(cookieData, forKey: user_styleName)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(dataMinutePath.suffix(5)) + "_saveA" + String(show_installId) + String(kMemoryOfftStr.suffix(5)) + " error"))
        }
    }

    //: func func__setAppCookies() {
    func commentRenderCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = data_cameraFormalValue.object(forKey: user_styleName)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func fallBehind() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        data_cameraFormalValue.removeObject(forKey: user_styleName)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
