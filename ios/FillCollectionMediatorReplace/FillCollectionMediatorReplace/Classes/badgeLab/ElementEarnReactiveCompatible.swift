
//: Declare String Begin

/*: "fee" :*/
fileprivate let mainCooperativeOutcomeAppealFormat:String = "pice"

/*: "VIPFee" :*/
fileprivate let const_advantageName:[Character] = ["V","I","P","F","e","e"]

/*: "chattingNum" :*/
fileprivate let show_goingPath:String = "greatestat"
fileprivate let show_errKey:[Character] = ["t","i","n","g","N","u","m"]

/*: "You're already in her live room" :*/
fileprivate let constAboutMessage:[UInt8] = [0x1f,0x35,0x3b,0xed,0x38,0x2b,0xe6,0x27,0x32,0x38,0x2b,0x27,0x2a,0x3f,0xe6,0x2f,0x34,0xe6,0x2e,0x2b,0x38,0xe6,0x32,0x2f,0x3c,0x2b,0xe6,0x38,0x35,0x35,0x33]

fileprivate func arrowThick(abs num: UInt8) -> UInt8 {
    let value = Int(num) + 58
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "toUid" :*/
fileprivate let userFaultRenderFloatStr:[UInt8] = [0x9d,0x98,0x7e,0x92,0x8d]

fileprivate func quantityGood(boy num: UInt8) -> UInt8 {
    let value = Int(num) + 215
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "momentId" :*/
fileprivate let data_selectCounterestTitle:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "extra" :*/
fileprivate let showWeeklyBottleCreationContent:[UInt8] = [0xae,0xb3,0xbf,0xb9,0xaa]

private func padGender(his num: UInt8) -> UInt8 {
    return num ^ 203
}

/*: "uid" :*/
fileprivate let main_foodValue:[UInt8] = [0x33,0x2f,0x22]

/*: "onlineStatus" :*/
fileprivate let noti_flexibleTitle:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let k_dotPath:[UInt8] = [0xc1,0xdb,0xe6,0xcd,0xdf,0xfd,0xdb,0xcd,0xda]

private func healthyElect(address num: UInt8) -> UInt8 {
    return num ^ 168
}

/*: "userStatus" :*/
fileprivate let kProfessionalBalloonStatementMessage:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x72,0x65,0x73,0x75]

/*: "Account is restricted！" :*/
fileprivate let data_farKey:String = "Acelectric partner tender"
fileprivate let main_followingId:String = "COUN"
fileprivate let showVisitorKey:String = "esbudget"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let userOrientationData:[UInt8] = [0x16,0x31,0x39,0x3c,0x35,0x34,0x70,0x24,0x3f,0x70,0x35,0x3e,0x24,0x35,0x22,0x70,0x24,0x38,0x35,0x70,0x33,0x38,0x31,0x24,0x70,0x22,0x3f,0x3f,0x3d,0x7e,0x70,0x0,0x3c,0x35,0x31,0x23,0x35,0x70,0x24,0x22,0x29,0x70,0x31,0x37,0x31,0x39,0x3e,0x70,0x3c,0x31,0x24,0x35,0x22]

private func enchiridionContend(image num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "url" :*/
fileprivate let appIntervalId:String = "umeasurementl"

/*: "userDetail" :*/
fileprivate let app_maximumName:String = "userDattach special character coat"
fileprivate let notiMultiData:[Character] = ["e","t","a","i","l"]

/*: "&type=6" :*/
fileprivate let main_componentMsg:String = "&type=6cost cloud restore limited official"

/*: "&type=7" :*/
fileprivate let dataObjectUrl:[Character] = ["&","t","y","p","e","="]
fileprivate let notiMarginValue:String = "pow"

/*: "amount" :*/
fileprivate let k_songTechnologyData:[UInt8] = [0x3b,0x37,0x35,0x2f,0x34,0x2e]

/*: "https://" :*/
fileprivate let data_libraryMessage:String = "hbouncebounce"
fileprivate let main_platformData:[Character] = ["p","s",":","/","/"]

/*: "http://" :*/
fileprivate let noti_siblingTransmissionId:String = "http://character only analyze currency finish"

/*: "transparency" :*/
fileprivate let main_taKey:String = "trancollectpare"
fileprivate let data_multiNaturalName:[Character] = ["n","c","y"]

/*: "version= :*/
fileprivate let userRationalName:String = "vhungrs"

/*: &packageId= :*/
fileprivate let notiChangeTitle:String = "&pbroadcast native beautiful too"
fileprivate let data_manageStr:[Character] = ["a","c","k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let constDoingMessage:String = "delay information&bund"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ElementEarnReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class ElementEarnReactiveCompatible: NSObject {
public class ElementEarnReactiveCompatible: NSObject {
    //: @objc static public let share = ElementEarnReactiveCompatible()
    @objc public static let share = ElementEarnReactiveCompatible()
    //: private override init() {}
    override private init() {}
}

//: extension ElementEarnReactiveCompatible {
extension ElementEarnReactiveCompatible {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func accept(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        noti_nearbyStr.tamperConnect(eventID: appOkKey)
        //: guard TalkingSocketManager.shared.isMutual == false else {
        guard MigrationSocketDelegate.shared.isMutual == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.diskTerms(showMsg: notiLocalText)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        EditThen.recordUpon { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            InsideReactiveCompatible.incomeFit { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = SalesViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(mainCooperativeOutcomeAppealFormat.replacingOccurrences(of: "pic", with: "fe"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(const_advantageName))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(show_goingPath.replacingOccurrences(of: "greatest", with: "ch") + String(show_errKey))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.reasonWindow()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func recoverVc() {
        //: AppManager.share.userFillInfoMode = UserFillInfoModel.init()
        AppManager.share.userFillInfoMode = BlueLetterInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = EchoGenderVc()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = modal()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func eventBack(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = modal()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: LoginViewController.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: YesterdaySuckViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = LoginViewController()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func datePress(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.reasonWindow() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? EmigrationViewController, AfterPushListener.at().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.miniTranslate()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? SuckObjectProtocol {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.approximateWithout()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = SuckObjectProtocol(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        pushMove()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func calledPerforate(uid: String, enterType: CustomEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard EditThen.beanPlantRecording() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(AfterPushListener.at().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.diskTerms(showMsg: String(bytes: constAboutMessage.map{arrowThick(abs: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        BlueReactiveCompatible().elementPlace(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = ElementModelType.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.modal()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: CreaseThen.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! CreaseThen).magnitudeerrupt()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            AfterPushListener.at().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = CreaseThen()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                noti_nearbyStr.tamperConnect(eventID: constPingId, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                noti_nearbyStr.tamperConnect(eventID: showNumberMsg, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func soldier(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = WhiteObjectProtocol(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = modal()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func totalAgain(chatID: String, isFrom: LabelFromEnum = .Normal, completion: ((_ vc: ChatViewDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != DelineateThen.getXiaoMiID() {
        if chatID != DelineateThen.slowModify() { // 系统消息
            //: ProgressHUD.show()
            UniversalProgressHUD.addUp()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: userFaultRenderFloatStr.map{quantityGood(boy: $0)}, encoding: .utf8)!: chatID, String(bytes: data_selectCounterestTitle.reversed(), encoding: .utf8)!: "0", String(bytes: showWeeklyBottleCreationContent.map{padGender(his: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            HouseThen.refreshOnce(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                UniversalProgressHUD.coverBetween()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = AccomplishThen.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = AccomplishThen.funcRequire(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                FiniteInfoManager.syllabusParty(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: main_foodValue.map{$0^70}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: noti_flexibleTitle.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: k_dotPath.map{healthyElect(address: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: kProfessionalBalloonStatementMessage.reversed(), encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: const_transformMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.diskTerms(showMsg: (String(data_farKey.prefix(2)) + main_followingId.lowercased() + "t is r" + showVisitorKey.replacingOccurrences(of: "budget", with: "tr") + "icted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.streak(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: AccomplishThen())
                let chatVC = self.streak(chatID: chatID, isFrom: isFrom, userWrap: AccomplishThen())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: AccomplishThen ) -> TalkingPrivateChatController {
    func streak(chatID: String, isFrom: LabelFromEnum = .Normal, userWrap: AccomplishThen) -> ChatViewDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = modal()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ChatViewDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ChatViewDelegate
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ChatViewDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func ofRecover(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        UniversalProgressHUD.addUp()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        HouseThen.bodyToo(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            UniversalProgressHUD.coverBetween()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.diskTerms(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if DelineateThen.isGroupChat(groupId) {
            if DelineateThen.replacing(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.editorRe(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = StartControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.modal()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.editorRe(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = StartControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.modal()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.diskTerms(showMsg: String(bytes: userOrientationData.map{enchiridionContend(image: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func editorRe(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.modal()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: StartControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! StartControllerDelegate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func driveSecond(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: AppManager.share.loginUserMode.isTPAuth)
        let tpAuth = DirectionQuickLookable(rawValue: AppManager.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = PositViewController()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = modal()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = ThinkViewController()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = modal()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func choroidCoatYear(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func oner() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = modal()
        //: let vc = TalkingFeedbackVC.init()
        let vc = SubsequentlyFieldDelegate()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func periodOfTimeModel(jumpModel: AssignTransformable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (appIntervalId.replacingOccurrences(of: "measurement", with: "r")) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            lab(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(app_maximumName.prefix(5)) + String(notiMultiData)) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func universalAdvancedVc() {
        //: ElementEarnReactiveCompatible.share.func__pushToWebVC(webViewType: .SubscribePage)
        ElementEarnReactiveCompatible.share.panelling(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func switcherPwd(type: BackClusterLiteral, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = OtherThen()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        modal()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension ElementEarnReactiveCompatible {
extension ElementEarnReactiveCompatible {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func panelling(webViewType: ExtraScalarLiteral) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        fullOn(webViewType: webViewType, webConfig: AnotherConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func graduatedTableButton(popupCallIndex: Int, appendParams: String = (String(main_componentMsg.prefix(7)))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = BindThen()
        //: view.show()
        view.betweenContainer()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.repulse(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                hairProgress(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func recharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(dataObjectUrl) + notiMarginValue.replacingOccurrences(of: "pow", with: "7"))) {
        //: let payWinType = AppManager.share.appUserConfigMode.payWinType
        let payWinType = AppManager.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = BindThen()
        //: view.show()
        view.betweenContainer()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (AppManager.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (AppManager.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.repulse(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                hairProgress(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func hairProgress(appendParams: String = (String(dataObjectUrl) + notiMarginValue.replacingOccurrences(of: "pow", with: "7"))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = AfterManager.stagePrice(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: ElementEarnReactiveCompatible.share.func__pushToWebVC(urlStr: urlStr)
        ElementEarnReactiveCompatible.share.lab(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        noti_nearbyStr.bottomStr(eventID: data_postRecordMsg, parameterStr: [String(bytes: k_songTechnologyData.map{$0^90}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func repulse(webViewType: ExtraScalarLiteral, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = AnotherConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        fullOn(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            userWindowTitle = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                noti_nearbyStr.bottomStr(eventID: clickEvent, parameterStr: [String(bytes: k_songTechnologyData.map{$0^90}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func playReceiveApplication() {
        //: guard !AppManager.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !AppManager.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = AppManager.share.appConfigMode.urlH5Domain + AppManager.share.appUserConfigMode.discountUrl
        let httpsUrl = AppManager.share.appConfigMode.urlH5Domain + AppManager.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (data_libraryMessage.replacingOccurrences(of: "bounce", with: "t") + String(main_platformData)), with: (String(noti_siblingTransmissionId.prefix(7))))
        //: ElementEarnReactiveCompatible.share.func__pushToWebVC(urlStr: httpUrl)
        ElementEarnReactiveCompatible.share.lab(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func fullOn(webViewType: ExtraScalarLiteral, webConfig: AnotherConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = AfterManager.stagePrice(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        lewiston(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func lab(urlStr: String?, webConfig: AnotherConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? AnotherConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        lewiston(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func lewiston(urlStr: String?, webViewType: ExtraScalarLiteral?, webConfig: AnotherConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(main_taKey.replacingOccurrences(of: "collect", with: "s") + String(data_multiNaturalName))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (userRationalName.replacingOccurrences(of: "hung", with: "e") + "ion=") + "\(noti_randomValue)" + (String(notiChangeTitle.prefix(2)) + String(data_manageStr)) + "\(mainAppId)" + (String(constDoingMessage.suffix(5)) + "leId=") + "\(notiMarginPath)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = GuidanceViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = modal() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: GuidanceViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! GuidanceViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.stateFee(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_explainId, height: show_explainId / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension ElementEarnReactiveCompatible {
public extension ElementEarnReactiveCompatible {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func modal() -> UIViewController? {
        //: return currentViewController()
        return reasonWindow()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct AssignTransformable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
