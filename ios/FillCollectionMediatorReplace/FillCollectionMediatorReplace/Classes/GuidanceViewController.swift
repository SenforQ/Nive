
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiSeparateTitle:[UInt8] = [0xc1,0xc6,0xc1,0xdc,0x80,0xcb,0xc7,0xcc,0xcd,0xda,0x92,0x81,0x88,0xc0,0xc9,0xdb,0x88,0xc6,0xc7,0xdc,0x88,0xca,0xcd,0xcd,0xc6,0x88,0xc1,0xc5,0xd8,0xc4,0xcd,0xc5,0xcd,0xc6,0xdc,0xcd,0xcc]

/*: "Load..." :*/
fileprivate let appRatingBoneText:String = "storage opening sureLoad.."
fileprivate let k_readContextIconMessage:String = "."

/*: "Skip" :*/
fileprivate let user_creaseBulletUrl:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let user_northStyleElementStr:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let noti_weekPath:[UInt8] = [0x8,0x16,0x11,0x1b,0x10,0x8,0x51,0x10,0x11,0x2f,0x1e,0x18,0x1a,0x2c,0x17,0x10,0x8,0x59,0x59,0x10,0x11,0x2f,0x1e,0x18,0x1a,0x2c,0x17,0x10,0x8,0x57,0x56,0x44]

private func justHeight(illegal num: UInt8) -> UInt8 {
    return num ^ 127
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let k_reMsg:String = "jsEvenbefore date broad"
fileprivate let appTraceFormat:String = "harvest recognizegeSho"
fileprivate let showTodayUrl:[Character] = ["w",")",":"," "]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let dataSourceMsg:[UInt8] = [0x8f,0x81,0x86,0x7c,0x87,0x8f,0x46,0x87,0x86,0x68,0x79,0x7f,0x7d,0x60,0x81,0x7c,0x7d,0x3e,0x3e,0x87,0x86,0x68,0x79,0x7f,0x7d,0x60,0x81,0x7c,0x7d,0x40,0x41,0x53]

fileprivate func threeClothes(threshold num: UInt8) -> UInt8 {
    let value = Int(num) - 24
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let show_reWeeklyMessage:String = "jsEventlive must ping greet"
fileprivate let showTrackDevoteUrl:[Character] = ["(","o","n","P","a"]
fileprivate let app_transportUrl:[Character] = ["g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let dataThanText:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let mainClearlyMsg:String = "balloon coordinate mention promptcanG"

/*: "PHPSESSID= :*/
fileprivate let const_scaleUrl:[Character] = ["P","H","P","S","E","S","S"]
fileprivate let noti_lengthDifferenceMessage:String = "transition creation recording year shelfID="

/*: ";uid= :*/
fileprivate let kWhoHungText:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let const_separateMsg:[Character] = ["C","o","o","k","i"]
fileprivate let data_prepareFormat:String = "show"

/*: "nav_back_black_nor" :*/
fileprivate let showHappyTitle:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c","k","_","n","o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let kFastPath:[Character] = ["i","c","o","_","i","n","t"]
fileprivate let show_statuteData:String = "imate_into under aircraft secure female"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let show_benchData:[UInt8] = [0x3d,0x36,0x3a,0x2c,0x34,0x3c,0x37,0x2d,0x77,0x3e,0x3c,0x2d,0x1c,0x35,0x3c,0x34,0x3c,0x37,0x2d,0x2a,0x1b,0x20,0xd,0x38,0x3e,0x17,0x38,0x34,0x3c,0x71,0x7e,0x3b,0x36,0x3d,0x20,0x7e,0x70,0x2,0x69,0x4,0x77,0x2a,0x2d,0x20,0x35,0x3c,0x77,0x3b,0x38,0x3a,0x32,0x3e,0x2b,0x36,0x2c,0x37,0x3d,0x64,0x7e,0x2b,0x3e,0x3b,0x38,0x71,0x69,0x75,0x69,0x75,0x69,0x75,0x69,0x70,0x7e]

private func perSurgery(bull num: UInt8) -> UInt8 {
    return num ^ 89
}

/*: "Alert" :*/
fileprivate let app_contrastMessage:String = "Alertheight determination"

/*: "Cancle" :*/
fileprivate let main_destroyMemoryTempStr:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let notiJawText:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let show_signData:String = "hold income without sweetjs c"
fileprivate let noti_carHairSPath:String = "ethod context spread beam bring tell"
fileprivate let notiCognitiveMessage:String = "bear ehname = "

/*: , message =  :*/
fileprivate let showAroundFormat:[Character] = [","," ","m","e","s","s","a","g","e"," ","="]
fileprivate let data_micKey:String = "regulate"

/*: "http://" :*/
fileprivate let kDegreeMsg:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let kTodayFormat:String = "hsubjectsubjectp"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kAssistantFormat:[UInt8] = [0xdf,0xc8,0xdb,0x89,0xca,0xc6,0xc6,0xc2,0xc0,0xcc,0xe7,0xc8,0xc4,0xcc,0xda,0x89,0x94,0x89,0xcd,0xc6,0xca,0xdc,0xc4,0xcc,0xc7,0xdd,0x87,0xca,0xc6,0xc6,0xc2,0xc0,0xcc,0x87,0xda,0xd9,0xc5,0xc0,0xdd,0x81,0x8e,0x92,0x89,0x8e,0x80,0x87,0xc4,0xc8,0xd9,0x81,0xcf,0xdc,0xc7,0xca,0xdd,0xc0,0xc6,0xc7,0x81,0xca,0xc6,0xc6,0xc2,0xc0,0xcc,0x80,0x89,0xd2,0x89,0xdb,0xcc,0xdd,0xdc,0xdb,0xc7,0x89,0xca,0xc6,0xc6,0xc2,0xc0,0xcc,0x87,0xda,0xd9,0xc5,0xc0,0xdd,0x81,0x8e,0x94,0x8e,0x80,0xf2,0x99,0xf4,0x89,0xd4,0x89,0x80,0x92,0xa3]

private func handleGlobal(hand num: UInt8) -> UInt8 {
    return num ^ 169
}

/*: "uid= :*/
fileprivate let app_recognizeFormat:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let show_produceValue:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let data_manualId:String = ";path=/print live"

/*: "document.cookie=' :*/
fileprivate let const_blueKey:[Character] = ["d","o","c","u","m","e","n","t",".","c","o","o","k","i","e","=","\'"]

/*: ;domin= :*/
fileprivate let constNeedTitle:String = ";domioxygen problem production host"
fileprivate let noti_harshStr:String = "n=camera woman"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let k_mouthData:[UInt8] = [0x57,0x58,0x1e,0x16,0x5d,0x51,0x51,0x55,0x57,0x5b,0x70,0x5f,0x53,0x5b,0x4d,0x10,0x57,0x50,0x5a,0x5b,0x46,0x71,0x58,0x16,0x19,0x6e,0x76,0x6e,0x6d,0x7b,0x6d,0x6d,0x77,0x7a,0x19,0x17,0x1e,0x3,0x3,0x1e,0x13,0xf,0x17,0x1e,0x45,0x1e,0x5a,0x51,0x5d,0x4b,0x53,0x5b,0x50,0x4a,0x10,0x5d,0x51,0x51,0x55,0x57,0x5b,0x3,0x19]

private func dropMain(openly num: UInt8) -> UInt8 {
    return num ^ 62
}

/*: "h5Domain =  :*/
fileprivate let dataWrittenPromptName:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let userLaterValue:String = "job slide server accountgetA"
fileprivate let const_complyId:String = "kies = mouth explore pause space easy"

/*: "PHPSESSID" :*/
fileprivate let showRefTitle:String = "automaticallyHautomaticallyS"

/*: "uid" :*/
fileprivate let app_medicationEquallyNeedUrl:String = "disappearid"

/*: "appCookies =  :*/
fileprivate let kEnterpriseKey:String = "appCpro determination island never your"
fileprivate let user_accelerateData:String = "spot businesses = "

/*: "UID" :*/
fileprivate let userSpecKey:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let app_shelfOpeningId:String = "navirevenuea"
fileprivate let mainRouteId:[Character] = ["s","e","r"]
fileprivate let user_prospectMsg:String = "Agentreserve abs normally"

/*: "xunwu_iphone" :*/
fileprivate let show_rowStr:[Character] = ["x","u","n","w","u","_","i","p","h"]
fileprivate let appGoldData:String = "ONE"

/*: " xunwu_device_id/ :*/
fileprivate let data_saveRunReaderMsg:[Character] = [" ","x","u","n","w","u","_","d","e","v"]
fileprivate let appEngineeringUrl:[Character] = ["i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let notiPlanMsg:String = "xunwu_sum fresh"
fileprivate let user_approveStr:String = " xunadvice cancer"
fileprivate let userHerId:String = "approximately manager found carrier estimatedsion/"

/*:  xunwu_package/ :*/
fileprivate let userProfessionalName:String = " xundeadline m due"
fileprivate let constPersonalConcernDetectPath:String = "ackage/may pair recent handle calculate"

/*:  xunwu_bundleId/ :*/
fileprivate let dataMatchExcessDayMsg:String = " xunwopportunity third manage zone"
fileprivate let app_automaticallyTitle:String = "ndleId/principal speaker class"

/*:   webkit/1" :*/
fileprivate let main_trustMsg:String = "  wetake chart successfully art"
fileprivate let mainBySheId:String = "bkit/1victory year per cognitive"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GuidanceViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class GuidanceViewController: BalloonSupportThen {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: AnotherConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: ExtraScalarLiteral?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: ExtraScalarLiteral?, webConfig: AnotherConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiSeparateTitle.map{$0^168}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        personal()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        noneventWeek()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(appRatingBoneText.suffix(6)) + k_readContextIconMessage.capitalized)
        //: addNotification()
        towardJoin()
        //: func__checkConfig()
        startTerms()
        //: func__setUpWebView()
        sinceSplit()
        //: func__registerObserver()
        valueObserver()
        //: func__beginStartRequest()
        enableer()
        //: func_checkAppleIAP()
        mopUpIap()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: ElementEarnReactiveCompatible.share.func__pushToWebVC(webViewType: .DiscountPopup)
                ElementEarnReactiveCompatible.share.panelling(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: user_cleanPath, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            belowClear(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.retirementProgram(), .font: UIFont.shareOff(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(user_creaseBulletUrl)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension GuidanceViewController {
    /// 添加通知
    //: private func addNotification() {
    private func towardJoin() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(figure),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: appLessTitle,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(personal),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func figure() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_northStyleElementStr.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func personal() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: noti_weekPath.map{justHeight(illegal: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_reMsg.prefix(6)) + "t(onPa" + String(appTraceFormat.suffix(5)) + String(showTodayUrl)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func noneventWeek() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dataSourceMsg.map{threeClothes(threshold: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(show_reWeeklyMessage.prefix(7)) + String(showTrackDevoteUrl) + String(app_transportUrl)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension GuidanceViewController {
    //: func func__registerObserver() {
    func valueObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(dataThanText)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(mainClearlyMsg.suffix(4)) + "oBack"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(cosView), name: showBeautyOfStr, object: nil)
    }

    //: func func__setUpWebView() {
    func sinceSplit() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        opFor()
        //: func__addBridgeMethod()
        overcrossingMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.referenceAndNonsolidColour()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            UniversalProgressHUD.increase(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(dataInfoTrustName)
            }
        }
    }

    //: func func__beginStartRequest() {
    func enableer() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.ditRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            ditRequest()
        }
    }

    //: func func__startRequest() {
    func ditRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = AppManager.share.loginUid
        let uid = AppManager.share.loginUid
        //: let ssid = AppManager.share.loginSessionId
        let ssid = AppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(const_scaleUrl) + String(noti_lengthDifferenceMessage.suffix(3))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(kWhoHungText)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(const_separateMsg) + data_prepareFormat.replacingOccurrences(of: "show", with: "e")))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        rateRe()
        //: func__initRightButton()
        holdfastButton()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func mopUpIap() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ManagerThen.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.sumry(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ManagerThen.shared.weekValue()
        }
    }

    //: func func__initLeftButton() {
    func counternalise() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.scheme(name: (String(showHappyTitle))), style: .plain, target: self, action: #selector(styleSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func holdfastButton() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(ExtraScalarLiteral.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.scheme(name: (String(kFastPath) + String(show_statuteData.prefix(6)) + "list_sm")), style: .plain, target: self, action: #selector(randomCommon))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func styleSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        bill()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.reasonWindow() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: GuidanceViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! GuidanceViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! GuidanceViewController).personal()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func startTerms() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func enclose(sucess: Bool) {
        //: ProgressHUD.dismiss()
        UniversalProgressHUD.coverBetween()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func rateRe() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: show_benchData.map{perSurgery(bull: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func windowCurrent(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: ElementEarnReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            ElementEarnReactiveCompatible.share.lab(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func popularity(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func cosView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: showBeautyOfStr, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func randomCommon() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = ThinkView(frame: CGRect(x: 0, y: 0, width: show_explainId, height: userSystemKey))
        //: view.show()
        view.studying()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension GuidanceViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            UniversalProgressHUD.increase(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            enclose(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        counternalise()
        //: func__clearJSBgColor()
        rateRe()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            enclose(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            enclose(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(app_contrastMessage.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(main_destroyMemoryTempStr)), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(notiJawText)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension GuidanceViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(show_signData.suffix(4)) + "all m" + String(noti_carHairSPath.prefix(6)) + String(notiCognitiveMessage.suffix(7))) + "\(message.name)" + (String(showAroundFormat) + data_micKey.replacingOccurrences(of: "regulate", with: " ")) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.theNotice(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func overcrossingMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in StillMethodType.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func bill() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in StillMethodType.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension GuidanceViewController {
    //: func func__addUserScript() {
    func putUp() {
        //: var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(kDegreeMsg))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(kDegreeMsg)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((kTodayFormat.replacingOccurrences(of: "subject", with: "t") + "s://")) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (kTodayFormat.replacingOccurrences(of: "subject", with: "t") + "s://"), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = AppManager.share.loginUserMode.userID
        let tempUid: String = AppManager.share.loginUserMode.userID
        //: let tempSessionID: String = AppManager.share.loginSessionId
        let tempSessionID: String = AppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kAssistantFormat.map{handleGlobal(hand: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(app_recognizeFormat)) + "\(tempUid)" + (String(show_produceValue)) + "\(htmlDomain)" + (String(data_manualId.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(const_blueKey)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(const_scaleUrl) + String(noti_lengthDifferenceMessage.suffix(3))) + "\(tempSessionID)" + (String(constNeedTitle.prefix(5)) + String(noti_harshStr.prefix(2))) + "\(htmlDomain)" + (String(data_manualId.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(const_blueKey)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: k_mouthData.map{dropMain(openly: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(app_recognizeFormat)) + "\(tempUid)" + (String(show_produceValue)) + "\(htmlDomain)" + (String(data_manualId.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(const_blueKey)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(app_recognizeFormat)) + "\(tempSessionID)" + (String(show_produceValue)) + "\(htmlDomain)" + (String(data_manualId.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(const_blueKey)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func belowClear(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(dataWrittenPromptName)) + "\(htmlDomain)")
            //: let userID: String = AppManager.share.loginUserMode.userID
            let userID: String = AppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(userLaterValue.suffix(4)) + "llCoo" + String(const_complyId.prefix(7))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (showRefTitle.replacingOccurrences(of: "automatically", with: "P") + "ESSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (app_medicationEquallyNeedUrl.replacingOccurrences(of: "disappear", with: "u")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.enableer()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.haveSession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func haveSession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = AppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(dataWrittenPromptName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: YesterdayAnotherAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(kEnterpriseKey.prefix(4)) + "ooki" + String(user_accelerateData.suffix(5))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(userSpecKey)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (app_medicationEquallyNeedUrl.replacingOccurrences(of: "disappear", with: "u"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (showRefTitle.replacingOccurrences(of: "automatically", with: "P") + "ESSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (showRefTitle.replacingOccurrences(of: "automatically", with: "P") + "ESSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.enableer()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func opFor() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((app_shelfOpeningId.replacingOccurrences(of: "revenue", with: "g") + "tor.u" + String(mainRouteId) + String(user_prospectMsg.prefix(5)))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (app_shelfOpeningId.replacingOccurrences(of: "revenue", with: "g") + "tor.u" + String(mainRouteId) + String(user_prospectMsg.prefix(5))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(show_rowStr) + appGoldData.lowercased())) {
                //: let str = getUserAgentString()
                let str = ping()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.deviceHidden { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(data_saveRunReaderMsg) + String(appEngineeringUrl)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func ping() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(notiPlanMsg.prefix(6)) + "iphone" + String(user_approveStr.prefix(4)) + "wu_ver" + String(userHerId.suffix(5))) + "\(noti_randomValue)" + (String(userProfessionalName.prefix(4)) + "wu_p" + String(constPersonalConcernDetectPath.prefix(7))) + "\(mainAppId)" + (String(dataMatchExcessDayMsg.prefix(5)) + "u_bu" + String(app_automaticallyTitle.prefix(7))) + "\(notiMarginPath)" + (String(main_trustMsg.prefix(4)) + String(mainBySheId.prefix(6)))
    }
}
